# Бигмах — bigmax.uz

Интернет-магазин товаров для малышей и мам с доставкой по Узбекистану.

## Прогресс

### Фаза 0 — Старт ✅ завершена

- [x] **P0-T1** — Монорепо pnpm + scope `@bigmax/*`, базовые конфиги (tsconfig strict, eslint, prettier, husky, commitlint).
- [x] **P0-T2** — Docker Compose: Postgres 16, Redis 7, MinIO + bucket `bigmax-media`, Mailhog, Adminer, Uniteller mock-server.
- [x] **P0-T3** — Prisma schema v1 (23 модели включая Payment/Refund/WebhookEvent/PaymentLog/SavedCard), миграция, идемпотентный seed.
- [x] **P0-T4** — `@bigmax/shared-types`: `BRAND`, `LOCALES`, `localized()`, Zod-enum'ы, money/phone/order-number хелперы.
- [x] **P0-T5** — Next.js 14 App Router `/app/[locale]/...`, shadcn/ui (Radix primitives), landing.
- [x] **P0-T6** — `next-intl` с `defaultLocale: "ru"`, словари `ru/uz/en`, переключатель языка с сохранением пути.
- [x] **P0-T7** — Design tokens: палитра коралл/небесный/мята, шрифты Nunito (body) / Comfortaa (headings).
- [x] **P0-T8** — GitHub Actions CI: typecheck / lint / format / tests / build + проверка синхронности ключей переводов.

### Фаза 1 — Аутентификация и профиль 🔧 в работе

- [x] **P1-T1** — NextAuth v5 (Auth.js), провайдеры `credentials` (email+password) и `otp` (phone, Eskiz.uz mock в dev), OTP-шаблоны на 3 языках, Redis rate-limit 60с.
- [x] **P1-T2** — Страницы `/auth/login`, `/auth/register`, `/auth/verify` с react-hook-form + Zod + shadcn tabs/inputs.
- [x] **P1-T3** — Личный кабинет `/account/*`: профиль (имя/язык/email), смена пароля, sign-out; auth-aware Header.
- [x] **P1-T4** — CRUD адресов доставки (14 регионов + 11 тумани Ташкента, лимит 10/юзер, транзакция `isDefault`).
- [x] **P1-T5** — Role-based middleware (`customer/manager/admin`): защита `/admin/*`, редирект залогиненных с `/auth/*`.
- [x] **P1-T6** — Playwright e2e: auth-login × 3 локали, locale-switching, role-middleware.

### Фаза 2 — Каталог 🔧 в работе

- [x] **P2-T1** — Главная bigmax.uz: hero + 9 категорий + 5 брендов + featured-товары из БД (seed расширен).
- [x] **P2-T2** — `/catalog` (индекс категорий) + `/catalog/[slug]` (карточки товаров, пагинация 24/стр, breadcrumb, 404).
  - [x] **A**: `?pageSize=24|48|60` selector (shadcn Select) с сохранением в href'ах пагинации.
  - [x] **B**: дефолтная сортировка `isFeatured desc → updatedAt desc` (расширенная — P2-T4).
  - [x] **C**: 2-уровневая иерархия категорий в seed (9 top + 6 sub). `getProductsByCategory` агрегирует из прямых детей, чипсы подкатегорий на странице родителя, breadcrumb с parent.
  - [x] **D**: кастомная локаль-осознанная `/[locale]/not-found.tsx` (ru/uz/en).
- [x] **P2-T3** — Фильтры категории (sidebar): цена (в сумах), бренд (чекбоксы, только релевантные), возраст (5 пресетов с overlap-логикой), пол (unisex-inclusive для boy/girl), в наличии (Stock.quantity > 0). URL-driven (`?minPrice=...&brand=chicco&brand=pampers&age=0-6&gender=boy&inStock=true`), сохраняются через pagination/pageSize, debounce 500ms на price-инпутах, кнопка «Сбросить».
- [x] **P2-T4** — Сортировка: `featured` (default, `isFeatured→updatedAt`), `newest` (`createdAt desc`), `price_asc` / `price_desc` (in-memory по min variant price — Prisma не умеет `_min` в relation orderBy). Селектор рядом с pageSize в header'е, сохраняется через пагинацию, fallback на `featured` при невалидном значении.
- [x] **P2-T5** — Карточка товара `/[locale]/product/[slug]`: галерея (next/image + placehold.co placeholders в seed, 12 фото), chip-picker вариантов (цвет/размер/stock, disabled на out-of-stock), цена + old-price + в наличии, breadcrumb с parent-category, таблица характеристик (только non-null), пустое состояние отзывов. `<ProductCard>` теперь рендерит реальные фото.
  - [x] **+QV (scope-expansion)**: Quick View на `<ProductCard>` — overlay-кнопка «Быстрый просмотр» над картинкой (desktop: opacity=0 по-умолчанию, появляется на `group-hover`/`group-focus-within`; mobile: всегда видна), Radix Dialog с центральной модалкой (zoom-in + fade-in анимации). Ленивый fetch ProductDetail через `GET /api/products/[slug]` (Zod slug-validator, `public, s-maxage=30`). Модалка переиспользует `<VariantPicker>` (add-to-cart с toast, работает изнутри Dialog) + `<AttributesTable variant="compact">` (категория/бренд/возраст/пол/цвет/размер/вес). Ссылка «Перейти на страницу товара» → `/product/[slug]`. **Адаптивность**: `w-[calc(100vw-2rem)] max-w-4xl`, `p-4 sm:p-6`, `gap-4 sm:gap-6`, `text-xl sm:text-2xl` для заголовка, картинка ограничена `max-w-[20rem]` на mobile (чтобы влезали варианты без скролла), `max-h-[90vh] overflow-y-auto` для длинного контента. `<AttributesTable>` сконвертирован из Server в Client Component (`useTranslations` вместо `getTranslations`) + новый prop `variant: "default" | "compact"` (compact — 120px label-колонка, меньшие отступы). i18n `quickView.*` × 3 локали (5 ключей, итого 217 × 3). ProductCard переведён с `<Link>`-wrapper на `<article>` с отдельными Link'ами на image/title — иначе nested `<button in a>` ломает HTML. 6 Playwright e2e × 2 проекта (quick-view.spec.ts).
- [x] **P2-T6** — Поиск с автодополнением: `<SearchBox>` в Header (desktop middle / mobile 2-я строка) с дебаунсом 250 мс → `GET /api/search?q=...` (Zod + `public, s-maxage=10` CDN-кеш) → dropdown top-8 (фото / имя / бренд / цена + «Показать все»). ILIKE-поиск по `nameRu/nameUz/nameEn` и `brand.name` (insensitive), защита от out-of-order ответов через `requestIdRef`, навигация стрелками + Enter + Escape, `role="combobox"` / `role="listbox"`. Полная страница `/[locale]/search?q=...` с пагинацией; пустой `q` → `/[locale]/catalog`; `<2` символов → подсказка. i18n: 8 ключей `search.*` × 3 локали.
- [x] **P2-T7** — SEO + hreflang + sitemap + JSON-LD: `seo/config.ts` (`NEXT_PUBLIC_SITE_URL` → fallback `BRAND.url`) + `languageAlternates()` единой точкой истины hreflang. `generateMetadata` на всех страницах: home / catalog index / category / product / search / not-found / account / auth; `<link rel="canonical">` без page/filters/sort на категориях, `robots: noindex, follow` на пагинации/фильтрах/`/search`, `noindex, nofollow` на `/account`, `/auth`. JSON-LD: Organization + WebSite+SearchAction (root), ItemList (catalog index + категории), BreadcrumbList (category + product), Product + Offer/AggregateOffer (UZS, InStock/OutOfStock) — все через `<JsonLd>` с `application/ld+json`. Динамический `app/sitemap.ts` (2 static + 15 категорий + 106 товаров = 123 URL с `alternates.languages` × 3 + x-default→ru) и `app/robots.ts` (Disallow `/api/`, `/*/account/`, `/*/auth/`, `/*/search`, Host + Sitemap). i18n: `seo.*` × 3 локали (12 новых ключей).
- [x] **P2-T8** — Тесты Фазы 2: Vitest в `apps/web` (50 unit на `catalog/filters.ts`, `catalog/sort.ts`, `seo/config.ts`, `seo/json-ld.ts`) + 5 новых Playwright-спецификаций на chromium+mobile-chrome (82 e2e всего с учётом P1): `catalog.spec.ts` (индекс × 3 локали, категория, подкатегория-breadcrumb, регрессия двойного локаль-префикса в пагинации), `filters-sort.spec.ts` (применение фильтра/reset, shadcn Select для sort), `product.spec.ts` (карточка, breadcrumb, 404), `search.spec.ts` (dropdown автокомплита, грид ≥24, пустой q → /catalog), `seo.spec.ts` (sitemap.xml, robots.txt, hreflang+canonical, Organization/WebSite/Product/AggregateOffer JSON-LD, noindex на search и page=2, index на чистом URL).

### Фаза 2 расширение — Category attributes & filters (scope-expansion)

- [x] **Категория-специфичные атрибуты + фильтры**. `Product.attributes` (Prisma `Json?`) хранит `Record<string, string | number | boolean>`. Конфиг per-category slug в [apps/web/src/catalog/category-attributes.ts](apps/web/src/catalog/category-attributes.ts): `enum | range | boolean` поля с i18n-labels для 9 top + 6 sub категорий. URL-схема: `?attr.<key>=val` (enum/bool) + `?attr.<key>.min=X&attr.<key>.max=Y` (range). Server-filter через Prisma JSON-path (`attributes: { path: [key], equals/gte/lte }`). `<CategoryFilters>` динамически рендерит раздел под category.slug. `<AttributesTable>` показывает заполненные атрибуты (enum→i18n label, bool→✓, range→`value unit`). Примеры: **Одежда** — `fabric` (cotton/wool/synthetic/mixed) + `season` (summer/winter/demi/all); **Игрушки** — `material` + `hasSound`; **Питание** — `volumeMl` range + `isOrganic`; **Подгузники** — `diaperSize` (1..6) + `piecesPerPack` range; **Гигиена/Кормление** — `volumeMl`; **Коляски** — `strollerType` + `maxWeightKg`; **Мебель** — `material`. Seed: canonical × 5 с типизированными значениями + 100 mock'ов с детерминированным mapping `n % …` по категории. Миграция `20260423154426_add_product_attributes`. i18n `attributes.*` × 3 локали (42 ключа → 293 total). Тесты: **22 новых unit** (parseAttributeFilters, buildAttributeFiltersQueryString, hasActiveAttributeFilters, buildProductWhere с JSON-path) + **6 e2e × 2 projects** (diapers enum filter режет до 1 карточки, food bool filter режет до 1, «Сбросить» убирает attr.\* из URL).

### Фаза 3 — Корзина и избранное 🔧 в работе

- [x] **P3-T1** — Zustand cart-store с persist в `localStorage` под ключом `bigmax:cart` (версионирован, `partialize` персистит только `items`, onRehydrateStorage выставляет флаг `hydrated` для SSR-safe счётчиков). `CartItem` — snapshot товара (nameRu/Uz/En, бренд, фото, цена в тийнах, цвет/размер) + `quantity` + `addedAt` для «recently added on top». API: `add` (merge по variantId), `remove`, `setQuantity` (≤0 → remove, >99 → clamp), `inc`, `dec` (при qty=1 → remove), `clear`. Селекторы `selectTotalItems` / `selectItemCount` / `selectSubtotalCents` / `selectLineSubtotalCents` + `useCartHydrated()` для Header-бейджа (P3-T5). 24 Vitest-теста (bounds, merge-семантика, persist-ключ, partialize shape).
- [x] **P3-T2** — Cart Sheet с анимацией: shadcn Sheet на Radix Dialog (slide-from-right, fade-overlay, tw-animate keyframes), mount в Header (state в `useCart.isOpen`). `<CartSheet>` — список позиций с фото/название/вариант/qty-controls (inc/dec, remove)/price, subtotal, CTA «Оформить» → `/cart` (P3-T3), empty-state с «Перейти в каталог». `<CartButton>` — иконка-триггер в Header (desktop + mobile nav). `<VariantPicker>` принимает `product` metadata и делает реальный `useCart.add(...)`. i18n: 13 ключей `cart.*` × 3 локали (212 всего). Sub-задачи из «Открытых вопросов»:
  - [x] **A**: add-to-cart → **Sonner toast** (а не auto-open Sheet) с action-кнопкой «Открыть корзину». `<Toaster position="bottom-right" richColors />` в `[locale]/layout.tsx`.
  - [x] **B**: e2e `cart-flow.spec.ts` (4 × 2 projects = 8 сценариев): toast появляется и Sheet НЕ открывается автоматически; Header-кнопка → Sheet → inc/dec/remove → empty state; SearchBox dropdown и Sheet overlay не конфликтуют (клик по CartButton закрывает dropdown и открывает Sheet независимо).
  - [x] **C**: счётчик-бейдж на `<CartButton>` — SSR-safe через `useCartHydrated()` (не рендерится до rehydrate → 0 mismatch), cap «9+» при count>9, `aria-live="polite"` + `sr-only` полная фраза для a11y. Это закрывает функциональность, запланированную в P3-T5 (для корзины).
  - [x] **+Inline (scope-expansion)**: инлайн «В корзину» → `[− qty +]` stepper на `<ProductCard>`. `ProductCardDTO` расширен `defaultVariant: { id, sku, color, size, oldPriceCents, stockQuantity }` (SQL select расширен через `PRODUCT_CARD_SELECT`). `<CardCartControls>` — client-компонент: до add — кнопка «В корзину» (или «Нет в наличии» при OOS); после — inline stepper с `[−] qty [+]`, `dec` при qty=1 удаляет позицию → кнопка возвращается. SSR-safe через `useCartHydrated()` (рендерим кнопку до rehydrate). `e.preventDefault() + stopPropagation()` на всех кликах — чтобы не улетать по родительским Link'ам карточки. Toast при первом add'е с action «Открыть корзину» (как на продуктовой). 4 e2e × 2 projects (card-inline-cart.spec.ts): кнопка → stepper → Header-бейдж растёт (1→2); dec дважды → удаление + бейдж исчезает; клики не навигируют на `/product/[slug]`. Итого e2e: **100/100**.
  - [x] **+Multi-variant chips & stock-cap (scope-extension)**: если `product.variants.length > 1`, `<CardCartControls>` показывает chip-picker (цвет · размер) над кнопкой «В корзину»; клик по чипу выбирает вариант — кнопка/stepper действуют на selected. `ProductCardDTO.variants: ProductCardVariant[]` (`take: 10` в `PRODUCT_CARD_SELECT`). При existing.quantity >= selected.stockQuantity — «+» disabled c title «В наличии: N». Out-of-stock-вариант показывается линёнутым и disabled. **In-cart-badge на чипе**: если вариант уже в корзине, чип получает ring-primary/40 + inline-бейдж с qty (cap «9+»), tooltip добавляет `cart.itemsCount` — юзер сразу видит, какие варианты и сколько уже выбраны. Реализовано через `useCart((s) => s.items)` + `useMemo` map'а — важно: селектор из Zustand НЕ должен возвращать новый литерал-объект каждый раз, иначе — infinite re-render (поймано e2e). 3 e2e × 2 projects (card-variant-picker.spec.ts): чипы белый/голубой на chicco-bodysuit с qty-badge после add; переключение чипа меняет контекст add/stepper; stock-cap на projector (stockQuantity=8 → после 8 inc'ов «+» disabled).
  - [x] **+Stepper в `<VariantPicker>` (продуктовая страница + Quick View, scope-extension)**: раньше был только «В корзину», теперь — после add'а заменяется на `[− qty +]` stepper (`h-11 w-11`, крупнее чем на карточке). Те же правила: SSR-safe через `useCartHydrated()`, stock-cap `existing.quantity >= selected.stockQuantity` → «+» disabled с title «В наличии: N», hard-cap 99, `dec` при qty=1 → remove → возвращается кнопка «В корзину». При переключении variant-чипа stepper/кнопка автоматически переключаются на контекст выбранного варианта. 3 e2e × 2 projects (product-stepper.spec.ts): основной flow на pampers + мульти-вариант на chicco-bodysuit + Quick View модалка.
- [x] **P3-T3** — Страница корзины + промокод: `/[locale]/cart` (server-обёртка с `generateMetadata` + `robots: noindex, follow` + `setRequestLocale` → Client `<CartPageClient>`). Layout — grid на lg: левая колонка со списком позиций (фото, имя, бренд, вариант, inc/dec/remove, per-line total), правая sticky-aside с промо-секцией, строками subtotal/discount/delivery-placeholder/total и CTA «Перейти к оформлению» → `/checkout` (заглушка до P4-T1). Pure promo-хелперы в `apps/web/src/cart/promo.ts` (`computeDiscountCents`, `computeDeliveryDiscountCents`, `isPromoApplicable` — cents integer, без float'ов). Route Handler `GET /api/promo/validate?code=XXX` (Zod + DB + expiry/limit validation, public, `no-store`). `useCart` расширен `appliedPromo: AppliedPromo | null` + `setPromo`/`clearPromo`; `partialize` теперь возвращает `{ items, appliedPromo }`. Auto-cleanup: при падении subtotal ниже `minOrderCents` после remove — `clearPromo()` в effect. `robots.txt` → Disallow `/*/cart` × `/*/cart/`. i18n `cart.page.*` × 3 локали (22 ключа) + `common.cancel/confirm` × 3 локали (итого **241 × 3**). Sub-задачи из «Открытых вопросов»:
  - [x] **A**: `<AlertDialog>` (canonical shadcn на `@radix-ui/react-alert-dialog`) + кнопка «Очистить корзину» в aside. Confirm → `useCart.clear()`; Cancel — без изменений. 4 новых e2e (× 2 projects = 4 total: confirm очищает → empty state; cancel оставляет позиции).
  - [x] **B**: `formatCurrencyUzs(cents, locale, { signDisplay? })` — новый опциональный 3-й параметр (`auto | always | never | exceptZero`) пробрасывается в `Intl.NumberFormat`. Discount на /cart теперь `formatCurrencyUzs(-discountCents, locale)` — ICU ставит корректный minus-sign (U+2212 для ru) без ручной конкатенации. +3 новых unit в `money.test.ts`.
  - [x] **C**: re-validate applied promo при mount `/cart` — `useEffect` делает fetch `/api/promo/validate?code=XXX`; если сервер ответил `!ok` (deactivated / expired / usage-limit) → `clearPromo()` + локализованный toast. `revalidatedCodeRef` защищает от повторных запросов для того же кода (один fetch per visit per code). `AbortController` отменяет при размонтировании. Server-side проверка всё равно будет в P4-T5 checkout — это UX-страховка, не security.
  - Итог: **116 e2e × 2 projects** + **129 unit** (91 `apps/web` + 35 `shared-types` + 3 `notifications`).
- [x] **P3-T4** — Избранное с persist + sync гость→юзер. Zustand-store `useFavorites` в `bigmax:favorites` (version=1, partialize:{items}), snapshot `FavoriteItem` (productId/slug/имена×3/бренд/image/minPrice/addedAt), API: `toggle/add/remove/has/clear/setItems`, селекторы `selectFavoriteItems`/`selectFavoritesCount`, `useFavoritesHydrated()`. Server-layer в `server/catalog.ts`: `getFavoritesForUser(userId)` (join Product с `isActive=true`, снапшот через `toProductCardDTO`), `addFavorite`/`removeFavorite` (idempotent через `@@id([userId, productId])` upsert/delete-ignore-P2025), `syncFavorites` (`createMany { skipDuplicates }`). REST API: `GET/POST/DELETE /api/favorites` + `POST /api/favorites/sync` — все auth-guarded через `auth()`. `<FavoritesSync>` (client, mount-once в root layout): после `useFavoritesHydrated=true` → `GET /api/auth/session` → если `user.id` → `POST /api/favorites/sync {productIds}` → `setItems(serverResponse)` (AbortController на unmount, `didSyncRef` защита от повторов). `<FavoriteButton>` heart-overlay на `<ProductCard>` (always-visible top-right) — локальный toggle + fire-and-forget POST/DELETE на `/api/favorites` (401 для гостей игнорируется). `/[locale]/favorites` — server-обёртка с `robots: noindex, follow` + client `<FavoritesPageClient>` (loader до rehydrate, empty-state или grid карточек). robots.txt → Disallow `/*/favorites[/]`. i18n `favorites.*` × 3 локали (10 ключей, 251 × 3). Sub-задачи из «Открытых вопросов»:
  - [x] **A**: двойное heartbeat-биение при `toggle → added`. Keyframe `heartbeat` в `tailwind.config.ts` (600ms, два scale-пика 1.25×/1.2×), анимация триггерится через `key={beatKey}` на `<Heart>` — React перезапускает CSS-анимацию при bump'е счётчика. На remove-случай без анимации (сердце просто гаснет).
  - [x] **B**: `syncFavorites` теперь фильтрует `guestProductIds` по `product.isActive=true` через предварительный `findMany` — не плодим `Favorite`-записи для deactivated/nonexistent id'шек, которые могли попасть в гостевой localStorage после деактивации товара админом.
  - [x] **C**: унифицирован UX удаления из избранного. На `/favorites` inline `<Trash2>` заменён на тот же `<FavoriteButton>` — heart-toggle работает и как «добавить» на каталоге, и как «убрать» на странице favorites. Консистентное поведение + one-component-for-one-action.
  - **Тесты**: 12 unit (`favorites/store.test.ts`: toggle/add/remove/has/clear/setItems/selectors/persist) + 12 e2e × 2 projects (empty-state, toggle→/favorites, повторный клик удаляет, heart-remove на странице favorites, `noindex` meta, robots Disallow). Sync guest→user verified в smoke (full auth+db integration в P3-T6). Итого **128 e2e + 103 unit web + 35 shared-types**.
- [x] **P3-T5** — Счётчики в шапке. `<FavoritesHeaderButton>` (client, `@/components/favorites/favorites-header-button.tsx`) — Heart-иконка в `<Button asChild>` с `<Link href="/favorites">` и абсолютным бейджем. SSR-safe через `useFavoritesHydrated()` + `selectFavoritesCount`; до rehydrate/при count=0 — скрыт; cap «9+» при count>9; `aria-live="polite"` + `sr-only` ICU-строка `favorites.itemsCount` для screen-reader'ов. Зеркалит pattern `<CartButton>` (P3-T2-C). Интегрирован в обе nav'и Header'а (mobile + desktop) рядом с `<CartButton>`. Без новых i18n-ключей — всё уже в `favorites.*` (openLabel, itemsCount). Тесты: **6 новых e2e × 2 projects** (`header-counters.spec.ts`: скрыт→1→2, cap 9+ через /ru/search?q=mock + 10 toggle'ов, клик → `/ru/favorites`). Итого **134 e2e** passed.
- [x] **P3-T6** — Тесты Фазы 3: расширение coverage по persist/sync/integration-flow. `phase-3-persist.spec.ts` (6 e2e × 2 projects): `cart.items` + `cart.appliedPromo` + `favorites.items` переживают `page.reload()` через Zustand `persist` → `onRehydrateStorage` (проверка `partialize` shape в прод-путях). `phase-3-favorites-sync.spec.ts` (1 e2e × 2): end-to-end `guest→user` sync — гость добавляет товар в favorites → логинится email+password → вызов `syncFavoritesWithServer` из login-form → проверка через Prisma что запись создалась в `Favorite` таблице. **Найдено и пофиксено**: `<FavoritesSync>` mount-once не ретригерится после login (не размонтируется при `router.push`), → гостевые favorites не попадали на сервер. Фикс: экстрагирован reusable `syncFavoritesWithServer(productIds)` helper, login-form (`onEmailSubmit`) теперь явно дёргает его сразу после успешного `signIn`. `phase-3-integration.spec.ts` (1 e2e × 2): full user-journey smoke — 2 карточки в корзину + 1 в избранное через `/ru/search?q=mock` → `/cart` → apply WELCOME10 → плашка «Применён» + строка «Скидка» → `/favorites` → позиция сохранилась → reload → всё persist'нулось. **Итого**: 160 e2e × 2 projects (+8 новых от P3-T6) passed. Unit осталось 120 — P3 store тесты уже были покрыты в P3-T1 (26), P3-T3 (15 promo), P3-T4 (12 favorites).

### Фаза 4 — Оформление заказа и оплата через Uniteller 🔧 в работе

- [x] **P4-T1** — Многошаговый чекаут: Контакты → Адрес → Доставка → Оплата → Подтверждение. Zustand + persist под ключом `bigmax:checkout-draft` (version=1), partialize персистит `currentStep + contacts/address/delivery/payment`. Per-step Zod в `checkout/schemas.ts` (`ContactsStepSchema` с `UZ_PHONE_REGEX`, `AddressStepSchema` с required street/house + refined region, `DeliveryStepSchema` с refine `method='pickup' → branchId required`, `PaymentStepSchema` enum uniteller/cod). Server-обёртка `/[locale]/checkout` с `robots: noindex, nofollow` + `auth()` prefill (name/email/phone) + `prisma.address.findMany` (saved addresses для авт.) + `prisma.storeBranch.findMany` (филиалы для pickup). `<CheckoutPageClient>` — гвард пустой корзины → `redirect('/cart')` через `useEffect`, stepper-индикатор (5 шагов, клик по пройденным возвращает), sticky `<OrderSummary>` с items + subtotal/discount/total (reused promo-helpers). 5 step-компонентов на `react-hook-form + zodResolver`: `<StepContacts>`, `<StepAddress>` (saved-address select + region-dropdown из UZ_REGIONS), `<StepDelivery>` (radio-cards courier/pickup + branch select), `<StepPayment>` (radio-cards uniteller/cod), `<StepReview>` (сводка с «Изменить» для каждой секции). **Кнопка «Оформить заказ» — disabled с tooltip'ом**: полная server-логика (Order creation + Uniteller Signature + redirect на `/pay/`) — в **P4-T5**. Важно: RHF-дефолты для optional text-fields = `""` (не `null`) — Zod `.string()` падает на null до `.optional()`. i18n `checkout.*` × 3 локали (60 ключей, **353 total**). **E2e** `checkout.spec.ts` — 5 сценариев × 2 projects = 10 тестов: guard empty cart → /cart; robots noindex,nofollow; полный happy-path (Contacts → Address → Delivery → Payment → Review + disabled Place Order); валидация Contacts (без email/phone не пропускает); persist draft в localStorage + восстановление `currentStep='address'` после reload.
  - [x] **A (UX на Review)** — Заменил текстовую плашку WIP на shadcn `<Alert>` (новый компонент `components/ui/alert.tsx` с variants `default/destructive/warning/info` на `cva`). Добавил secondary-CTA «Вернуться в корзину» (`<ShoppingCart>` icon, `variant=ghost`) рядом с «Назад». Новые i18n-ключи `checkout.review.{wipTitle, backToCart}`.
  - [x] **B (auth-gate на Review)** — Сервер пробрасывает `isAuthenticated: Boolean(session?.user.id)` в `CheckoutInitial`. Гость на Review видит warning-Alert с `<LogIn>` + `authRequiredTitle/Body` и двумя кнопками `Sign In / Sign Up`, которые ведут на `/auth/login?returnTo=/{locale}/checkout` (и `/register?returnTo=...`) — после успешного входа юзер возвращается ровно на чекаут. Авторизованный видит info-Alert `wipTitle/wipNotice`. Кнопка «Оформить заказ» остаётся disabled на обеих ветках, но `title` tooltip'а зависит от состояния. Новые ключи: `authRequiredTitle, authRequiredBody, signIn, signUp` (× 3 локали, **359 total**). +3 e2e (guest warning-Alert + returnTo-links, secondary «Вернуться в корзину» → /cart, авторизованный видит info-Alert и не видит warning) → 8 × 2 projects = 16.
  - [x] **C (type-safety schemas)** — Убрал `as unknown as AddressStepInput/DeliveryStepInput` escape-касты в форм-дефолтах. Схемы переписал на `z.string().trim().default("")` для optional text-fields: пустая строка = «не указано», output-тип совпадает с RHF-дефолтами 1-в-1, без `.transform()`. Телефон: `z.string().trim().default("").refine((v) => v === "" || UZ_PHONE_REGEX.test(v))`. Соответственно `CheckoutAddress.district/landmark/apartment/street/house/phone` в store теперь `string` (не `string | null`). Миграция persist-schema не нужна — version=1 остался, но сохранённые ранее null'ы всё равно не могли сериализоваться из-за старого бага.
- [x] **P4-T2** — Калькулятор стоимости доставки по районам Ташкента и регионам Узбекистана. Чистый модуль `@bigmax/shared-types/delivery` — зоны + тарифы + SSR-safe `estimateDelivery({method, region, district, subtotalCents}) → DeliveryEstimate` (discriminated union `priced | needs-info`). Single source of truth: тот же расчёт переиспользуется в UI (реактивный preview) и на сервере при создании Order в P4-T5. **Зоны** (тарифы в сумах/tiyin): `tashkent-central` (5 районов: Мирабадский/Мирзо-Улугбек/Шайхантахур/Яккасарай/Юнусабад) 15 000 сум · 1–2 дн; `tashkent-outer` (6 районов: Бектемир/Чиланзар/Олмазор/Сергели/Учтепа/Яшнобод) 25 000 · 1–2; `tashkent-region` 35 000 · 2–3; `nearby-region` (10 областей — Ферганская долина + Самарканд-Бухара + юг) 50 000 · 2–4; `far-region` (Хорезм + Каракалпакстан) 80 000 · 3–6. **Free-shipping threshold** `FREE_SHIPPING_THRESHOLD_CENTS = 500_000 сум` — выше порога `cents=0 + isFree=true`, `baseCents` сохраняется для аналитики/Order. **Pickup** → `{cents: 0, zone: "pickup"}` независимо от адреса. **needs-info** ветка (`missing-region/missing-district/unknown-region`) — UI показывает подсказку «Укажите регион/район Ташкента» вместо цифры. Sanity `tashkentDistrictsCoverageOk()` гарантирует что central+outer покрывают все 11 `TASHKENT_DISTRICTS` (sanity-assert для сида/миграций). **StepAddress**: для `region=tashkent-city` district-поле переключается с free-text Input на `<Select>` из `TASHKENT_DISTRICTS` (slug → локализованное имя) — иначе калькулятор не мог бы разрешить зону. Для остальных регионов — Input остаётся (махалля/микрорайон, на цену не влияет). **Client hook** `useDeliveryEstimate()` (`apps/web/src/checkout/use-delivery-estimate.ts`) — читает checkout-store (method/region/district) + cart-store (`selectSubtotalCents`), мемоизируется. **Пробросы**: `<OrderSummary>` — строка «Доставка» показывает цену/«Бесплатно»/подсказку + цвет (primary при isFree); total = `subtotal − discount + deliveryCents`. `<StepDelivery>` — preview-карточка `<EstimatePreview data-testid="delivery-estimate-preview">` с label + value + ETA-подсказкой (pickupReady для самовывоза). `<StepReview>` — в карточке «Способ доставки» добавлена строка «Стоимость доставки: N сум · 2–4 дн.». На лету (без submit'а) пишем method в стор из StepDelivery, чтобы `useDeliveryEstimate` пересчитал при переключении радио. **Unit** `delivery.test.ts` — **33 теста** (68 общих в @bigmax/shared-types): pickup, 5 central × 6 outer × 10 nearby × 2 far × 11 районов в `it.each`, threshold на граничном tiyin, needs-info ветки, fallback на outer для неизвестного district в Ташкенте. **i18n** +9 ключей `checkout.delivery.cost.{label, free, freeHint, needsRegion, needsDistrict, etaRange, etaSame, pickupReady, estimateNotice}` × 3 локали (**368 total**). **E2e** `delivery-cost.spec.ts` — 5 сценариев × 2 projects = 10 тестов: Андижан → 50 000 в summary + preview с ETA; Ташкент+Мирабадский → 15 000; Ташкент без района → подсказка; pickup → «Бесплатно» + «Забрать можно в тот же день»; Review-карточка с ценой + ETA. Для корректности тестов seed'им `nuby-cherry-pacifier` (25 000 сум) — гарантирует subtotal ниже threshold. **Открытые вопросы (не блокирующие для P4-T3+)**: (a) тарифы-константы не настраиваются бизнесом без деплоя — P6 добавит админ-редактор или Prisma-модель `DeliveryRate`; (b) district slug сохраняется в `CheckoutAddress.district` в бывшее free-text поле — при записи в Prisma `Address.district` в P4-T5 запишем как slug для Ташкента, свободный текст для прочих регионов (пометка в БД не требуется). **CI**: typecheck/lint/format ✅. **Регресс** 188/188 (+10 от P4-T2).
- [x] **P4-T3** — Выбор самовывоза из филиалов Бигмах с картой. Зависимости: `leaflet@^1.9.4` + `react-leaflet@^4.2.1` (+ `@types/leaflet`); зафиксирован v4, потому что v5 требует React 19 — у нас React 18.3.1. **Seed** расширен до 3 филиалов Ташкента (Центр/Юнусабад/Чиланзар) с детерминированными id, phone, workingHours, координатами. Server-query (`apps/web/src/app/[locale]/checkout/page.tsx`) добирает `phone/workingHours/latitude/longitude`; интерфейс `Branch` в `<CheckoutPageClient>` расширен до `string | null` по всем new полям. **`<BranchPicker>`** (`apps/web/src/components/checkout/branch-picker.tsx`): 2-колоночная raster-grid (mobile: stack) — слева `<ul role="radiogroup" aria-label="Выберите филиал для самовывоза">` с `<button role="radio" aria-checked>` карточек (имя, адрес, `<a href=tel:>` телефон, часы работы), справа Leaflet-карта. **Карта** (`branch-map.tsx`, dynamic-imported с `{ssr: false}` + animated skeleton на 320px для layout-stability): OSM TileLayer, `divIcon` SVG-пины (чтобы не тянуть `marker-icon.png` ассеты Leaflet, которые ломаются с webpack/Next без конфига); selected pin 36px/primary, остальные 28px/slate. Клик по карточке или пину синхронизируется в обе стороны. `useMap().flyTo` плавно центрирует карту на выбранном филиале (duration 0.6с). В `<StepDelivery>` replaced `<Select>` на `<BranchPicker>`. В `<StepReview>`: карточка «Способ доставки» показывает адрес + `phone · workingHours` через `[...].filter(Boolean).join(" · ")`. **i18n**: +2 ключа `checkout.delivery.branch.{selectHint, noBranches}` × 3 локали (**370 total**). Намеренно `branch.*`, а не `pickup.*` — `checkout.delivery.pickup` был string («Самовывоз из филиала»), нельзя превращать в object (next-intl падает с «missing message» если ключ — nested). **E2e** `branch-pickup.spec.ts` — 3 сценария × 2 projects = 6 тестов: 3 карточки в radiogroup + Leaflet-контейнер + OSM-тайлы загрузились; клик по Чиланзарской карточке → `aria-checked=true` → Review показывает «Ташкент — Чиланзар», адрес (Бунёдкор), телефон; submit без выбора филиала блокируется zod-refine `method='pickup' → branchId required`. `role=radiogroup` с `name` фильтрует 3 карточки от 2 method-radio'ов в том же шаге. **CI**: typecheck/lint/format ✅. **Регресс** 194/194 (+6 от P4-T3). **Открытые вопросы**: (a) OSM-тайлы без API-ключа — для MVP ок, но в prod pod'е с высоким трафиком нужно или кэшировать через Next rewrites/Caddy, или переключиться на Yandex Maps (лучшее покрытие Узбекистана, требует `NEXT_PUBLIC_YANDEX_MAPS_KEY`) — апгрейд в P8; (b) мобильный UX 375/414px — проверен только mobile-chrome (Pixel 5 = 393px); iPhone SE viewport не в CI. Перенести в P8-T4 (a11y pass).
- [x] **P4-T4** — Пакет `@bigmax/payments/uniteller`: server-only чистый домен, подготовка фундамента под P4-T5 (`POST /api/checkout/pay`). **Структура** (`packages/payments/src/`): `index.ts` re-экспортирует `uniteller` namespace; `uniteller/index.ts` — public API; `uniteller/constants.ts`, `uniteller/signature.ts`, `uniteller/types.ts`, `uniteller/mock-server.ts` (оставлен с P0-T2, в public API не экспортируется). **`constants.ts`**: `UNITELLER` — URLs `payUrl/resultsUrl/cancelUrl/wsdlUrl/dashboardUrl` (§5.2); `UNITELLER_STATUSES` (Authorized, Paid, Canceled, NotAuthorized, Waiting); `INTERNAL_PAYMENT_STATUSES` (pending/captured/failed/cancelled/refunded/partially_refunded, зеркалят Prisma enum); `mapUnitellerStatus(s)` — strict switch Uniteller→внутренний (§5.8: Authorized/Paid → captured, Canceled/NotAuthorized → failed, Waiting → pending); `centsToUnitellerSubtotal(cents)` — `1_500_000 → "15000.00"` без локалей с бросками `TypeError` на non-integer и `RangeError` на негатив (§6.9). **`signature.ts`**: `buildSignature({shopId, orderId, subtotal, meanType?, eMoneyType?, password})` — ровно алгоритм §5.5: `md5(md5(shop)+&+md5(order)+&+md5(subtotal)+&+md5(mean||"")+&+md5(emoney||"")+&+md5(password)).toUpperCase()`. Использует `createHash("md5")` из `node:crypto` — modul server-only (в package.json нет `"sideEffects": false` namespace-import'а клиентом; Next.js отклонит `node:crypto` в client chunk автоматически). `password` никогда не логируется, не возвращается в ошибки. Throws `TypeError` на пустые обязательные поля. **`types.ts`** — Zod-схемы (shapes, не клиенты): `UnitellerStatusSchema`, `InternalPaymentStatusSchema`, `PaymentFormParamsSchema` (11 обязательных: `Shop_IDP/Order_IDP/Subtotal_P/Signature/URL_RETURN_OK/NO/RETURN/Email/Phone/Language/Lifetime` + 4 optional `Customer_IDP/Comment/MeanType/EMoneyType` — с regex-валидациями по master-prompt: `Order_IDP=/^BGX-\d{8}-\d{4}$/`, `Subtotal_P=/^\d+\.\d{2}$/`, `Signature=/^[A-F0-9]{32}$/`, `Phone=/^\+998\d{9}$/`), `UnitellerCallbackPayloadSchema` (минимум Order_ID/Status/Signature + optional Billnumber/Response_Code/MeanType/CardNumber), `UnitellerResultsItemSchema`. Все Uniteller-поля в PascalCase как в HTTP-форме. **Unit-тесты** (`signature.test.ts` + `constants.test.ts`, **28 тестов**): формат вывода (32 hex upper), детерминизм, совпадение с независимой reference-реализацией на `node:crypto` внутри теста (не «сам себя» проверяем), чувствительность к каждому полю (shopId/orderId/subtotal/password/meanType → разные подписи), throws на пустых обязательных полях, стабильный vector-anchor для регрессии, URL/HTTPS, status mapping, `centsToUnitellerSubtotal` граничные случаи (0, 1, 99, 100, billions, non-integer, negative). **Drive-by fix**: `packages/db/prisma/seed.ts` использовал `{attributes: null as Prisma.InputJsonValue | null}` — Prisma v5 отвергает литеральный `null` для `Json?` полей в strict TS; заменил на `Prisma.DbNull` и поменял `import type { Prisma }` → value import. Без фикса `pnpm -r run typecheck` валился на db (в CI прошло бы как красное). **CI**: typecheck ✅ (все 10 workspace-проектов), lint ✅, format ✅, i18n parity 370×3 ✅, unit **219/219** (68 shared-types + 28 payments + 3 notifications + 120 web), e2e **194/194**. **Открытые вопросы**: (a) `verifyCallbackSignature` из §5.13 перенесён в P4-T6 (webhook handler) — master-prompt §8 явно даёт P4-T4 только `buildSignature`, формула callback-подписи в документации Uniteller отличается и требует отдельного теста; (b) canonical vector из «Интернет-эквайринг v1.43+» пока не в тестах — использован синтетический `{POINT0001, BGX-20260424-0001, 15000.00, secret}` с reference-реализацией на `node:crypto`, после получения `UNITELLER_SHOP_ID`/password можно добавить snapshot реальной тестовой формы.
- [x] **P4-T5** — Серверный `POST /api/checkout/pay`: транзакционное создание Order + Payment(status=pending) + self-submitting HTML-форма на `UNITELLER.payUrl`. **Shared-types контракт** `packages/shared-types/src/checkout-pay.ts`: `CheckoutPayRequestSchema` (composite: `Contacts/Address/Delivery/Payment` + `items[{variantId,quantity}]` + `locale` + optional `promoCode`); два refine'а — courier требует address, pickup требует branchId. `CheckoutPayErrorSchema` — discriminated union из 9 `reason`-кодов. Живёт в shared-types (§6 правило 2, API-контракты не в apps/web). **Server-helpers** (`apps/web/src/server/`): `orders.ts` — `nextOrderSequenceForDay(utcDate)` читает `MAX(Order.number LIKE 'BGX-YYYYMMDD-%')` и возвращает `+1`; `renderUnitellerRedirectHtml(action, fields)` собирает `<!doctype html>` c self-submit формой + `<noscript>` fallback кнопкой + `escapeHtmlAttr` для защиты от XSS в Order*IDP/etc; `promo.ts` — `validatePromoCode(code, subtotalCents)` с проверкой `isActive/startsAt/endsAt/usageLimit/usedCount/minOrderCents` + case-insensitive lookup; возвращает discriminated union `{ok:true,promo} | {ok:false,reason:6-вариантов}`. **Route** `apps/web/src/app/api/checkout/pay/route.ts` (POST-only): (1) `auth()` → 401; (2) JSON.parse + `CheckoutPayRequestSchema.safeParse` → 400 `invalid_body`; (3) `payment.method='cod'` → 501 `cod_not_implemented` (P4-T8); (4) env-check `UNITELLER_SHOP_ID` + `UNITELLER_PASSWORD` → 503 `payment_provider_misconfigured`; (5) re-fetch `productVariant.findMany({where:{isActive:true}})` — **anti-tamper**, цены всегда из БД; (6) server-side recompute subtotal → `estimateDelivery` (§T2 single source of truth) → `validatePromoCode` + `computeDiscountCents/computeDeliveryDiscountCents` → total; (7) `createOrderWithRetry` — до 5 попыток на случай гонки Order.number (ловит `P2002` c `target=['number']`, ре-генерирует sequence); в `prisma.$transaction`: `Order` + `OrderItem[]` со снапшотом `{sku,color,size,product:{slug,nameRu,nameUz,nameEn}}` + `Payment{provider:uniteller, status:pending, amountCents, unitellerOrderIdp:order.number}`; (8) `uniteller.centsToUnitellerSubtotal(total)` → `"15000.00"`, `uniteller.buildSignature({shopId,orderId,subtotal,password})`, сборка `PaymentFormParams` с `URL_RETURN*{OK,NO,RETURN}`через`absoluteUrl(/orders/{orderId}/{success|failure|return}, locale)`+`Phone.replace(/\s+/g,"")`(§5.4 требует`+998XXXXXXXXX`без пробелов); (9) возврат`text/html`(Cache-Control: no-store, X-Robots-Tag: noindex). **Клиент**`<StepReview>`: кнопка «Оформить заказ» теперь активна при `isAuthenticated + payment.method='uniteller' + items>0 + estimate.kind='priced'`; onClick → `fetch('/api/checkout/pay',{method:'POST',body:JSON.stringify(request)})`→ если`text/html`→`document.open/write/close`(новый DOM автосабмитит на Uniteller); если JSON-ошибка →`toast.error(errorMessage(err,t))`с i18n-строкой под каждый`reason`. Для `method='cod'`— info-Alert «Оплата при получении появится в P4-T8».`<Loader2 className="animate-spin">`на submitting. **i18n**: +12 ключей`checkout.review.{placingOrder, codComingTitle, codComingBody, errors.{generic,network,unauthorized,empty*cart,invalid_variant,invalid_delivery,invalid_promo,provider}}`× 3 локали (**381 total**). **env**:`.env.example`и`.env`обновлены dev-placeholder'ами`UNITELLER_SHOP_ID=BGX_TEST_SHOP`+`UNITELLER_PASSWORD=bigmax-dev-placeholder-change-in-prod`— route отдаёт 503 на пустые значения, с placeholder'ами работает сигнатура (валидирована на reference-реализации в P4-T4 тестах), но Uniteller в prod отклонит. **Unit-тесты**: 10 новых в`apps/web/src/server/orders.test.ts` (`escapeHtmlAttr`+`renderUnitellerRedirectHtml`— XSS-escape в name/value, & в action URL, number→string,`<noscript>`fallback) + 10 в`packages/shared-types/src/checkout-pay.test.ts`(Zod happy paths + 7 rejection rules: courier без address / pickup без branchId / invalid phone / unknown region / unknown locale / empty items / qty=0). **E2e**`checkout-pay.spec.ts`— 3 сценария × 2 projects = 6 тестов: guest POST → 401; auth+click → Playwright`page.route('**/wpay.uniteller.ru/pay/**', abort)`перехватывает POST до ухода браузера, парсит`postData` и проверяет все 11 обязательных полей (Shop_IDP=BGX_TEST_SHOP, Order_IDP=/^BGX-\d{8}-\d{4}$/, Subtotal_P=/^\d+\.\d{2}$/, Signature=/^[A-F0-9]{32}$/, URL_RETURN*{OK,NO,RETURN} c locale-префиксом, Email, Phone=+998XXXXXXXXX, Language, Lifetime=30); COD-payload → 501 `cod_not_implemented`. Helper `deleteTestUser` расширен — cleanup Orders/OrderItems/Payments до удаления юзера, иначе `orders_user_id_fkey` роняет tear-down. Обновлён stale `Review (залогинен)` test из P4-T1 — больше нет `wipTitle` Alert для Uniteller, кнопка теперь enabled. **CI**: typecheck ✅ (все 10 workspace-проектов), lint ✅, format ✅, i18n parity 381×3 ✅, unit **239/239** (68 shared-types + 28 payments + 10 new checkout-pay + 3 notifications + 130 web), e2e **200/200** (+6). **Открытые вопросы**: (a) страницы `/orders/:id/{success,failure,return}` отдают 404 до P4-T7 — Uniteller с placeholder-credentials всё равно отвергнет реальный POST, реальный end-to-end flow валидируется в P4-T12 через mock-server; (b) `free_delivery`-промо + 100% скидка даёт `total=0` → 503 `invalid_delivery total_zero` — полностью бесплатные заказы требуют COD-ветки (P4-T8); (c) test-векторы Signature из документации Uniteller всё ещё синтетические — разблокируется после получения credentials от менеджера Uniteller.
- [x] **P4-T6** — Webhook `POST /api/webhooks/uniteller` (§5.7 master-prompt). **Callback signature** (`@bigmax/payments/uniteller/signature.ts`): `computeCallbackSignature(orderId, status, password)` + `verifyCallbackSignature({orderId, status, signature, password})` по формуле «Интернет-эквайринг» v1.43+: `MD5(MD5(Order_ID) + MD5(Status) + MD5(Password)).toUpperCase()`. Сравнение через `crypto.timingSafeEqual` (защита от timing-атак); `toUpperCase` нормализация; пустые поля → `false` без throw (webhook должен быть устойчив к мусорному вводу). **Rate-limit** (`apps/web/src/server/rate-limit.ts`): `enforceRateLimit({key, limit, windowSec})` на Redis `INCR + TTL` pipeline; **fail-open** при недоступности Redis (лучше пропустить пару callback'ов, чем потерять платёж — потеря дороже false-allow). `clientIpFromHeaders()` с приоритетом `x-forwarded-for[0]` → `x-real-ip` → `unknown`. **Decision-функция** (`apps/web/src/server/uniteller-callback.ts`): `parseCallbackBody(text)` парсит `application/x-www-form-urlencoded` через `URLSearchParams` (декодирует `%xx` и `+`); `evaluateCallback({raw, password})` — pure, возвращает discriminated union `invalid_payload | invalid_signature | valid` (Zod + `verifyCallbackSignature`). **Route** `apps/web/src/app/api/webhooks/uniteller/route.ts`: (1) rate-limit 100 req/60sec по IP → 429 с `Retry-After`; (2) парсит body, env-check `UNITELLER_PASSWORD` → 500; (3) `evaluateCallback` → 400 на invalid payload, 401 + `PaymentLog{action:"webhook_invalid_signature"}` на bad signature; (4) **идемпотентность** через unique-key `WebhookEvent(provider, externalId, signature)` — если `processed=true` уже, возвращаем 200 без побочек; иначе `findUnique → create` (либо переиспользуем существующий нетронутый event); (5) поиск `Payment.findUnique({unitellerOrderIdp})` — если нет, помечаем event `processed=true` + `PaymentLog{action:"webhook_foreign_tx"}` + 200 (§5.7 «чужая транзакция»); (6) `prisma.$transaction([Payment.update + (если captured) Order.update + WebhookEvent.update + PaymentLog.create])` — всё атомарно; condition'ные `Billnumber/Response_Code/CardNumber` пишутся только если присланы; `capturedAt = new Date()` ставится только при первом переходе в `captured`; (7) необработанный exception → 500 + `PaymentLog{action:"webhook_internal_error"}`, Uniteller сам сделает retry. **NotificationJob enqueue (P4-T10)** оставлен TODO-комментарием — вне скоупа T6. **Маскирование PAN (P4-T11)**: Uniteller уже присылает маскированный `4000 **** **** 2487`, в `PaymentLog` ничего дополнительно не делаем; полная стратегия маскирования — в P4-T11. **Unit-тесты** **+34**: 11 в `signature.test.ts` (`computeCallbackSignature` с reference `node:crypto` chain, разные статусы → разные подписи, 32-hex format) + `verifyCallbackSignature` (uppercase/lowercase normalization, 6 negative scenarios — wrong orderId/status/password, signature длиной ≠32, пустые поля без throw); 15 в `uniteller-callback.test.ts` (`parseCallbackBody` URL-decode + spaces, `evaluateCallback` всех веток + 5 Uniteller-статусов через `it.each`); 8 в `rate-limit.test.ts` (XFF parsing с whitespace + multi-IP, fail-open без Redis, `limit=0` degenerate). **E2e** `uniteller-webhook.spec.ts` — 6 сценариев × 2 projects = **12 тестов**: Authorized → Payment.captured + Order.confirmed + Payment.capturedAt set + WebhookEvent.processed=true; Waiting → Payment остаётся pending; невалидная подпись → 401 + Payment не тронут + WebhookEvent не создан; дубль callback'а с тем же Signature → unique-constraint держит 1 row, PaymentLog тоже 1; чужой Order_ID → 200 + WebhookEvent.processed=true (Uniteller не ретраит); без Order_ID → 400 (Zod). Прямой seed Order+Payment в Prisma (без UI) — webhook public, auth не нужен. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 381×3 ✅, unit **273/273** (78 shared-types + 39 payments + 3 notifications + 153 web), e2e **212/212** (+12). **Открытые вопросы**: (a) формула callback-подписи (§5.7) использует `MD5(MD5(Order_ID)+MD5(Status)+MD5(Password)).upper()` — типовая для Uniteller v1.43, но требует подтверждения у менеджера Uniteller перед prod — иначе callback'ы будут отвергаться. Если в реальной документации формула другая (например, c `Billnumber` в hash), правится в одном месте — `signature.ts:computeCallbackSignature`. (b) Rate-limit fail-open ослабляет защиту — для prod добавить Sentry-алерт «Redis недоступен дольше N секунд» (P8-T3). (c) `NotificationJob` (P4-T10) пока не триггерится — после T10 webhook будет публиковать в BullMQ-очередь.
- [x] **P4-T7** — Страницы `URL_RETURN_OK / URL_RETURN_NO / URL_RETURN` (§5.6 master-prompt) — три SSR-страницы под `/[locale]/orders/[orderId]/{success|failure|return}` + polling-эндпоинт. **API** `GET /api/orders/[id]/status` (`apps/web/src/app/api/orders/[id]/status/route.ts`): auth-gated через `auth()`, owner-only — на чужой Order отвечает **404** (не 403, чтобы не раскрывать существование чужих ID); подтягивает последний `Payment` через `payments: { take: 1, orderBy: { createdAt: 'desc' } }` и отдаёт `OrderStatusResponse {id, number, status, paymentStatus, paymentProvider, totalCents, currency, locale}` с `Cache-Control: no-store, private`. Контракт `OrderStatusResponseSchema` в `packages/shared-types/src/checkout-pay.ts`. **SSR-страницы** — общая логика в `_lib.ts::loadOrderReturnPage(params)`: проверка `isLocale`, `auth()` → redirect на `/auth/login?returnTo=/{locale}/orders` для гостя, owner-mismatch → `notFound()`; подгружает initial snapshot из БД до того как client-island начнёт polling (избегаем «мигания» данных). У всех трёх pages `metadata: {robots: {index:false, follow:false}}` — дублируем robots.ts на случай если он не сработал. **Client island** `<OrderStatusView variant initial>` (`apps/web/src/components/orders/order-status-view.tsx`, `"use client"`): три варианта (`success | failure | return`) ветвятся на разные Alert'ы; `success` стартует polling каждые **3000ms** через `setInterval` пока `paymentStatus` не станет terminal (`captured/failed/cancelled/refunded/partially_refunded`) или пока не истечёт **60_000ms** total timeout. `inFlightRef` блокирует параллельные fetch'и; на timeout — info-Alert с кнопкой `<RotateCw>` «Проверить статус» (manual retry). UI-состояния: pending → info-Alert «Подтверждаем оплату» с `<Loader2 className="animate-spin">`; captured → warning-Alert «Оплата прошла» с `<CheckCircle2>`; failed/cancelled → destructive-Alert «Платёж отклонён» с `<XCircle>`; return + non-terminal → нейтральный info-Alert. CTA: «Мои заказы» (`/account/orders` — заглушка до P5-T1), «Вернуться в корзину» / «Продолжить покупки» в зависимости от paid-state. `<StatusBadge>` — circular badge ✓/✕/…/? с цветом по тону. **i18n** — новый top-level namespace `orders.status.*` × 3 локали с под-namespace'ами `{success/failure/return}.title`, `polling.{title,body}`, `paid.{title,body}`, `failed.{title,body}`, `timeout.{title,body,retry}`, `actions.{myOrders,backToCart,continueShopping}` + `orderNumber` ICU-template (**398 keys × 3**, +17 от T7). **E2e** `order-return.spec.ts` × 2 projects = **14 тестов**: API guest → 401, API свой Order → 200 с проверкой всех полей payload'а (id/number/status/paymentStatus/paymentProvider/totalCents/currency), API чужой Order → 404 (создаём другого юзера через прямой `prisma.user.create` + cleanup); success-страница с pending → polling Alert + номер заказа; success с captured → terminal «Оплата прошла» + ссылка «Мои заказы»; failure-страница с failed → destructive Alert + «Вернуться в корзину»; гость на success → redirect на `/auth/login?returnTo=`. Seed-helper `seedOrder(paymentStatus)` создаёт Order+Payment напрямую через Prisma (без UI-flow для скорости). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 398×3 ✅, unit **273/273** (без изменений — pure SSR/UI), e2e **226/226** (+14). **Открытые вопросы**: (a) `/account/orders` пока 404 — будет в P5-T1; кнопка «Мои заказы» с success-страницы туда сейчас не ведёт никуда полезного, но ссылка корректная; (b) при долгом отсутствии webhook'а полный реальный flow проверится в P4-T12 (mock-server) — сейчас polling до 60с и timeout-Alert работают, но настоящий callback симулируется только напрямую через `POST /api/webhooks/uniteller`.
- [x] **P4-T8** — COD (наложенный платёж): `Payment(provider="cod", status="pending")` без Uniteller (§5.1, §8 master-prompt). **Refactor `/api/checkout/pay`**: единый flow до момента создания Order, далее ветвление по `request.payment.method`. Uniteller env-check (`UNITELLER_SHOP_ID/PASSWORD` → 503) выполняется **только** для `isUniteller`; COD не требует секретов. Блок `total === 0 → invalid_delivery` тоже **только** для Uniteller — Uniteller отвергает 0-суммы, COD позволяет полностью бесплатные заказы (промо `free_delivery` + 100% percent-скидка). Транзакция `Order + OrderItem[] + Payment` параметризована по `provider: "uniteller" | "cod"`; `unitellerOrderIdp` проставляется условным spread'ом — `null` для COD (контракт с webhook-эндпоинтом: foreign-tx-логика P4-T6 пропускает Payment без `unitellerOrderIdp`). **Response branching**: Uniteller → `text/html` self-submit форма (как в P4-T5); **COD → `application/json` с `CheckoutPayCodSuccess {ok:true, provider:"cod", orderId, orderNumber, redirectTo:"/{locale}/orders/{id}/success"}`**. Новый Zod-контракт `CheckoutPayCodSuccessSchema` в `packages/shared-types/src/checkout-pay.ts` — типобезопасное чтение на клиенте. **Client `<StepReview>`**: убран `isCod`-гейт из `canPlaceOrder` (кнопка теперь активна для COD); удалён info-Alert «codComingTitle/Body» (P4-T5 заглушка); `onPlaceOrder` ветвится по `Content-Type` ответа — `text/html` → `document.write` (Uniteller), `application/json` → `clearCart() + reset() + window.location.assign(redirectTo)` (cart-store очищается чтобы при возврате юзер не видел «старую» корзину). `useRouter` НЕ используется для COD-redirect — `redirectTo` уже включает префикс локали, нативный `location.assign` корректнее (плюс полная навигация даёт чистый SSR на success-странице). **Client `<OrderStatusView>`**: новая ветка `isCod = initial.paymentProvider === "cod"`. Polling **отключён** для COD: `Payment.status` остаётся `pending` до admin-flow (P6-T5 → confirmed → packing → shipped → delivered) и кэптурится в админке только при доставке (P6-T6) — нет смысла поллить. На success-странице COD показывает warning-Alert «Заказ принят, оплата при получении наличными или картой курьеру» вместо «Оплата прошла»; CTA — «Мои заказы» + «Продолжить покупки» (как для paid Uniteller). `isAccepted = isPaid || (isCod && variant === "success")` управляет тоном `<StatusBadge>` (✓) и вариантом второй CTA. **i18n**: убраны obsolete `checkout.review.{wipNotice, wipTitle, codComingTitle, codComingBody}` × 3 локали (4 ключа × 3 = 12 строк); добавлены `orders.status.cod.{title, body}` × 3 локали (2 ключа × 3 = 6). Итого **396 keys × 3** (было 398, -2 net). **Stale e2e cleanup**: в `checkout.spec.ts` (P4-T1 sub-task B) убран ассерт `expect(...).not.toBeVisible("Оформление временно недоступно")` — ключа больше нет, проверка бессмысленна; в `checkout-pay.spec.ts` (P4-T5) перепил «COD → 501» → «COD → 200 JSON» — теперь полный happy path с проверкой что `Order/Payment` создались с правильными атрибутами (provider=cod, status=pending, unitellerOrderIdp=null). **Новые e2e** `checkout-cod.spec.ts` × 2 projects = **6 тестов**: полный UI flow (auth → checkout → переключение «При получении» в payment → click «Оформить заказ» (enabled!) → редирект на success → проверка COD-Alert «Заказ принят» + отсутствия «Оплата прошла» + наличия «Мои заказы» + «Продолжить покупки»); cart+draft cleanup (`bigmax:cart` пуст, `bigmax:checkout-draft.currentStep=contacts` после COD); прямая проверка БД через Prisma (Order.status=pending, Payment.provider=cod, Payment.status=pending, Payment.unitellerOrderIdp=null, OrderItem создан). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 396×3 ✅, unit **273/273** (без изменений — все T8-логика покрыта e2e), e2e **232/232** (+6). **Открытые вопросы**: (a) admin-flow подтверждения COD-заказов и кнопка «Подтвердить получение» (Payment.status pending → captured при доставке) — **P6-T5/T6**; (b) polling success-страницы для COD сейчас всегда показывает «Заказ принят» независимо от `Payment.status` — после P6 (когда статус действительно может смениться) добавить ветку «Оплата получена» для `isCod && isPaid`; (c) `paid.title/body` строки остались для Uniteller-paid случая — не удалены, они применяются на Uniteller success после webhook'а.
- [x] **P4-T9** — `checkPendingPaymentsJob` (BullMQ cron, раз в минуту, §5.8 master-prompt) — pull-проверка зависших Uniteller-платежей через `GET /results/`. **HTTP-клиент** [`@bigmax/payments/uniteller/client.ts`](packages/payments/src/uniteller/client.ts): `fetchPaymentStatus({orderId, shopId, authLogin, authPassword, fetchImpl?})` — basic-auth `Authorization: Basic base64(login:password)`, GET к `UNITELLER.resultsUrl` с `Shop_IDP/Order_ID/Format=XML`. **Не throw'ит** — возвращает discriminated union `kind: ok | not_found | auth_error | network_error | parse_error` (job-orchestrator решает что делать). **XML-парсер** `parseResultsXml(xml)` — без внешних deps, regex по блокам `<order>...</order>` с подмножеством полей `orderid/status/total/billnumber/approvalcode/lastmodified` (Uniteller v1.43+ возвращает flat XML без атрибутов и вложенностей); валидирует `Status` через Zod-enum (`UnitellerStatusSchema`) — неизвестный статус → throw + `parse_error`; пустой `<orders></orders>` → пустой массив (валидно). **Worker package** [`apps/worker/`](apps/worker): заполнен стаб с P0-T2 — добавлены `bullmq@^5.21`, `ioredis@^5.4`, `tsx`-dev. `src/redis.ts` — singleton ioredis с `maxRetriesPerRequest: null` (требование BullMQ для блокирующих команд). `src/index.ts` — entry-point: создаёт `Queue("uniteller-pull")` + `Worker` с `concurrency: 1`, регистрирует repeatable job `every: 60_000ms`. Перед регистрацией удаляет старые repeatables с тем же именем (idempotent на restart). Graceful shutdown по SIGTERM/SIGINT. Если `UNITELLER_*` env пуст — job логирует warn и возвращает нулевой результат (dev-окружение часто без боевых credentials, не нужно spam-логирования). **Job logic** [`apps/worker/src/jobs/check-pending-payments.ts`](apps/worker/src/jobs/check-pending-payments.ts): разделена на (a) **pure decision** `decidePaymentAction({payment, snapshot, now, lifetimeMin?, graceMin?})` — возвращает `capture | fail | cancel_local | noop`. Логика по §5.8: `Authorized/Paid → capture` (с переносом `Billnumber/ApprovalCode`), `Canceled/NotAuthorized → fail`, `Waiting → noop` (проверим в следующий запуск); приоритет терминальным статусам — даже после lifetime'а если Uniteller прислал `Authorized`, capture'ится. Иначе при `now >= createdAt + (lifetimeMin + graceMin) * 60s` (default 30+5 = 35мин) → `cancel_local`. (b) **Orchestrator** `checkPendingPaymentsJob({prisma, fetchStatus, now, ...})` — чистый async с DI: находит `Payment.findMany({provider:"uniteller", status:"pending", createdAt: {lt: now - 2min}, unitellerOrderIdp: {not: null}})`; для каждого вызывает `fetchStatus`, применяет `decidePaymentAction`, делает атомарную мутацию через `$transaction`. На `capture` пишет `Payment.status='captured' + capturedAt + Order.status='confirmed' + PaymentLog{action:"pull_capture"}`. На `fail` — `Payment.status='failed' + PaymentLog{action:"pull_fail"}` (Order не трогаем — может прийти retry). На `cancel_local` — `Payment.status='cancelled' + Order.status='cancelled' + PaymentLog{action:"pull_cancel_local"}`. **Удалённый Uniteller-cancel НЕ дёргаем** — это refunds-flow (P6-T6 / §5.9). Возвращает `{scanned, captured, failed, cancelled, errors}` — для observability в logs/Sentry. Ошибки fetch'а логируются через `safeLog` (action=`pull_status_error`) + `errors++`; decision продолжает работать на основе lifetime (cancel_local даже без snapshot'а). **Unit-тесты** **+36** в двух файлах: 15 в [`client.test.ts`](packages/payments/src/uniteller/client.test.ts) (parseResultsXml × 6 — happy/multi/empty/missing-fields/unknown-status/bad-total; fetchPaymentStatus × 9 — happy/auth-header/url-params/401/403/404/500/throw/malformed-xml-as-empty); 21 в [`check-pending-payments.test.ts`](apps/worker/src/jobs/check-pending-payments.test.ts) (decision × 14 — все 5 Uniteller-статусов + lifetime границы 34:59/35:00/36:00 + custom lifetimeMin + null/auth_error/empty snapshots; orchestrator × 7 — без payments / Authorized / Canceled / Waiting / lifetime+Waiting → cancel / fetch throws → errors+lifetime / multiple payments независимо). Тесты на pure DI'шной логике без Redis/HTTP/реального Prisma — мокаются только `findMany/$transaction/update/create`. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 396×3 ✅, **unit 309/309** (54 payments + 21 worker + 78 shared-types + 3 notifications + 153 web), e2e **232/232** (без изменений — UI не тронут).
  - [x] **A (AbortController timeout)** — `fetchPaymentStatus({timeoutMs?})` (default 30000 ms / §5.12) теперь использует `AbortController`: создаём controller + `setTimeout(controller.abort, timeoutMs)`, прокидываем `signal` в `fetchImpl(url, {headers, signal})`. В `catch` различаем `AbortError` (по `err.name === "AbortError"` или `/aborted/i`-message) — возвращаем `network_error` с `message="timeout 30000ms"`. `clearTimeout` в `finally` чтобы успешные запросы не утекали таймером. Экспортирован `DEFAULT_FETCH_TIMEOUT_MS = 30_000`. **+3 unit-теста**: AbortController срабатывает на бесконечном fetch'е через 50 мс → корректный network_error/timeout message; успешный fetch до timeout проходит чисто; `signal: AbortSignal` пробрасывается во второй аргумент fetchImpl (структурный тест).
  - [x] **B (Sentry hook)** — `apps/worker/src/observability.ts::reportError(err, ctx)` — explicit hook-point для P4-T11/P8-T3, без новых deps. Без `SENTRY_DSN` пишет `[error] scope=... message=...` в `console.error`; с `SENTRY_DSN` — тег меняется на `[sentry-stub]` (видно что DSN сконфигурирован, но полная интеграция ещё не ставится — `@sentry/node` придёт с P4-T11). **Никогда не throw'ит** — `try/catch` вокруг `console.error`, observability не должна валить рабочий код. Подключено в orchestrator (`fetchStatus throw` + `apply throw`), `worker.on("failed")`-хендлер BullMQ, и top-level `main().catch`. **+5 unit-тестов**: тег `[error]` без DSN, `[sentry-stub]` с DSN, non-Error значения через `String()`, `extra` метаданные передаются вторым аргументом, не throw'ит даже если `console.error` сломан.
  - [x] **C (демонстрация worker'а)** — `package.json` (root): новые scripts `web:dev`, `worker:dev`, `worker:start`. README: новый раздел «Worker (BullMQ jobs)» с пошаговой инструкцией (docker:up → migrate/seed → `pnpm worker:dev`); упомянут fail-soft при пустых `UNITELLER_AUTH_*`; описан Sentry-stub как hook-point. Раздел «Скрипты» расширен `web:dev` / `worker:dev`. Docker-сервис worker'а — отложен до P4-T10 (там же добавится `apps/worker/Dockerfile` после интеграции notifications).
  - [x] **D (integration smoke на реальном Prisma)** — `apps/worker/src/jobs/check-pending-payments.integration.test.ts`. `describe.skipIf(!DATABASE_URL)` — если БД недоступна, тесты пропускаются (защищает CI без Postgres). `apps/worker` test-script переключён на `dotenv -e ../../.env -- vitest run` для подгрузки `DATABASE_URL` из корневого `.env`. Тест-юзер с timestamp-email, `beforeAll` создаёт, `afterAll` удаляет. `seedPending({ageMs, suffix})` — создаёт Order+Payment напрямую через Prisma, потом `$executeRaw UPDATE ... created_at = now() - ageMs` (Prisma `create` ставит created_at в текущий момент, для теста lifetime'а нужно искусственное прошлое). `afterEach` сносит все Order'ы юзера + Order'ы с тестовым префиксом `BGX-19990101-` — устойчиво к утечкам. **+3 теста**: Authorized → capture (проверяем атомарную мутацию Payment.status/Billnumber/ResponseCode/capturedAt + Order.status='confirmed' + PaymentLog{action:"pull_capture"}); Lifetime+grace 40 мин с Waiting-snapshot → cancel_local (Payment+Order='cancelled', PaymentLog с errorMessage='lifetime_expired'); свежий pending (30 сек) фильтруется minAgeMin=2мин и не трогается.
  - [x] **E (`@sentry/node` integration)** — replaced sub-task B's stub with real Sentry SDK (раннее завершение части P4-T11). `apps/worker/package.json` +`@sentry/node@^8.36`. `observability.ts::reportError` теперь делает **lazy `Sentry.init`** на первом вызове при наличии `SENTRY_DSN` (`tracesSampleRate: 0` — performance-tracing включится в P8-T3, `serverName: "bigmax-worker"`, `environment` из `NODE_ENV`); внутри `Sentry.withScope` — `setTag("scope", ctx.scope)` + `setExtra(k, v)` для каждого ключа `extra`; затем `captureException(err)` для Error-инстансов или `captureMessage(message, "error")` для не-Error значений. `console.error` параллельно дублирует с тегом `[sentry]` или `[error]` (без DSN или при init-fail). **Graceful fallback**: если `Sentry.init` бросает — переходим в console-only режим. **Безопасность (§5.12)**: doc-комментарий явно запрещает класть PAN/secrets в `extra` — caller'ы шлют только id'шники и summaries; PaymentLog с полными payload'ами и маскированием PAN — отдельный канал в P4-T11. Экспорт `_resetSentryForTests()` для контроля state в unit. **Тесты переписаны на vi.mock("@sentry/node")** — 10 сценариев: тег `[error]` без DSN; lazy init на первом вызове с DSN; init вызывается ровно один раз для двух последовательных reportError; Error → `captureException(err)`; non-Error → `captureMessage(string, "error")`; scope/extra ставятся через `withScope.setTag/setExtra`; init throws → graceful fallback на `[error]`; extra передаётся в console.error вторым аргументом; не throw'ит при сломанном console.error (+1 базовый flow без DSN/Sentry.init не вызывается).
  - [x] **F (`apps/worker/Dockerfile` + compose service)** — multi-stage `FROM node:22-alpine`: stage `deps` копирует `pnpm-workspace.yaml` + все workspace `package.json` файлы и ставит deps `--frozen-lockfile --ignore-scripts` (postinstall husky не нужен в контейнере); stage `runner` копирует deps из первого stage'а + sources, гонит `prisma generate` для `linux-musl-openssl-3.0.x` (alpine-binary отличается от хостовой macOS), запускает через `pnpm exec tsx src/index.ts`. `tsc`-build отложен до P8-T1 (нагрузочное профайлинг startup'а). **`docker-compose.yml`**: новый сервис `worker` под profile `["worker"]` — дефолтный `docker compose up` worker'а **не поднимает** (в dev обычно `pnpm worker:dev` локально с hot-reload через tsx watch); запускается через `docker compose --profile worker up`. `depends_on: postgres + redis service_healthy`, env переменные `DATABASE_URL=postgresql://bigmax:bigmax@postgres:5432/bigmax`, `REDIS_URL=redis://redis:6379` + Uniteller secrets/SENTRY_DSN из host-env (`${VAR:-}` fallback на пусто). `docker compose --profile worker config` валидируется без ошибок.
  - [x] **G (BullMQ live smoke)** — `apps/worker/src/jobs/check-pending-payments.bullmq.test.ts`. `describe.skipIf(!REDIS_URL)` — без Redis тесты пропускаются. Уникальное имя очереди `bigmax-test-${Date.now()}-${random}` чтобы не конфликтовать с production-`uniteller-pull` или параллельными прогонами. Отдельные ioredis-connection'ы для Queue и Worker (BullMQ требует разные connection'ы — один не может одновременно слать команды и блокирующе слушать). **+2 теста**: (1) Worker обрабатывает one-shot job — `queue.add` + `await Promise.race(processedPromise, timeout 10s)`, проверяет что `job.name` и `job.data` дошли до handler'а (Promise+resolve декомпозированы вне executor'а чтобы избежать TDZ); cleanup в `try/finally` закрывает Worker. (2) Repeatable job регистрируется с правильным `every: 60_000` — проверяет `getRepeatableJobs()` контракт нашего production-кода `apps/worker/src/index.ts`. `Number(ours.every)` cast — BullMQ возвращает `every` как string в свежих версиях. `afterAll` закрывает Queue + оба ioredis-connection'а.

  **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 396×3 ✅, **unit 326/326** (+17 от sub-tasks A+B+C+D+E+F+G: +3 timeout +5 stub-observability + позже расширено до 10 sentry-observability +3 integration +2 bullmq), e2e **232/232** (без изменений). **Hand-over в P4-T11/T12**: PaymentLog с маскированием PAN (P4-T11), end-to-end mock-server flow с реальным worker'ом (P4-T12). Sentry init и Dockerfile теперь production-ready, но требуют реальный `SENTRY_DSN` + Uniteller credentials для prod-deploy.

- [x] **P4-T10** — Уведомления Telegram / SMS / email (§4.8, §8 master-prompt). **`@bigmax/notifications` расширен** с одного канала (Eskiz SMS/OTP, P1-T1) до трёх: добавлены `telegram/` и `resend/` под тем же mock-pattern'ом — production-клиент, mock на `console.info`, фабрика `getXxxClient()` выбирает по env (`TELEGRAM_BOT_TOKEN` / `RESEND_API_KEY`). **Telegram** → fetch на `https://api.telegram.org/bot{token}/sendMessage` с AbortController-таймаутом 10 сек, поддержка `parseMode` (Markdown/HTML/MarkdownV2) и `disableNotification`. **Resend** → POST `https://api.resend.com/emails` с Bearer-auth + 15-сек таймаутом; поля `to/subject/html/text` (plain-text fallback опционален). **Шаблоны** [`templates/order/created.ts`](packages/notifications/src/templates/order/created.ts): `buildOrderCreatedSms/Telegram/Email(locale, payload)` через `next-intl/createTranslator` + словари `@bigmax/i18n` (новый namespace `notifications.order.created.{sms,telegramTitle,telegramBody,emailSubject,emailBody}` × 3 локали = **15 ключей**, итого **401 keys × 3**). Email-builder возвращает `{subject, text, html}` — minimal inline-styled HTML (без external CSS, дружелюбно к Gmail/Outlook); все payload-значения экранируются в HTML через `escapeHtml(s)` (защита от XSS в `orderNumber`/`url`). `formatCurrencyUzs(locale)` форматирует сумму per-locale (NBSP в RU, comma в EN). **Диспетчер** [`service.ts::sendOrderCreated`](packages/notifications/src/service.ts) — pure async с DI на `{prisma, eskiz, telegram, resend}`. Каждый канал обрабатывается **независимо** (try/catch вокруг каждого), на каждый — отдельная `Notification`-row (`type, channel, locale, payload, sentAt`). Если у получателя нет phone/email/chatId — канал пропускается (`{ok:false, error:"no_destination"}`), `Notification.payload.skipped=true` пишется. **BullMQ-очередь** `notifications` ([`queue.ts`](packages/notifications/src/queue.ts)) — отдельная от `uniteller-pull`. `enqueueOrderCreatedNotification(input)` — fire-and-forget вызов из web-роутов, lazy ioredis-connection. Опции job: `attempts: 3`, exponential backoff 1с/4с/16с (сетевые сбои SMS/Telegram/Resend часто требуют retry), `removeOnComplete/Fail: 1000`. **Worker** [`apps/worker/src/jobs/notification.ts`](apps/worker/src/jobs/notification.ts): `notificationJob(clients, payload)` валидирует `type === "order_created"` (status_changed/delivered → noop, P6-T5/T6) и делегирует в `sendOrderCreated`. Partial failure → `reportError` в Sentry с `{type, userId, results}` + продолжение. **Worker entry** [`apps/worker/src/index.ts`](apps/worker/src/index.ts) — две Queue+Worker: `uniteller-pull` (concurrency 1, serialized) + `notifications` (concurrency 5, параллельно SMS/Telegram/Email). Shared `failed`-listener + graceful shutdown через `Promise.all([pullWorker.close(), notificationsWorker.close()])`. **Triggers**: (1) [webhook Uniteller](apps/web/src/app/api/webhooks/uniteller/route.ts) — на pending → captured переходе (`isFirstCapture`) делает `enqueueOrderCreatedNotification` с `User.language` приоритетом над `Order.locale`; payment lookup расширен `select: {order: {user: {language, phone, email}, ...}}`. (2) [COD-route](apps/web/src/app/api/checkout/pay/route.ts) — после `Order.create` в COD-ветке делает аналогичный enqueue. Оба триггера в `try/catch` — enqueue-сбой не валит ответ юзеру / Uniteller (best-effort). **Telegram-канал пока не enqueue'тся** (channels: `["sms", "email"]` only) — линковка `User.telegram_chat_id` после `/start @bigmax_shop_bot` будет в P6 admin-flow; шаблон готов и тестируется на mock'е. **Unit-тесты** **+22** в трёх файлах: 11 в [`templates/order/created.test.ts`](packages/notifications/src/templates/order/created.test.ts) (× 3 локали SMS, Telegram-структура, email subject/text/html, XSS escape в orderNumber, `&` в url query, `lang` HTML-атрибут, fallback на ru, en-comma vs ru-NBSP); 8 в [`service.test.ts`](packages/notifications/src/service.test.ts) (3-канальный happy path, корректные phone/chatId/email прокидываются, Notification row с sentAt, no_destination skip × 3 канала, изоляция при partial failure); 3 в [`apps/worker/src/jobs/notification.test.ts`](apps/worker/src/jobs/notification.test.ts) (order_created happy, неизвестный type → no-op, partial failure → ok/failed counts). **E2e** **+2** в [`notifications.spec.ts`](apps/web/e2e/notifications.spec.ts) × 2 projects (skipIf без `REDIS_URL`): COD-чекаут → BullMQ job `order_created` появляется в очереди `notifications` (тест polls `getJobs(["waiting","delayed","active"])` до 2 сек, ищет по `payload.orderNumber`), name + payload поля проверены. Полный chain (enqueue → worker → Notification row → real SMS/Telegram/Email) — в P4-T12 с поднятым worker и mock-server'ом Uniteller. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 401×3 ✅, **unit 348/348** (+22; 22 notifications + 38 worker + 57 payments + 78 shared-types + 153 web), e2e **234/234** (+2). **Открытые вопросы**: (a) Telegram-канал автоматически уведомляет — линковка `@bigmax_shop_bot` через `/start` — flow в P6 admin (требует кнопку «Подключить Telegram» в `/account/profile` + endpoint обмена `chat_id` на `User.id`); (b) `order_status_changed` и `order_delivered` шаблоны — поднимутся в P6-T5/T6 когда admin будет менять Order.status; (c) full e2e c real SMS/Email отправкой через mock'и + worker'a живой — P4-T12.
- [x] **P4-T11** — PaymentLog с маскированием карт + Sentry алерты на провалы webhook'а и pull-job'а (§5.12 master-prompt). **Mask-helpers** в `@bigmax/payments/uniteller/mask` ([`mask.ts`](packages/payments/src/uniteller/mask.ts)): `maskCardNumber(s)` — regex `(?<![\d])(\d[\d\s-]{11,21}\d)(?![\d])` ловит любую 13–19-digit подпоследовательность (с опц. пробелами/дефисами), сохраняет separators и last4, остальные digit'ы → `*`; `4000 1234 5678 2487` → `**** **** **** 2487`. **Идемпотентность**: уже маскированный Uniteller-формат `4000 **** **** 2487` не трогается (regex отбрасывается, т.к. `compact.length` после удаления `*` = 4). Не задевает Order-номера `BGX-20260427-0001` (нет 13+ digit подряд) и телефоны `+998 90 123-45-67`. `scrubPaymentPayload(unknown)` рекурсивно обходит object/array, scrub'ит секретные ключи (regex-list: `^password$`, `^signature$`, `_secret$`, `_token$`, `^auth_?login/password$`, `^api_?key$`, `^bearer*` — case-insensitive) → `"[REDACTED]"`, остальные string-значения прогоняет через `maskCardNumber`. Не мутирует input. **Helper для apps/web** [`writePaymentLog`](apps/web/src/server/payment-log.ts): обёртка над `prisma.paymentLog.create` с автоматическим scrub request/response, best-effort try/catch (упавшая запись лога → `reportError` в Sentry, но не throw). **Sentry-observability** [`apps/web/src/server/observability.ts`](apps/web/src/server/observability.ts) — зеркало worker'ского `observability.ts` (P4-T9 sub-task E): lazy `Sentry.init({dsn, tracesSampleRate:0, serverName:"bigmax-web"})`, без DSN → `console.error` only. **Webhook Uniteller** [`route.ts`](apps/web/src/app/api/webhooks/uniteller/route.ts): inline `safeLog` удалён → 3 точки логирования переписаны на `writePaymentLog` (`webhook_invalid_signature` 401, `webhook_foreign_tx` 200, `webhook_internal_error` 500); `paymentLog.create` внутри транзакции теперь оборачивает `raw` через `scrubPaymentPayload`; Sentry-alert на 401 (signature mismatch — потенциальная атака) и 500 (БД/приложение упало — Uniteller сделает retry, но мы хотим видеть ошибку). **Pay-route** [`route.ts`](apps/web/src/app/api/checkout/pay/route.ts): `createOrderWithRetry` теперь возвращает `{id, number, paymentId}`; happy path добавляет audit-row `create_uniteller`/`create_cod` со scrub'нутым `request` и (для Uniteller) `response: {fields, payUrl}`; failure path (catch вокруг `createOrderWithRetry`) — `reportError` + `create_uniteller_failed`/`create_cod_failed` 500-row; sequence-exhaust (return null) тоже логируется. **Worker pull-job** [`check-pending-payments.ts`](apps/worker/src/jobs/check-pending-payments.ts): `safeLog` теперь scrub'ит payload через `scrubPaymentPayload` (на случай попадания CardNumber в error.context); существующие `reportError`-вызовы (P4-T9 sub-task E) на `pull_status_error` и `pull_apply_error` остаются — Sentry-alert на провалы pull'а уже работал. **Зависимости**: `@sentry/node@^8.36` добавлен в `apps/web` (worker уже имел). **Тесты**: **+24** unit в [`mask.test.ts`](packages/payments/src/uniteller/mask.test.ts) — 6 базовых форматов (13/15/16/19-digit, spaces/dashes/solid), 2 идемпотентность, 5 не-PAN кейсов (короткие/длинные/телефон/Order_ID/non-numeric), 2 PAN внутри текста, 3 секретные ключи (case-insensitive, full webhook payload), 2 PAN-в-CardNumber, 4 nested (object, array, null/undefined/numbers passthrough, immutability), 1 полный Uniteller webhook callback. **+4** в [`payment-log.test.ts`](apps/web/src/server/payment-log.test.ts) — scrub request, scrub response, best-effort на падении prisma, опциональный `paymentId`. **CI**: typecheck ✅ (10 проектов), lint ✅, **unit 376/376** (+28; 81 payments + 38 worker + 57 shared-types + 22 notifications + 157 web + 21 db/i18n), e2e **234/234** (без изменений — webhook/pay-route контракт не менялся, добавились только PaymentLog-rows). **Открытые вопросы**: (a) `@sentry/nextjs` vs `@sentry/node` для apps/web — оставлено `@sentry/node` для симметрии с worker'ом и минимума scope; миграция на `@sentry/nextjs` (с middleware-tracing и Edge runtime support) — после P8-T3, когда поднимем `tracesSampleRate > 0`; (b) `Notification`-row в `Order.audit` ещё не показывается админу — UI в P6.
- [x] **P4-T12** — Mock Uniteller + e2e платёжный flow (§5.11 master-prompt). **Stub из P0-T2** заменён на полнофункциональный mock-сервер: проверяет Signature на POST /pay/, держит in-memory state-машину `Map<Order_IDP, MockOrderState>`, шлёт автоматический callback на наш webhook с правильно подписанным `computeCallbackSignature(orderId, status, password)`, отдаёт XML на /results/. **Pure helpers** [`packages/payments/src/uniteller/mock-helpers.ts`](packages/payments/src/uniteller/mock-helpers.ts) выделены в отдельный файл (без http-side-effects) для unit-тестов: `verifyPayForm` (валидация Signature через `buildSignature`, case-insensitive, missing_field/signature_mismatch discriminated-union), `buildCallbackBody` (form-encoded body с `Response_Code: 00` для Authorized / `05` для NotAuthorized + auto Signature через тот же `computeCallbackSignature` что использует production-webhook — гарантия что mock не разъедется с реальной верификацией), `decidePayAction` (чистая функция `mode → {kind: "callback_then_redirect"|"no_callback_redirect", status, times, redirectTo}` для 4 test-режимов), state-API (`createState/upsertOrder/setOrderStatus/blockCallback/recordCallbackSent`), `renderResultsXml` (XML-escape через `&amp;/&lt;/&gt;/&quot;`, `lastmodified` в UTC ISO-формате). **Mock-сервер** [`mock-server.ts`](packages/payments/src/uniteller/mock-server.ts): pure-Node 22 без deps кроме built-in. **Test-modes** через `?test=success|fail|timeout|double` (default `success`): `success`/`fail` — 1 callback Authorized/NotAuthorized + 302 на URL_RETURN_OK/NO; `double` — callback Authorized × 2 (network-retry симуляция); `timeout` — НЕТ callback'а + 302 на URL_RETURN. **Admin endpoints** для детерминированных e2e: `GET /admin/state/:orderId` (snapshot для assertion'ов), `POST /admin/timeout/:orderId` (блокирует следующий callback), `DELETE /admin/state` (очистка между тестами). Sync-await callback'ов перед 302 — без race condition в e2e. **Dockerfile.mock** обновлён: копирует `mock-server.ts` + `mock-helpers.ts` + `signature.ts` (`zod`/`constants.ts`/`types.ts` не нужны — нет в графе). **docker-compose** новые env: `MOCK_PASSWORD: ${UNITELLER_PASSWORD}` (обязан совпадать иначе verify Signature 400'ит), `MOCK_WEBHOOK_BASE: ${MOCK_WEBHOOK_BASE:-http://host.docker.internal:3030}`. **`pnpm --filter @bigmax/payments mock:dev`** — локальный запуск через `tsx` (для разработки + e2e без docker). **Playwright webServer-array** ([`apps/web/playwright.config.ts`](apps/web/playwright.config.ts)) теперь поднимает оба процесса: dev-web на 3030 + mock на 8787 (env `E2E_MOCK_PORT/URL`), оба ждут `/health` 200. **E2e спек** [`apps/web/e2e/uniteller-flow.spec.ts`](apps/web/e2e/uniteller-flow.spec.ts) (4 теста × 2 projects = 8): через `page.route("https://wpay.uniteller.ru/pay/**", route.fulfill())` redirect'ит исходящий POST на `${MOCK_URL}/pay/?test=<mode>` (use `fulfill` not `continue` — playwright требует одинаковый протокол при continue, https→http запрещён). (1) **success** → mock callback `Authorized` → webhook update → `Payment.captured + Order.confirmed + capturedAt + unitellerBillnumber=MOCK-... + Response_Code=00`, redirect на `/success`, `WebhookEvent.processed=true`; (2) **fail** → callback `NotAuthorized` → `Payment.failed + Response_Code=05 + capturedAt=null`, Order остаётся `pending`, redirect на `/failure`; (3) **timeout** → mock не делает callback, Payment+Order остаются `pending`, redirect на `/return`, `mockState.callbacksSent=0`, `WebhookEvent` отсутствует; (4) **double** → mock делает callback дважды (`mockState.callbacksSent=2`), webhook'у вторая атака идёт по тому же `provider+externalId+signature` unique-key, `existing.processed=true` → 200 silently; финал `Payment.captured + Order.confirmed`, в БД ровно один `WebhookEvent`. **Cleanup** в `deleteTestUser` ([`e2e/helpers/user.ts`](apps/web/e2e/helpers/user.ts)) расширен: `PaymentLog` (FK на Payment, ловится FK-violation иначе) и `WebhookEvent` (`externalId IN (orderNumbers)` — связь string'ом не FK, иначе stale events ломают P4-T12 assertion'ы при reuse Order.number'ов в дни-границах). **Invalid signature scenario** покрыт unit'ом в [`uniteller-webhook.spec.ts`](apps/web/e2e/uniteller-webhook.spec.ts) P4-T6 (app сам всегда отправляет валидную подпись, e2e через mock неинформативен); **COD** — в [`checkout-cod.spec.ts`](apps/web/e2e/checkout-cod.spec.ts) P4-T8. **Unit-тесты** **+29** в [`mock-helpers.test.ts`](packages/payments/src/uniteller/mock-helpers.test.ts) (3 parseTestMode + 8 verifyPayForm + 3 buildCallbackBody + 4 decidePayAction + 7 state machine + 4 renderResultsXml). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, **unit 405/405** (+29; 110 payments + 38 worker + 78 shared-types + 22 notifications + 157 web), **e2e 242/242** (+8 uniteller-flow × 2 projects). **Открытые вопросы**: нет — Фаза 4 закрыта чисто. **Hand-over в Фазу 5**: фаза оплаты production-ready (Uniteller flow + COD + retry pull-job + notifications + audit-log с PAN-маскированием + Sentry-alerts + полный mock-driven e2e); следующее — UX истории заказов, отслеживание, отмена клиентом до «упаковки».

### Фаза 5 — История заказов и отслеживание ✅ завершена

- [x] **P5-T4** — Отмена заказа клиентом до «упаковки» с автоотменой Uniteller-платежа (P5-T4 master-prompt §8 / §5.9). **Eligibility** [`validateCancelEligibility`](apps/web/src/server/order-cancel.ts) — pure-функция: `Order.status` ∈ `{pending, confirmed}` → ОК, `packing/shipped/delivered` → `order_too_late`, `cancelled/refunded` → `order_already_cancelled`, неизвестный статус → `order_too_late` (paranoia). Если есть captured Uniteller Payment с pending Refund'ом — `refund_in_progress` (нельзя дублировать flow). Возвращает discriminated-union с `capturedPayment: {id, billnumber, orderIdp} | null` для caller'а: `null` → просто local-flip, не-null → сначала зов Uniteller `/cancel/`. `CancelRequestSchema` — `{reason?: string.trim().max(500), strict()}` (опциональный reason, посторонние поля → fail). **Uniteller client** — добавлен [`cancelUnitellerPayment`](packages/payments/src/uniteller/client.ts) в `@bigmax/payments`: POST `application/x-www-form-urlencoded` на `UNITELLER.cancelUrl` (`https://wpay.uniteller.ru/cancel/`) с Basic-auth (`UNITELLER_AUTH_LOGIN/PASSWORD`), body `{Shop_IDP, Order_ID, Billnumber?, Subtotal?}`. Парсинг ответа: `<Status>Canceled|Cancelled</Status>` → `kind: "ok"`, `<Status>Error</Status>` + `<Message>` → `provider_error`, 401/403 → `auth_error`, 404 → `not_found`, network exception → `network_error` с timeout-message при AbortError; 200 без явного Status тоже `ok` (Uniteller иногда возвращает legacy-формат `<Order>`). **Endpoint** `POST /api/account/orders/[id]/cancel` ([route](apps/web/src/app/api/account/orders/[id]/cancel/route.ts)): auth → owner-check через `getOrderDetail` (404 на чужой Order) → `CancelRequestSchema.safeParse` (400 на reason > 500) → `validateCancelEligibility` (409 с reason-кодом) → если `capturedPayment` есть, синхронный вызов `cancelUnitellerPayment` (на fail → 502 + PaymentLog audit + Sentry, локальный state НЕ трогаем — юзер сможет ретрайнуть; на missing `UNITELLER_AUTH_*` → 503 `provider_misconfigured`) → `prisma.$transaction`: `Order.status='cancelled'` + все pending/captured Payments → `cancelled` + `writePaymentLog(action="cancel_by_user")` для admin-аудита. Refund-row при cancel НЕ создаётся (это void, не refund). **UI** [`<CancelButton>` + `<CancelDialog>`](apps/web/src/components/account/order-detail-actions.tsx) добавлены в `<OrderDetailActions>` (P5-T2): cancel-кнопка с `<XCircle>` иконкой `variant="ghost"`, при клике — Radix Dialog с `<textarea>` (опц. reason до 500 chars), submit-кнопка `variant="destructive"`. На 200 → success-state в том же диалоге, `router.refresh()` откладывается до close (тот же anti-unmount-pattern что в refund-flow P5-T2). После refresh tracker переходит в `terminal=cancelled` (P5-T3) и cancel-кнопка пропадает. Для `order_already_cancelled` кнопка не рендерится вообще (`return <></>`); для `order_too_late`/`refund_in_progress` — disabled с inline-пояснением. Eligibility компьютится на SSR в [page.tsx](apps/web/src/app/[locale]/account/orders/[id]/page.tsx) и передаётся в actions client island. **i18n** — extension `account.orders.detail.actions.{cancel, cancelIneligible.{order_too_late, order_already_cancelled, refund_in_progress}}` + новый namespace `account.orders.detail.cancelDialog.{title,body,reasonLabel,reasonPlaceholder,errors.{8 keys},cancel,submit,submitting,successTitle,successBody}` × 3 локали — итого **499 keys × 3** (+21 от P5-T4). **Tests**: **+19 unit** в [`order-cancel.test.ts`](apps/web/src/server/order-cancel.test.ts) — happy paths × 5 (pending/confirmed COD, captured Uniteller, pending Uniteller, failed-Refund-не-блок), отказы × 8 (3× too_late, 2× already_cancelled, paranoia, refund_in_progress, refund-другого-payment-не-блок), CancelRequestSchema × 6 (без тела/short/500/501/strict/wrong-type); **+11 unit** в [`client.test.ts`](packages/payments/src/uniteller/client.test.ts) для `cancelUnitellerPayment` — happy 200 Canceled, accept Cancelled (BR), 200 без Status узла, 200 + Error + Message, 401/404/500, network exception, body-структура (Shop_IDP+Order_ID+Basic auth), Billnumber/Subtotal optional, timeout. **+11 e2e** в [`account-order-cancel.spec.ts`](apps/web/e2e/account-order-cancel.spec.ts) — guest 401, COD pending/confirmed 200 + БД flip, packing/shipped/already-cancelled 409 + reason-код, owner-mismatch 404, body 501-chars 400, UI happy (dialog → submit → success → tracker terminal + cancel-кнопка пропала), UI ineligible shipped (disabled + tooltip), UI cancelled (вообще не рендерится). Полный Uniteller-cancel chain через mock-server отложен в P8-T1 (нужен server-side URL-override для `/cancel/`). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 499×3 ✅, **unit 484/484** (+30; **121 payments** + 38 worker + 78 shared-types + 22 notifications + **225 web**), **e2e P5 целиком 33/33** (11 P5-T4 + 7 P5-T3 + 8 P5-T2 + 7 P5-T1). **Открытые вопросы**: нет — Фаза 5 закрыта чисто. **Hand-over в Фазу 6**: client-flow для заказов готов (list → detail → tracker → reorder/refund/cancel); admin-flow для одобрения refund'ов и смены `Order.status` приедет в P6-T5/T6.

- [x] **P5-T3** — Трекер статуса заказа (P5-T3 master-prompt §8). Визуальный horizontal stepper по `Order.status` на детальной странице, рендерится между header и Items. **Pure helper** [`computeOrderTrackerView`](apps/web/src/server/order-tracker.ts) маппит `Order.status` в один из двух view-режимов: `kind: "stepper"` с массивом из 4 шагов (`confirmed → packing → shipped → delivered`) с `state: upcoming|current|done` для каждого, либо `kind: "terminal"` с `terminal: cancelled|refunded` (прерывают воронку — рендерятся как самостоятельный alert-блок, без stepper'а). `pending` Order.status попадает в stepper с `pendingPreflight=true` и всеми шагами `upcoming`. Инварианты: ровно 4 шага в фиксированном порядке, максимум один `current`, `upcoming` идут только после `current`/`done` (никаких «дыр»). **Component** [`<OrderTracker>`](apps/web/src/components/account/order-tracker.tsx) — pure server component с tone-mapping (done = emerald primary, current = blue + `animate-pulse`, upcoming = muted), кружки с номером шага или `<Check>` иконкой, коннекторы между ними окрашены по правилу «коннектор слева от current = done, справа = upcoming». Step-label берётся из существующего `account.orders.statuses.{confirmed|packing|shipped|delivered}` (P5-T1). Terminal-блоки: cancelled — `border-rose` + `<AlertTriangle>` + body «вернём в течение нескольких дней», refunded — `border-slate` + `<RotateCcw>` + body «деньги уже на карте». **Data-attributes** для e2e-стабильности: `data-testid="order-tracker"`, `data-tracker-kind`, `data-tracker-pending`, `data-tracker-terminal`, `data-step={key}`, `data-step-state={state}` — переживут UX-tweaks верстки и timestamp'ов в P6. **i18n** — extension `account.orders.detail.tracker.{title, pendingCaption, lastUpdated, terminal.{cancelled,refunded}.{title,body}}` × 3 локали — итого **478 keys × 3** (+7 от P5-T3). **Tests**: **+10 unit** в [`order-tracker.test.ts`](apps/web/src/server/order-tracker.test.ts) — happy-path × 5 (pending все upcoming, confirmed/packing/shipped current-position, delivered все done без current), terminal × 2 (cancelled, refunded), инварианты × 3 (4 шага в порядке, максимум один current, нет дыр upcoming-после-done); **+7 e2e** в [`order-tracker.spec.ts`](apps/web/e2e/order-tracker.spec.ts) — каждый Order.status seedится напрямую через Prisma, page.goto → tracker `data-step-state` атрибуты ассёртятся для всех шагов, terminal-страницы проверяют `data-tracker-kind="terminal"` + текст и отсутствие `[data-step]`. **Updated** P5-T2 detail spec — assertion на `getByText("Доставлен", exact)` теперь scope'нут к header'у, потому что tracker дублирует label делового шага. **Что отложено в P6-T5**: `OrderStatusHistory` таблица с timestamps на каждом step'е (admin меняет статусы → создаём rows → tracker подцепит на done-шагах). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 478×3 ✅, **unit 454/454** (+10; 110 payments + 38 worker + 78 shared-types + 22 notifications + **206 web**), e2e regression P5 целиком локально **22/22** (7 P5-T3 + 8 P5-T2 + 7 P5-T1). **Открытые вопросы**: (a) timestamps на каждом done-шаге — нужна `OrderStatusHistory` таблица, добавится в P6-T5 когда admin будет менять `Order.status`; (b) tracker сейчас live-обновляется ТОЛЬКО при reload страницы — polling/SSE для real-time переходов pending→confirmed на каптуре webhook'а можно подключить через тот же `/api/orders/[id]/status` endpoint, что уже poll'ит P4-T7-страница, но это уже out-of-scope (юзер обычно открывает детальку «потом», не ждёт live).

- [x] **P5-T2** — Детали заказа, повтор заказа, запрос возврата (P5-T2 master-prompt §8). **Detail-страница** `/[locale]/account/orders/[id]` ([page.tsx](apps/web/src/app/[locale]/account/orders/[id]/page.tsx)) — SSR через `getOrderDetail(userId, orderId)` ([account-order-detail.ts](apps/web/src/server/account-order-detail.ts)): owner-check `findFirst({where:{id, userId}})` → `null` на чужой Order → `notFound()` (404, не 403, чтобы не подтверждать существование чужого ID). Подгружает `Order` + `OrderItem[]` (productSnapshot JSON) + `Address`/`StoreBranch` (по `deliveryMethod`) + `Payment[]` + `Refund[]` (отдельный запрос, чтобы избежать дублирования через `payments.refunds` join). Page рендерит 5 секций: header (номер заказа `font-mono`, `<OrderStatusBadge>`, дата через `Intl.DateTimeFormat`), Items (имя через `localized(snapshot.product, "name", locale)` с RU-fallback, цвет/размер, qty, line-total), Totals (subtotal/delivery/discount/total с `formatCurrencyUzs`), Delivery (courier+полный address или pickup+branch с локализованным name/address), Payment (provider + status label из P4-T7 i18n + `unitellerCardMask` + amount), Refunds (если есть — список с reason/amount/status). **Re-order endpoint** `POST /api/account/orders/[id]/reorder` ([route](apps/web/src/app/api/account/orders/[id]/reorder/route.ts)) + [helper](apps/web/src/server/order-reorder.ts) `loadOrderItemsForReorder`: загружает текущие `ProductVariant` по `variantId` из OrderItems с `product.isActive=true`-фильтром (если variant удалён или product деактивирован — пропускаем); возвращает `{addedItems: ReorderCartItem[], skippedCount, totalCount}` где shape совпадает с `CartItemInput` из cart store; имя продукта берётся ТЕКУЩЕЕ (не snapshot) — re-order рефрешит каталожные данные. Auth-gated, owner-check через тот же `findFirst({where:{id, userId}})`, на чужой Order — 404. **Re-order UI** в [`<OrderDetailActions>`](apps/web/src/components/account/order-detail-actions.tsx) — `"use client"`: кнопка с `<RotateCcw>` иконкой, при клике fetch + iterate `useCart.add(item, qty)` (zustand store сам мерджит quantities если variant уже в корзине), toast.success(plural count) + toast.warning если `skippedCount > 0`, `router.push("/cart")`. **Refund-request endpoint** `POST /api/account/orders/[id]/refund` ([route](apps/web/src/app/api/account/orders/[id]/refund/route.ts)): валидация тела через `RefundRequestSchema` (`{reason: string.trim().min(10).max(1000)}` — пустые пробелы → too_short); owner-check через `getOrderDetail`; eligibility через [`validateRefundEligibility`](apps/web/src/server/order-actions.ts) — pure-функция с правилами: `Order.status==="cancelled"` → блок (`order_cancelled`), `payments.find(captured)` обязателен (`no_captured_payment`), нет `Refund.status==="completed"` (`refund_already_completed`), нет `Refund.status==="pending"` (`refund_already_requested`); failed Refund НЕ блок — можно перезапросить (admin отклонил, юзер исправляет). На успех создаём `Refund(paymentId, amountCents=Payment.amount, reason, initiatedByUserId, status=pending)` + `writePaymentLog(action="refund_requested")` для admin-аудита (P6-T6); ответ 201 с снапшотом refund'а. Eligibility-fail → 409 Conflict с `reason`-кодом для i18n. **Refund-dialog** в `<OrderDetailActions>` — Radix Dialog с textarea (10..1000 chars, html5 `required`/`minLength`/`maxLength`), submit fetch'ит endpoint, на 200 переключается на success-state (новый title «Запрос отправлен» + body), router.refresh() **только** на close-dialog (иначе SSR-перерендер с уже-pending Refund мгновенно unmount'ит success-state до того как юзер его увидит); на error inline-message по `errorKey` (reason_too_short / reason_too_long / invalid_body / generic). При не-eligible — кнопка disabled с тултип-текстом причины из 4 локалей. Lucide иконки `<RefreshCw>`/`<Loader2>`. **List-card** в P5-T1 переключён на `/account/orders/{id}` (детальная вместо `/orders/{id}/success`). **i18n** — extension `account.orders.detail.{back, createdAt, items, itemQty, totals.{title,subtotal,delivery,discount,total}, delivery.{title,courier,pickup,address,branch,phone,comment}, payment.{title,uniteller,cod,card,billnumber}, refunds.{title,amount,createdAt,statuses.{pending,completed,failed}}, actions.{reorder,reorderInProgress,reorderSuccess,reorderSkipped,reorderFailed,refund,refundIneligible.{4 reasons}}, refundDialog.{title,body,reasonLabel,reasonPlaceholder,errors.{4 keys},cancel,submit,submitting,successTitle,successBody}}` × 3 локали — итого **471 keys × 3** (+50 от P5-T2). **Tests**: **+17 unit** в [`order-actions.test.ts`](apps/web/src/server/order-actions.test.ts) — happy paths × 3 (delivered/confirmed/shipped), отказы × 7 (cancelled, no captured, empty payments, only failed, pending refund, completed refund, completed-priority-over-pending), failed-refund НЕ блок, multi-payment isolation, RefundRequestSchema (5 негатив + 1 позитив + trim-empty); **+8 e2e** в [`account-order-detail.spec.ts`](apps/web/e2e/account-order-detail.spec.ts) — SSR contents (h1 номер, status, items, payment card mask, totals), чужой Order → 404, re-order API (addedItems/quantity/totalCount), re-order UI (клик → cart + redirect /cart), refund-disabled с pending-payment, refund happy path (dialog → reason → submit → success-state + Refund row + reason в БД), refund 400 на короткий reason, refund 409 на повторный запрос. **Updated** `e2e/account-orders.spec.ts` — клик-карточка теперь идёт на `/account/orders/{id}` (P5-T2 деталка). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 471×3 ✅, **unit 444/444** (+17; 110 payments + 38 worker + 78 shared-types + 22 notifications + **196 web**), e2e regression P5-T1+P5-T2 локально **15/15**. **Открытые вопросы**: (a) admin-вьюшка Refund'ов (одобрение/отказ + dispatch на Uniteller `/cancel/`) приедет в P6-T6 — пока юзер видит только pending-statuses; (b) tracker «трекер статуса» (P5-T3) ещё не визуализирован — статус показан badge'ем без timeline; (c) частичный возврат (refund < `Payment.amount`) запрашивается в админке P6-T6 — UI юзера сейчас просит только полный (амunt = Payment.amount).

- [x] **P5-T1** — «Мои заказы» со статусами и фильтрами (F11, §8 master-prompt). Замена P4-T7 stub'а на полноценный SSR-список под `/[locale]/account/orders` с querystring-driven `?status=<OrderStatus>&page=<N>`, размер страницы `ORDERS_PAGE_SIZE=10`. **Pure helper** [`apps/web/src/server/account-orders.ts`](apps/web/src/server/account-orders.ts) — `parseOrderListQuery` (sanitize: garbage status → null, page<=0 / NaN / float → 1) + `getUserOrders(userId, query)` (Prisma `findMany` с `_count.items` + `payments[take:1, orderBy createdAt desc]` для последнего payment'а провайдера, параллельный `count` через `Promise.all`, `orderBy createdAt desc`, `pageCount = max(1, ceil(total/10))`). Список енумов `ORDER_STATUSES` экспортирован для drop'а в Select. **SSR-страница** [`page.tsx`](apps/web/src/app/[locale]/account/orders/page.tsx) — auth-gated через layout (гость → redirect на /auth/login); рендерит header (h1 + ICU-summary `{count, plural, ...}` total), `<OrderListFilters>`, list или empty-state, опциональный `<Pagination>` если `pageCount > 1`. **Карточка** — `Link` через `@bigmax/i18n/navigation` (auto-locale prefix) на `/orders/{id}/success` (P4-T7 страница; полная деталь-страница приедет в P5-T2): номер заказа `font-mono`, `<OrderStatusBadge>` с цветом по тону, дата через `Intl.DateTimeFormat` с `dateStyle:medium` + locale-маппингом (`ru-RU`/`uz-UZ`/`en-US`), ICU plural `itemsCount`, опц. label провайдера (cod/uniteller), `formatCurrencyUzs(totalCents, locale)`. **Empty state** различает «совсем нет» (`title + body`) и «нет под фильтр» (`emptyFiltered`). **Pagination** — Prev/Next ссылочная (не SPA-router) с querystring-сохранением status, текст «Страница X из Y». **Components**: [`<OrderListFilters>`](apps/web/src/components/account/order-list-filters.tsx) — `"use client"` Select на native `<select>` с `useTransition` для smoother nav, `useRouter().push()` сбрасывает page при смене status (чтобы не оказаться на пустой странице фильтра); [`<OrderStatusBadge>`](apps/web/src/components/account/order-status-badge.tsx) — pure server component с tone-mapping `bg-{amber|blue|indigo|violet|emerald|rose|slate}-100`. **i18n** — расширены `account.orders.list.{summary, empty.{title,body}, emptyFiltered, filters.{label,all}, card.{itemsCount, viewDetails, paymentCod, paymentUniteller, createdAt}, pagination.{prev,next,page}}` + `account.orders.statuses.{pending|confirmed|packing|shipped|delivered|cancelled|refunded}` × 3 локали; убран obsolete `account.orders.stub`. **421 keys × 3** (+20 от P5-T1). **Tests**: **+22 unit** в [`account-orders.test.ts`](apps/web/src/server/account-orders.test.ts) — status enum-allowlist (× 7 валидных + garbage / case / array first-pick / undefined searchParams), page sanitize (1, 0, -5, NaN, float→2, 999999, default); **+7 e2e** в [`account-orders.spec.ts`](apps/web/e2e/account-orders.spec.ts) — гость → redirect, empty-state, 3 заказа DESC по createdAt + ICU plural summary, фильтр delivered, фильтр без совпадений, pagination (12 заказов → 10+2), клик на карточку → `/orders/{id}/success`. Helper `seedOrders(specs[])` создаёт Order+Payment напрямую через Prisma с custom `createdAt` для контроля порядка; уникальный `Order.number` — `BGX-YYYYMMDD-${(Date.now()|0)+counter}-T1`-suffix во избежание конфликтов с P4-T12 номерами. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 421×3 ✅, **unit 427/427** (+22; 110 payments + 38 worker + 78 shared-types + 22 notifications + **179 web**), e2e regression — будет накатан в общем CI-прогоне (локально 7/7 спека прошли). **Открытые вопросы**: (a) карточка заказа сейчас ведёт на `/orders/{id}/success` (P4-T7 страница со статусом payment'а) — полная деталь-страница `/account/orders/{id}` со списком позиций / адресом / трекером / кнопкой повтора заказа приедет в P5-T2; (b) фильтр на даты создания (`?from=...&to=...`) пока не сделан — добавим если будет UX-need (по умолчанию хватает status + DESC порядка); (c) индекс на `(userId, createdAt DESC)` пока не оптимизирован — текущий `@@index([userId])` + сортировка работают, но при росте до 100k+ заказов потребуется compound index.

### Фаза 6 — Админ-панель Bigmax (F13) ✅ закрыта

- [x] **P6-T8** — Управление клиентами (F13/§8 master-prompt). Полноценный admin-CRUD по таблице `User` для поиска, просмотра и базовых safety-операций (role + password reset). **Pure-helpers + Zod** [`apps/web/src/server/admin-customers.ts`](apps/web/src/server/admin-customers.ts): `parseAdminCustomerListQuery` (sanitize `?q=&role=&page=`), `RoleChangeSchema = z.object({role: enum, reason: string.min(3,"reason_too_short").max(500,"reason_too_long")}).strict()`, `PasswordResetSchema = z.object({reason}).strict()` — отдельный schema без password (admin не задаёт пароль, мы всегда **генерируем**). **`generateTempPassword(length=12)`** — pure-функция через `crypto.randomBytes`: безопасный alphabet `ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789` **без I/l/1/O/0** (визуально похожие символы — admin диктует по телефону без путаницы); 12 chars × 54-char alphabet → ~71 bits entropy, throws при `length < 4`. **`hashPassword(plain)`** — bcrypt cost-factor 10 (стандарт NextAuth). **Server-fetch**: `getAdminCustomers(query)` (Prisma findMany с `OR`-clause по email/phone/name ILIKE, page-size 20, parallel count, `_count.orders` для счётчика без extra query); `getAdminCustomer(id)` — full профиль + 10 recent orders + все addresses + 20 loyalty txs + saved cards + parallel `order.aggregate({_sum: totalCents, where: status in PAID})` для `totalSpentCents`. **API endpoints**: [`PATCH /api/admin/customers/[id]/role`](apps/web/src/app/api/admin/customers/%5Bid%5D/role/route.ts) — Zod → 404 `user_not_found` → **`cannot_self_demote`** (admin не может понизить себя — защита от accidental lockout, проверяется через `target.id === auth.userId && target.role === "admin" && newRole !== "admin"` → 409); → `role_unchanged` (409, idempotency); → **manager-only restriction**: `auth.role === "manager" && newRole === "admin"` → 403 `forbidden` (только admin может назначать admin'а); update + audit в `PaymentLog{action: "user.role_change", request: {targetUserId, oldRole, newRole, reason, adminId, adminEmail}}`. [`POST /api/admin/customers/[id]/reset-password`](apps/web/src/app/api/admin/customers/%5Bid%5D/reset-password/route.ts) — Zod reason → 404 user_not_found → **409 `user_has_no_password`** (OTP-only пользователь — нечего сбрасывать, admin должен использовать OTP-flow); generate temp pass + bcrypt-hash + update; PaymentLog audit БЕЗ plain-password (только `{ok: true}` в response field — критично: plain никогда не пишется в БД). Plain-password возвращается в HTTP response **один раз** для admin'а скопировать. **UI**: [list page](apps/web/src/app/[locale]/admin/customers/page.tsx) с filter-form [`<CustomersListFilters>`](apps/web/src/components/admin/customers/customers-list-filters.tsx) (q ILIKE по email/phone/name + role select), table 6 колонок (name + Link / contacts email+phone / role-badge / orders count / loyalty points / createdAt), tone-coded role-badges (rose=admin, violet=manager, slate=customer), pagination prev/next; [detail page](apps/web/src/app/[locale]/admin/customers/%5Bid%5D/page.tsx) с breadcrumbs, header (name + email/phone + large role-badge), 2-grid: профиль section (language/loyalty-balance/totalSpent/orders-count/createdAt/updatedAt) + actions section (RoleChanger + ResetPasswordDialog). Полные секции: **recent orders** (last 10 с Links на `/admin/orders/{id}` + OrderStatusBadge), **addresses** (с default-tag), **loyalty transactions** (last 20 с tone-coded points: emerald positive, rose negative), **saved cards** (panLast4 mask + brand + default/blocked badges, conditional). **`<RoleChanger>`** ([client](apps/web/src/components/admin/customers/role-changer.tsx)) — **defensive UX**: `isSelf && currentRole === "admin"` → form-content заменяется на "Вы не можете понизить свою роль" message (защита и в backend, но UI предупреждает заранее); radio с 3 ролями + reason textarea (required, min 3); errors маппятся через `translateErr` в `admin.customers.errors.{key}`. **`<ResetPasswordDialog>`** ([client](apps/web/src/components/admin/customers/reset-password-dialog.tsx)) — Radix `<Dialog>` two-state: form (reason input) → success (показывает temp-password в `<code class="select-all font-mono text-lg">` + кнопка «Копировать» через `navigator.clipboard.writeText` + amber warning «Этот пароль показан только сейчас»). **Кнопка close обнуляет state** — пароль исчезает из памяти компонента, повторно не показать (только новый reset). Вход `hasPassword` показывает amber warning о OTP-only клиенте. **AdminNav** обновлён: `wip` флаг снят с `customers` (точка больше не amber). **i18n** — новый namespace `admin.customers.{list (title/subtitle/searchPlaceholder/filterRole/filterAllRoles/summary-plural/empty/table.{6 cols + noName}), detail.{back/noName/profile.{7 keys}/actions/orders.{title-with-count/empty/4 cols}/addresses.{title-with-count/empty/default}/loyalty.{title/empty/4 cols}/cards.{title/default/blocked}}, roles.{customer/manager/admin}, loyaltyTypes.{earn/spend}, role.{title/current/selfBlocked/targetLabel/reason/reasonPlaceholder/submit/submitting/successTitle/successBody}, resetPassword.{trigger/title/reason/reasonPlaceholder/submit/submitting/cancel/close/successHelp/warning/copy/copied/copyFailed/otpUserWarning}, errors.{9 keys}}` × 3 локали — итого **1149 keys × 3** (+80 от P6-T8). **Tests**: **+20 unit** в [`admin-customers.test.ts`](apps/web/src/server/admin-customers.test.ts) — `parseAdminCustomerListQuery` × 7 (defaults / undefined / happy / unknown role → null / page sanitize × 2 / q slice / array first); `RoleChangeSchema` × 5 (happy + bogus role fail + reason 3-char/501-char + strict-extra fail); `PasswordResetSchema` × 3 (happy + reason fail + strict-extra fail); **`generateTempPassword` × 5** (default 12 chars + custom length + safe alphabet — нет I/l/1/O/0 в 100-char sample / разные вызовы дают разные значения / length < 4 → throw). **+11 e2e** в [`admin-customers.spec.ts`](apps/web/e2e/admin-customers.spec.ts) — auth (anon role/reset → 401, customer role → 404 silent); list page видит target + filter role=customer + filter q по email; PATCH role customer→manager + БД flip + PaymentLog `user.role_change` audit; PATCH `cannot_self_demote` → 409 + БД unchanged; PATCH `role_unchanged` → 409; PATCH invalid_body (reason < 3) → 400; **POST reset-password** → temp-pass match `^[A-Za-z0-9]{12}$` + НЕ содержит I/l/1/O/0 + новый bcrypt-hash валидируется через `compare(temp, reloaded.passwordHash)` true; reset-password OTP-only user → 409 `user_has_no_password`; reset-password non-existent → 404; UI smoke: detail page → `<RoleChanger>` + reset-trigger видны; UI: смотрим самого себя → role-changer показывает selfBlocked-message. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 1149×3 ✅, **unit 474/474 web** (+20 от P6-T8), e2e P6-T8 локально 11/11. **Открытые вопросы**: (b) Reset password через email — explicit non-goal, admin-driven дизайн purpose-built для UZ-аудитории (звонок в support — нормальный flow). Остальные три — **(a)+(c)+(d) — закрыты follow-up'ом**. **(a) `User.isBlocked` flag**: новая обратимая [миграция `20260507160000_user_is_blocked`](packages/db/prisma/migrations/20260507160000_user_is_blocked/migration.sql) — `ALTER TABLE users ADD COLUMN is_blocked BOOLEAN NOT NULL DEFAULT false` (rollback = DROP COLUMN; existing users backfill'ятся false). NextAuth [`config.ts`](apps/web/src/auth/config.ts) обоих credentials-провайдеров (`email-password` + `otp`) теперь проверяют `user.isBlocked` после lookup'а и возвращают `null` (== auth fail). API endpoints: [`POST /api/admin/customers/[id]/block`](apps/web/src/app/api/admin/customers/%5Bid%5D/block/route.ts) с защитами `cannot_self_block` (admin не может заблокировать себя — 409), `already_blocked` (idempotency 409), `forbidden` (manager не может блокировать admin'а — 403); [`POST /api/admin/customers/[id]/unblock`](apps/web/src/app/api/admin/customers/%5Bid%5D/unblock/route.ts) — симметричный с `not_blocked` (409). Audit через PaymentLog `user.blocked` / `user.unblocked` с `targetUserId/reason/adminId/adminEmail`. **UI** [`<BlockToggleDialog>`](apps/web/src/components/admin/customers/block-toggle-dialog.tsx) с двумя режимами (block/unblock), reason textarea (required min 3), `<Ban>` или `<Undo2>` иконкой; кнопка вообще не рендерится для self (`isSelf && !isBlocked` → `null`). На detail page'е добавлен `<blockedBadge>` rose-tone, в list-page — `<blocked>` mini-tag в name-колонке. **(c) `/admin/audit` page** [`apps/web/src/server/admin-audit.ts`](apps/web/src/server/admin-audit.ts) — `parseAdminAuditQuery({group, q, from, to, page})` с allow-list group (`user|order|refund|recheck|cancel|webhook`), date-range sanitize (from > to → оба null, как в P6-T5), `getAdminAuditLog` query — `AND`-collected conditions с group-prefix через `OR.startsWith` для каждого prefix'а группы (`user.` ловит `user.role_change/password_reset/blocked/unblocked`); `q` ищет в action ИЛИ errorMessage. [Page](apps/web/src/app/%5Blocale%5D/admin/audit/page.tsx) — page-size 50, table 5 колонок (when ISO/action mono/status-code-tone-coded/details-via-renderRequestSummary/payment-id-truncated). **`renderRequestSummary(req)`** распознаёт частые fields (orderId/targetUserId/from→to/oldRole→newRole/amountCents/reason) и собирает compact «order=abc · user=def · «causes»» строку — admin видит суть события без раскрытия JSON. AdminNav расширен `audit` entry с `<ClipboardList>` иконкой; type union `labelKey` дополнен `"audit"`. **(d) Bulk role change**: `RoleBulkSchema = z.object({userIds: array.min(1).max(50), role: enum, reason}).strict()`, [`POST /api/admin/customers/bulk-role`](apps/web/src/app/api/admin/customers/bulk-role/route.ts) — pre-fetch users + per-id loop с теми же защитами что в single (`cannot_self_demote`/`role_unchanged`/`forbidden`/`user_not_found` → `skipped[{userId, reason}]`); валидные → `prisma.user.updateMany` одной atomic операцией + parallel `writePaymentLog` с `bulk: true` flag'ом. **UI** [`<CustomersBulkTable>`](apps/web/src/components/admin/customers/customers-bulk-table.tsx) replaces inline-table в list-page'е — checkbox-колонка + sticky bulk-bar (selected count + role select + reason input + Apply/Clear); confirm через `window.confirm` с ICU-plural'ом «# клиенту/клиентам»; toast `successAll`/`successPartial`. **i18n** +56 ключей (`admin.customers.{block + bulkRole + table.blocked + detail.blockedBadge + errors.{cannot_self_block, already_blocked, not_blocked}}` + `admin.audit.*` + `admin.nav.audit`) × 3 локали → **1205 keys × 3** (+56 от follow-up'а). **Tests**: **+18 unit** в `admin-customers.test.ts` (`BlockToggleSchema` × 3 + `RoleBulkSchema` × 6) и **+9 unit** в новом [`admin-audit.test.ts`](apps/web/src/server/admin-audit.test.ts) (defaults / undefined / happy / unknown group / from > to / mussor format / page sanitize × 3 / q slice / array first); **+10 e2e** в [`admin-customers-followup.spec.ts`](apps/web/e2e/admin-customers-followup.spec.ts) — (a) block + PaymentLog audit; cannot_self_block 409; already_blocked + not_blocked idempotency; **blocked user НЕ может залогиниться** (form fill + verify остаёмся на /auth/login); UI block-dialog → toast + БД flip; (c) audit page рендерит recent events + group=user filter показывает только `user.*` actions; (d) bulk happy 2 customers + skipped self-demote; bulk empty 400; bulk > 50 400; bulk anon 401. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 1205×3 ✅, **unit 492/492 web** (+18 от follow-up'а), e2e P6-T8 follow-up локально 10/10. **Закрыто третьим follow-up'ом ((b) self-service password reset через email)**: пересмотрел изначальное «explicit non-goal» решение в пользу полноценного flow'а. Новая обратимая [миграция `20260507180000_password_reset_tokens`](packages/db/prisma/migrations/20260507180000_password_reset_tokens/migration.sql) добавляет `PasswordResetToken(id, userId, tokenHash, expiresAt, usedAt, createdAt)` с FK Cascade на User. **Pure-helpers** [`apps/web/src/server/password-reset.ts`](apps/web/src/server/password-reset.ts): `PasswordResetRequestSchema` (email lowercase normalization), `PasswordResetCompleteSchema` (token regex + password 8..200); **`generateResetToken()`** — `crypto.randomBytes(32)` → 64-char hex plain + bcrypt-hash; **`parseCompoundToken(input)`** splits `tokenId.plain` на первой точке; `verifyResetToken` обёртка над bcrypt.compare; `computeExpiresAt(now)` → +30 минут; `createResetToken(userId)` — invalidates все non-used previous tokens (security best-practice — старые links перестают работать после нового request'а), создаёт row + возвращает compound `${tokenId}.${plain}`; `lookupResetToken(compound)` — discriminated `{kind: "ok"|"not_found"|"expired"|"already_used"|"invalid_format"}`. **API endpoints**: [`POST /api/auth/password-reset/request`](apps/web/src/app/api/auth/password-reset/request/route.ts) — Zod → **double rate-limit** (`pwd-reset:ip:{ip}` 5 req/15min anti-distributed-spam + `pwd-reset:email:{email}` 3 req/15min anti-flooding) → **tactical silence**: 200 одинаковый response для known/unknown email/blocked/OTP-only-user (защита от enumeration); для valid-user'а с password — `createResetToken` + `getResendClient().sendEmail(buildPasswordResetEmail(...))` + PaymentLog audit `auth.password_reset_requested {email, ip, sent}` (БЕЗ token!). [`POST /api/auth/password-reset/complete`](apps/web/src/app/api/auth/password-reset/complete/route.ts) — Zod → IP-rate-limit 10 req/15min (anti-bcrypt-bruteforce) → `lookupResetToken` discriminated mapping (`tokenInvalid` / `tokenExpired` / `tokenAlreadyUsed`); **atomic transaction** обновляет `User.passwordHash` + `PasswordResetToken.usedAt = now` (защита от race-condition replay'а — нельзя использовать token дважды даже при двух concurrent calls); blocked-users отбиваются с tokenInvalid; PaymentLog audit `auth.password_reset_completed`. **Email-шаблон** [`packages/notifications/src/templates/password-reset.ts`](packages/notifications/src/templates/password-reset.ts) — `buildPasswordResetEmail({rawLocale, email, resetUrl})` через `loadMessages` + `createTranslator`, возвращает `{subject, html, text}` с inline CSS-styled HTML (sky-button CTA + bigmax footer) + plain-text fallback; локализован на ru/uz/en; HTML-escape spec-symbols против XSS. **UI**: [request page](apps/web/src/app/%5Blocale%5D/auth/password-reset/page.tsx) с [`<PasswordResetRequestForm>`](apps/web/src/components/auth/password-reset-request-form.tsx) — submit → `setSubmitted(true)` всегда показывает success-message (tactical silence); [complete page](apps/web/src/app/%5Blocale%5D/auth/password-reset/%5Btoken%5D/page.tsx) с [`<PasswordResetCompleteForm token>`](apps/web/src/components/auth/password-reset-complete-form.tsx) — два password-input'а с client-side mismatch check + length 8..200 pre-validate. **`<LoginForm>`** обновлён: добавлен «Забыли пароль?» Link над кнопкой submit. **i18n** +34 ключа `auth.passwordReset.{request, complete, email}` × 3 локали → **1239 keys × 3** (+34). **Tests**: **+18 unit** в [`password-reset.test.ts`](apps/web/src/server/password-reset.test.ts) — Schemas × 7, `generateResetToken` × 3, `parseCompoundToken` × 5, `verifyResetToken` × 2, `computeExpiresAt` × 1. **+5 unit** в [`packages/notifications/templates/password-reset.test.ts`](packages/notifications/src/templates/password-reset.test.ts) — ru/uz/en happy + locale-fallback + HTML-escape против XSS. **+12 e2e** в [`password-reset.spec.ts`](apps/web/e2e/password-reset.spec.ts) — request known/unknown email + invalid format; complete happy → password updated (bcrypt.compare с новым/старым) + token.usedAt; **complete replay → 400 tokenAlreadyUsed**; complete invalid token; complete expired (manual UPDATE); complete short password; **полный round-trip: после reset login с новым password проходит**; UI request/complete smoke; UI password-mismatch inline error без round-trip'а; «Забыли пароль» link → `/auth/password-reset`. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 1239×3 ✅, **unit 510/510 web** (+18) + notifications **27/27** (+5), e2e локально 12/12. **Финал P6**: фаза 6 действительно закрыта — все 8 задач + 4 follow-up волны (P6-T5/T6/T7/T8 каждая получила follow-up; P6-T7 и P6-T8 — даже по два). Архитектурный bonus: token-based reset infra переиспользуема для будущих flow'ов (email-confirmation, magic-link login) — общий pattern `cuid.{plain}` + bcrypt-hash + expiresAt + usedAt.

- [x] **P6-T7** — Управление остатками по филиалам (F13/§8 master-prompt). Полноценный admin-CRUD по таблице `Stock` (variant × branch) с обязательным audit'ом каждого изменения. **Новая модель** [`StockLog`](packages/db/prisma/schema.prisma) + обратимая [миграция `20260507081655_add_stock_log`](packages/db/prisma/migrations/20260507081655_add_stock_log/migration.sql) — append-only лог (action / oldQty / newQty / delta / reason / adminUserId), FK через `SetNull` чтобы лог переживал удаление Stock-row'а или admin-пользователя. **Pure-helpers + Zod** [`apps/web/src/server/admin-stock.ts`](apps/web/src/server/admin-stock.ts): `parseAdminStockListQuery` (sanitize `?branchId=&q=&lowStock=&page=`), **`applyAdjust(oldQty, mode, value)`** — pure-арифметика для set/inc/dec с `Math.max(0, ...)` clamp'ом (защита от race-condition'а если admin наделал параллельных dec'ов больше чем есть на складе), `StockAdjustSchema = z.object({mode: enum, value: int.min(0).max(1M), reason: string.min(3, "reason_too_short").max(500, "reason_too_long")}).strict()`, `StockUpsertSchema` (variantId + branchId + quantity + reason), `StockImportRowSchema` (sku + branch*slug XOR branch_id + quantity coerce-числом), `parseStockCsv(text)` (header-validate, CRLF norm, > 1000 → too_many_rows, пустые ячейки игнор). **Server-fetch**: `getAdminStockBranches()` (active list для select'а), `getAdminStock(query)` (Prisma findMany с `OR`-clause по `variant.sku` + `variant.product.nameRu` ILIKE, page-size 50, **lowStock-фильтр post-fetch'ом** через `available <= LOW_STOCK_THRESHOLD` потому что Prisma не поддерживает column-to-column сравнение в WhereInput; для lowStock-режима fetch'им до 1000 строк затем filter+slice — branch-scope гарантирует small dataset), `getAdminStockEntry(id)` (детали + последние 50 audit-records с adminEmail). **API endpoints** все 4: [`POST /api/admin/stock`](apps/web/src/app/api/admin/stock/route.ts) — upsert по unique-индексу `[variantId, branchId]` с автоматическим `action: "create"` или `"set"` в audit; на P2003/P2025 → 400 `invalid_relation`; [`POST /api/admin/stock/[id]/adjust`](apps/web/src/app/api/admin/stock/[id]/adjust/route.ts) — set/inc/dec через `applyAdjust` + audit, **idempotent no-op**: если `newQty === oldQty` → 200 + `noop: true` без StockLog (избегаем замусоривания лога одинаковыми значениями); [`DELETE /api/admin/stock/[id]`](apps/web/src/app/api/admin/stock/[id]/route.ts) — pre-check `reserved > 0` → 409 `has_reserved` (есть pending-резервации, admin сначала должен их обработать), иначе StockLog `action: "delete"` + delete-row; [`POST /api/admin/stock/import`](apps/web/src/app/api/admin/stock/import/route.ts) — `text/csv` body, max 1000 rows, **batch-prefetch** variants by SKU + branches by id-or-nameRu (защита от N+1), per-row Prisma transaction для upsert+log, per-row failures в `skipped[]` (variant_not_found / branch_not_found / invalid_row / db_error), один request → multiple atomic transactions (важно: один rotten-row не валит остальные). **UI**: [list page](apps/web/src/app/[locale]/admin/stock/page.tsx) с auto-redirect на дефолтный branchId если не указан (`redirect(/${locale}/admin/stock?branchId=...)` через next/navigation); таблица 8 колонок (product / sku font-mono / variant `color · size` / quantity / reserved / **available** = `max(0, qty - reserved)` / updatedAt / actions), low-stock rows подсвечены `bg-amber-50` + `font-semibold text-amber-700` на available-числе; filter-form [`<StockListFilters>`](apps/web/src/components/admin/stock/stock-list-filters.tsx) (branch select обязателен, q ILIKE search, lowStock checkbox + `useTransition` для smooth nav); [`<StockAdjustDialog>`](apps/web/src/components/admin/stock/stock-adjust-dialog.tsx) — Radix Dialog с radio mode (set/inc/dec), label-меняющийся value input (для set — «Новое количество», для inc — «Сколько прибавить»), reason textarea, default value=current для set-режима (admin часто знает целевое количество после физического пересчёта); ошибки маппятся через `translateErr` в `admin.stock.errors.{key}`. [Import page](apps/web/src/app/[locale]/admin/stock/import/page.tsx) с [`<StockImportForm>`](apps/web/src/components/admin/stock/stock-import-form.tsx) — textarea + file-upload (читаем как text), POST `text/csv`, inline-summary `{processed, skipped[]}` с collapsible details (row + sku + reason). **AdminNav** обновлён: `wip` флаг снят со `stock` (точка больше не amber). **i18n** — новый namespace `admin.stock.{list (title/subtitle/branch/searchPlaceholder/lowStockOnly/import/summary-plural/empty/noBranches/table.{8 cols}), adjust.{trigger/title/current/modeLegend/mode.{3}/valueLabel.{3}/reason/reasonPlaceholder/submit/submitting/cancel/successTitle/successBody}, import.{title/subtitle/hint/submit/submitting/result/skippedSummary/skippedRow/errors.{empty/missing_columns/too_many_rows/generic}}, errors.{8 keys}}` × 3 локали — итого **1032 keys × 3** (+54 от P6-T7). **Tests**: **+35 unit** в [`admin-stock.test.ts`](apps/web/src/server/admin-stock.test.ts) — `parseAdminStockListQuery` × 7 (defaults / undefined / happy / lowStock-not-true → false / page sanitize × 3 / q slice / array first); **`applyAdjust` × 6** (set / set 0 / inc / dec / clamp-to-zero / runtime-clamp на отрицательный); `StockAdjustSchema` × 8 (happy + inc/dec + bogus mode fail + value < 0 / > 1M fail + reason 3-char/501-char + strict-extra fail); `StockUpsertSchema` × 3 (happy / quantity 0 / quantity < 0 fail); `StockImportRowSchema` × 5 (branch_slug / branch_id / без обоих → branch_required / quantity coerce / quantity invalid); **`parseStockCsv` × 6** (empty / missing_columns / happy / CRLF norm / > 1000 → too_many_rows / пустые ячейки игнор). **+13 e2e** в [`admin-stock.spec.ts`](apps/web/e2e/admin-stock.spec.ts) — auth (anon 401, customer 404 silent); list page видит seeded stock + filter по branchId; lowStock filter скрывает обычные records; POST upsert + StockLog `action: "create"`; adjust set 10→25 + StockLog reason verified; adjust dec clamp на 0 при value > current; **adjust no-op** (set к текущему qty) → 200 + `noop: true` + 0 StockLog rows; DELETE → ok + StockLog `action: "delete"` + delta=-12 + Stock-row removed (но лог остался через FK SetNull); DELETE с reserved>0 → 409 `has_reserved` с reserved=N в payload'е; CSV import 2 happy + 1 unknown SKU → processed=2 + skipped=1 reason=`variant_not_found`; CSV missing_columns → 400; CSV anon → 401; UI smoke (adjust-dialog → submit → router.refresh → row updated). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 1032×3 ✅, **unit 446/446 web** (+35 от P6-T7), e2e P6-T7 локально 13/13. **Открытые вопросы**: только (d) частично — checkout-reserve flow остаётся в P8-T1. **Закрыто follow-up'ом**: (a)+(c)+(b)+(d-частично) пакетом. **(a) Реальный `slug` для StoreBranch**: новая обратимая [миграция `20260507100000_add_store_branch_slug`](packages/db/prisma/migrations/20260507100000_add_store_branch_slug/migration.sql) (`ALTER TABLE … ADD COLUMN slug TEXT` + `CREATE UNIQUE INDEX`, rollback = `DROP COLUMN slug + DROP INDEX`). `BranchBaseSchema` расширен `slug: SlugSchema.nullable().optional()`, `<BranchForm>` получил input-поле + hint, `POST /api/admin/branches` и `PATCH /api/admin/branches/[id]` обрабатывают slug + 409 `slug_exists` (дублирующийся через `prismaErrorCode === "P2002"`). [`/api/admin/stock/import`](apps/web/src/app/api/admin/stock/import/route.ts) теперь lookup'ает branch по `id` ИЛИ `slug` (раньше — `id` ИЛИ `nameRu`). **(c) Stock history page** [`/admin/stock/[id]/history`](apps/web/src/app/[locale]/admin/stock/[id]/history/page.tsx) — page-size 50, pagination через `?page=N`, header с current state (qty/reserved/available + product info), table 7 колонок (when/action/oldQty/Δ/newQty/admin/reason) с tone-coded delta (emerald positive, rose negative, muted zero). `getAdminStockHistory(stockId, page)` — отдельный fetch с `prisma.stockLog.findMany` + count parallel; resolves variantId+branchId по stockId чтобы лог переживал удаление Stock-row'а через FK SetNull. Кнопка «История» добавлена в actions-колонку list-page. **(b) Bulk-adjust** по SKU-pattern: `StockBulkAdjustSchema = z.object({branchId, skuPattern, mode, value, reason, limit?: 1..200}).strict()`, **`skuPatternToLike(pattern)`** pure-функция (`*`→`%`, экранирование `%`/`*` от LIKE-injection); [`POST /api/admin/stock/bulk-adjust`](apps/web/src/app/api/admin/stock/bulk-adjust/route.ts) — pre-fetch matching rows с regex post-filter, sequential transaction'ы (per-row Stock+StockLog) c reason="bulk: …", лимит 200 за один запрос (4 страницы). UI [`<StockBulkAdjustDialog>`](apps/web/src/components/admin/stock/stock-bulk-adjust-dialog.tsx) — Radix Dialog с pattern input + radio mode + value + reason, `confirm`через`window.confirm`; toast'ит `successAll`/`successPartial`/`noMatch`. **(d) Stock shipment audit (частично)**: новый helper [`commitOrderShipment(orderId, adminUserId)`](apps/web/src/server/order-stock-movement.ts) — pickup-orders берут `Order.branchId`, courier — фолбэк на первый-активный (proper routing → P8-T1); per-item: `Stock.quantity -= item.quantity`+`StockLog{action: "ship", reason: "Order {number} shipped"}`. Best-effort: skipped[] для no_stock_row/no_branch_resolved/db_error (не валим status flip). Wired в [`POST /api/admin/orders/[id]/status`](apps/web/src/app/api/admin/orders/[id]/status/route.ts) — на transition'е `→ shipped`после`Order.update`зовём`commitOrderShipment`+`writePaymentLog{action: "order.shipment*committed"}` для трейсабельности. **i18n** +38 ключей (`admin.stock.{historyAction, history, bulkAdjust}`+`admin.branches.form.fields.{slug, slugHint}`) × 3 локали → **1070 keys × 3** (+38). **Tests**: **+8 unit** в [`admin-stock.test.ts`](apps/web/src/server/admin-stock.test.ts) — `skuPatternToLike` × 4 (`\*`→`%`, `%`экранирование,`\*`экранирование, без wildcard),`StockBulkAdjustSchema` × 4 (happy + дефолт limit=200 + > 200 fail + пустой pattern fail) — теперь 43/43. **+10 e2e** в [`admin-stock-followup.spec.ts`](apps/web/e2e/admin-stock-followup.spec.ts) — (a) CSV import lookup by branch_slug + PATCH /api/admin/branches с slug; (b) bulk-adjust SKU-pattern `NB-_` → 2 happy матча + OTHER-_ не задет + StockLog audit; (b) noMatch → matched=0; (b) anon → 401; (c) history page рендерит StockLog-row после adjust с правильным data-action + delta; (c) pagination (55 seeded → 50+5 на page 1+2); (d) `→ shipped` транзишн → Stock.quantity -3 + StockLog action="ship" с order.number в reason; (d) shipped без Stock-row → status flip всё равно (best-effort). **CI**: typecheck ✅, lint ✅, format ✅, i18n parity 1070×3 ✅, **unit 454/454 web** (+8 от follow-up'а), e2e P6-T7 follow-up локально 10/10. **Открытые вопросы**: нет — все четыре полностью закрыты, в том числе **(d-полное) Reserve at checkout** реализован вторым follow-up'ом. **Закрыто вторым follow-up'ом ((d-полное))**: новая обратимая [миграция `20260507140000_stock_log_reserved`](packages/db/prisma/migrations/20260507140000_stock_log_reserved/migration.sql) — `ALTER TABLE stock_logs ADD COLUMN old_reserved/new_reserved/reserved_delta INTEGER NOT NULL DEFAULT 0` (rollback = три DROP COLUMN; existing rows backfill'ятся 0 без data-миграции). [`order-stock-movement.ts`](apps/web/src/server/order-stock-movement.ts) переписан с трёх pure-helpers'ов: **`reserveOrderStock(orderId, adminUserId|null, tx?)`** инкрементит `Stock.reserved += qty` для каждого item'а + StockLog{action:"reserve", oldQty=newQty=quantity (нетронут), oldReserved/newReserved/reservedDelta} с `reason: "Order {number} reserved (X units)"`; **`releaseOrderStock(orderId, ...)`** — декрементит `Stock.reserved -= qty` (qty не трогает) + StockLog{action:"release"}, idempotency-guard на `reserved <= 0` → `skipped[no_reserved_to_release]`; **`commitOrderShipment(orderId, ...)`** расширен — теперь декрементит И quantity И reserved одной транзакцией, одной StockLog-записью с обеими delta'ми. **Wired в 4 точки order-flow'а**: (1) [`/api/checkout/pay`](apps/web/src/app/api/checkout/pay/route.ts) — внутри транзакции после `tx.order.create + tx.orderItem.createMany + tx.payment.create` зовём `reserveOrderStock({tx})` атомарно; (2) [`/api/account/orders/[id]/cancel`](apps/web/src/app/api/account/orders/%5Bid%5D/cancel/route.ts) — после flip'а `Order.status=cancelled` зовём `releaseOrderStock({adminUserId: null})` (customer-flow); (3) admin status-change → `cancelled`/`refunded` → `releaseOrderStock` + PaymentLog{action:"order.stock_released"}; (4) admin status-change → `shipped` → расширенный `commitOrderShipment` + PaymentLog{action:"order.shipment_committed"}. Все wires — best-effort: `skipped[]` записывается в PaymentLog для трейсабельности, не валит status flip / cancel / checkout если Stock-row отсутствует. **Branch routing**: pickup → `Order.branchId`, courier → первый-активный (proper logistics — P8-T1). **History page UI** обновлён — table-колонки `oldQty/Δ/newQty` объединены в одну `qty` с inline ↑/↓-arrow + delta value, добавлена parallel-колонка `reserved` с такой же визуализацией (sky-tone для +, violet-tone для −). **i18n** ключи `admin.stock.history.table.{oldQty/delta/newQty}` заменены на `{qty/reserved}` × 3 локали → **1069 keys × 3** (-1 от консолидации). **Tests**: **+2 e2e** в `admin-stock-followup.spec.ts` — (d-полное) shipped: pre-seeded `quantity=50, reserved=3` + order(qty=3) → после ship: `quantity=47, reserved=0` + StockLog{delta=-3, reservedDelta=-3, oldReserved=3, newReserved=0}; (d-полное) admin cancelled → release: pre-seeded `reserved=5` + order(qty=2) → после cancel: `quantity=50 (нетронут), reserved=3` + StockLog{action="release", delta=0, reservedDelta=-2}. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 1069×3 ✅, **unit 454/454 web** (без изменений — все новые проверки в e2e), e2e локально 12/12. **Известные ограничения** (P8-T1 — load-test phase): (1) `reserveOrderStock` не дедупит при повторном вызове — caller отвечает за единственность через transaction-locking; webhook-double-callback теоретически может удвоить reserve, mitigation через `Order.metadata.stock_reserved_at` flag — пока не критично потому что `/api/checkout/pay` зовёт reserve внутри tx ровно один раз; (2) courier-order routing — фолбэк на «первый-активный» филиал; multi-warehouse fulfillment routing требует логистических правил которых пока нет; (3) если Order имеет items из несуществующих Stock-row'ов (variant ещё не размещён в branch'е), reserve пропускает их — admin может потом физически добавить через [`/admin/stock`](apps/web/src/app/[locale]/admin/stock/page.tsx) с reason "post-checkout placement".

- [x] **P6-T6** — Управление платежами и возвратами (F13/§5.9, §8 master-prompt). Закрывает все три фичи задачи: **просмотр** Payment/PaymentLog/WebhookEvent, **возврат средств** (полный/частичный) → запрос в Uniteller (или скип для COD) → запись `Refund(status=completed)`, **ручной перезапрос статуса** (`recheck`) для зависших pending-платежей через `/results/` API. **Pure helpers** [`apps/web/src/server/admin-payments.ts`](apps/web/src/server/admin-payments.ts): `parseAdminPaymentListQuery` (sanitize `?q=` trim+slice 100, `?status=` allow-list, `?provider=` allow-list `uniteller`/`cod`, `?page=` NaN/<=0 → 1), `RefundCreateSchema = z.object({amountCents: int.positive().max(1B), reason: string.trim().min(3, "reason_too_short").max(500, "reason_too_long")}).strict()` — три явных error-message-ключа маппятся в i18n, **`computeRefundableRemaining(amountCents, refunds[])`** — pure-арифметика: `payment.amountCents − ∑(refunds where status ∈ {completed, pending}).amountCents`, clamp на 0; **pending refund'ы тоже резервируют** (защита от race-condition'а когда admin наделал параллельных рефандов на сумму больше captured), **`canRefundPayment(status)`** → true только для `captured`/`partially_refunded`. **Server-fetch**: `getAdminPayments(query)` (Prisma findMany с `OR`-clause по `Order.number` + `Order.user.email` ILIKE, page-size 20, parallel count, join order+user для customer-info), `getAdminPayment(id)` (full детали + refunds[orderBy desc] + logs[take:50, orderBy desc] + WebhookEvent через отдельный query по `unitellerOrderIdp` — связь идёт через идемпотентный `externalId` из P4-T6, не FK). Возвращаемый `AdminPaymentDetail` включает computed `refundableRemaining` + `canRefund` (boolean) — UI просто читает флаг. **API endpoints**: [`POST /api/admin/payments/[id]/refund`](apps/web/src/app/api/admin/payments/[id]/refund/route.ts) — `requireAdminSession` → Zod → 404 `payment_not_found` → 409 `not_refundable` (статус-проверка) → 409 `amount_exceeds_remaining` (с remaining в payload'е чтобы UI мог показать актуальную цифру) → **Uniteller-step** (для `provider="uniteller"`): env-check (если `UNITELLER_SHOP_ID/AUTH_LOGIN/AUTH_PASSWORD` пустые → 503 `provider_misconfigured` + Sentry-report), `cancelUnitellerPayment({orderId, billnumber, ...isFull ? {} : {subtotal: centsToDecimal}})` — Uniteller `/cancel/` с Subtotal только для частичного refund'а (для полного — Uniteller берёт исходную сумму из своих логов). На `cancelResult.kind !== "ok"` → PaymentLog(`refund_failed`) + Sentry + 502; **НЕ создаём Refund(failed)** — admin может ретраить без подсчёта зарезервированной суммы. **COD-step**: skip провайдер (у наложенного нет онлайн-cancel'а — refund возмещается курьером оффлайн, мы только помечаем в БД). Atomic transaction: `Refund.create({status: "completed", initiatedByUserId})` + `Payment.update({status: amountCents == remaining ? "refunded" : "partially_refunded"})`; PaymentLog audit с `refund_completed` action + `{adminId, adminEmail, provider}` для трейсинга. Response `{ok, refundId, paymentStatus, refundableRemaining}`. [`POST /api/admin/payments/[id]/recheck`](apps/web/src/app/api/admin/payments/[id]/recheck/route.ts) — manual pull-status: rejects COD (400 `cod_recheck_unsupported`) и платежи без `unitellerOrderIdp` (400 `no_uniteller_idp` — например, если запрос `/api/checkout/pay` упал ДО создания Uniteller-формы); env-check; `fetchPaymentStatus({orderId: unitellerOrderIdp, ...auth})` → `mapUnitellerStatus(item.Status)` → flip Payment если `internal !== current` (Authorized/Paid → captured + `capturedAt = now` + `Order.status = confirmed` если ещё `pending`; Canceled/NotAuthorized → failed); WebhookEvent НЕ вставляется чтобы не дублировать webhook'и (recheck = pull, не push); PaymentLog `recheck_ok`/`recheck_failed` с raw response. Response `{ok, status, changed: bool, providerStatus}` — UI показывает «изменён»/«без изменений» в toast'е. **`centsToDecimal(cents)`** локальный helper в refund-route'е: `Math.floor(cents / 100) + "." + (cents % 100).pad(2)` — Uniteller требует `"X.YY"` decimal (master-prompt §5.4). **UI**: [list page](apps/web/src/app/[locale]/admin/payments/page.tsx) с filter-form [`<PaymentsListFilters>`](apps/web/src/components/admin/payments/payments-list-filters.tsx) (search input по number/email + status select + provider select + `useTransition`-driven querystring), table 7 колонок (orderNumber font-mono Link → detail / createdAt / customer name+email / provider-translated / amount UZS / cardMask / status-badge), pagination prev/next; [detail page](apps/web/src/app/[locale]/admin/payments/[id]/page.tsx) с breadcrumbs `[payments, {orderNumber}]`, header с `<PaymentStatusBadge>` + `<RecheckButton>` + `<RefundDialog>` (показывается только если `canRefund=true`), 2-grid summary/uniteller-info секции (orderIdp/billnumber/responseCode/cardMask), conditional refunds-table с tone-mapped status'ами, **PaymentLog timeline** (50 последних записей: createdAt/action/statusCode/errorMessage), **WebhookEvent log** (20 последних callback'ов с processed flag — admin сразу видит pending events при инциденте). **`<PaymentStatusBadge>`** ([`payment-status-badge.tsx`](apps/web/src/components/admin/payments/payment-status-badge.tsx)) — pure server component, tone-mapping `bg-{amber|emerald|rose|slate|violet|indigo}-100` для 6 статусов, выровнен с `<OrderStatusBadge>` чтобы admin не переключал ментальные палитры. **`<RefundDialog>`** ([`refund-dialog.tsx`](apps/web/src/components/admin/payments/refund-dialog.tsx)) — Radix `<Dialog>` с amount input (`type="number"`, default = full remaining → admin делает full refund в один клик) + reason textarea + client-side pre-validate (`amount > 0`, `≤ remaining` чтобы не делать round-trip за очевидной ошибкой). Submit → POST → `toast.success(successTitle/successBody)` + `router.refresh()` + close; ошибки маппятся через `translateErr` в `admin.payments.errors.{key}`. **`<RecheckButton>`** ([`recheck-button.tsx`](apps/web/src/components/admin/payments/recheck-button.tsx)) — `<RefreshCw>` иконка с `animate-spin` во время submit'а, `disabled` для `provider="cod"` + tooltip `disabledCod`. На success → `toast.info` если `changed=false`, `toast.success` если `changed=true`. **AdminNav** обновлён: `wip` флаг снят с `payments` (точка больше не amber). **i18n** — новый namespace `admin.payments.{list (title/subtitle/searchPlaceholder/filterStatus/filterAllStatus/filterProvider/filterAllProvider/table.{7 cols}/empty), detail.{back/title/createdAt/summary.{7 keys}/uniteller.{6 keys}/refunds.{4 cols}/logs.{title/empty/4 cols}/webhooks.{title/empty/4 cols/pending}}, statuses.{6 enum keys}, providers.{uniteller/cod}, refundStatuses.{3 keys}, refund.{trigger/title/remaining/amount/reason/reasonPlaceholder/submit/submitting/cancel/successTitle/successBody}, recheck.{trigger/submitting/successChanged/successUnchanged/disabledCod}, errors.{12 keys: invalid_body/invalid_amount/amount_exceeds_remaining/not_refundable/payment_not_found/provider_error/provider_misconfigured/cod_recheck_unsupported/no_uniteller_idp/reason_too_short/reason_too_long/generic}}` × 3 локали — итого **960 keys × 3** (+88 от P6-T6). **Tests**: **+29 unit** в [`admin-payments.test.ts`](apps/web/src/server/admin-payments.test.ts) — `parseAdminPaymentListQuery` × 8 (defaults / undefined / happy / unknown status/provider / page sanitize / q slice / array first), `RefundCreateSchema` × 9 (happy + amountCents 0/-100/float/>1B + reason 3-char/501-char/strict-extra/trim), `computeRefundableRemaining` × 6 (no refunds, completed minus, pending reserved, failed-not-counted, multi-completed, clamp на 0), `canRefundPayment` × 6 через `it.each` (captured/partially_refunded → true; pending/failed/cancelled/refunded → false). **+13 e2e** в [`admin-payments.spec.ts`](apps/web/e2e/admin-payments.spec.ts) — auth (anon refund/recheck → 401, customer refund → 404 silent); list (seeded видна), filter status=captured/provider=cod; detail (captured Uniteller → Refund + Recheck enabled; COD → Recheck disabled но Refund есть); recheck COD → 400 `cod_recheck_unsupported`; refund COD happy full → БД-flip (`Payment.status=refunded` + Refund row + PaymentLog `refund_completed`); refund COD partial 30% → `partially_refunded` + remaining 70%; refund amount > remaining → 409 `amount_exceeds_remaining` + remaining в payload'е; refund pending → 409 `not_refundable`; refund missing reason → 400; refund non-existent id → 404; UI smoke (refund-modal trigger → form fill → submit → БД flip + refunds-section появляется). Refund на Uniteller-провайдере **НЕ покрыт e2e** — требует mock-server'а который не запущен в CI прогоне для admin-flow'а; покрыто unit'ами (cancelUnitellerPayment имеет 100%-coverage в `packages/payments`) + интеграционная проверка через CI Uniteller-mock в P4-T12. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 960×3 ✅, **unit 404/404 web** (+29 от P6-T6), e2e P6-T6 локально 13/13. **Открытые вопросы**: нет — все четыре закрыты follow-up'ом. **Закрыто follow-up'ом**: (a)+(b)+(c)+(d) сразу пакетом. **(a) E2E Uniteller-refund через mock-server**: добавлена ленивая env-override в [`UNITELLER` constants](packages/payments/src/uniteller/constants.ts) — getter'ы `payUrl`/`resultsUrl`/`cancelUrl`/`wsdlUrl` лениво резолвят `process.env["UNITELLER_BASE_URL"]` (с fallback'ом на `https://wpay.uniteller.ru`), что позволяет admin-flow refund'ам автоматически идти на mock-server. **playwright-config** теперь поднимает web-server c `UNITELLER_BASE_URL=${MOCK_URL}`, поэтому server-side `cancelUnitellerPayment` адресуется на `localhost:8787/cancel/`. Mock auto-creates Canceled-state для unknown order'ов (см. [`mock-server.ts handleCancel`](packages/payments/src/uniteller/mock-server.ts)), и тест `admin-payments-followup.spec.ts:(a)` проверяет: `Refund(status=completed)` в БД, `unitellerRefundId` = `${billnumber}-r1`, mock-state переключился в `Canceled`. **(b) Refund(status="failed") audit row**: при `cancelResult.kind !== "ok"` теперь создаётся `Refund(status="failed", reason: admin-reason)` ДО `writePaymentLog(refund_failed)`. Безопасно потому что `computeRefundableRemaining` исключает failed (admin может ретраить без подсчёта зарезервированной суммы). UI «Refunds»-секция на detail page'е уже показывает все статусы — auto-видна история попыток. **(c) Synthetic `unitellerRefundId`**: Uniteller v1.43 не возвращает `RefundID` в ответе `/cancel/`. Мы строим стабильный correlation-id `${billnumber || orderIdp || orderNumber}-r${attemptIndex}` где `attemptIndex = payment.refunds.length + 1` (включая failed-row'ы — нумерация по всем попыткам, чтобы admin мог сматчить PaymentLog запись с ЛК Uniteller'а через timestamp + counter). Записывается в `Refund.unitellerRefundId` при `status="completed"`, для COD остаётся null. **(d) Bulk refund**: `RefundBulkSchema = z.object({paymentIds: array.min(1).max(50), reason}).and(discriminatedUnion("mode", [{mode: "full"}, {mode: "fixed", amountCents}]))` — 50 vs. 200 для bulk status change потому что каждый refund — outbound Uniteller call с 30s timeout. **`refund-execute.ts`** — извлечён общий исполнитель из single-refund route'а в `executeRefund(input)` с discriminated `RefundExecuteResult`-union; используется и single-route, и bulk-route. **`POST /api/admin/payments/bulk-refund`** — sequential обработка (не Promise.all) чтобы не ddos'ить Uniteller параллельными запросами и чтобы один stalled refund не блокировал остальные. `processed[]` + `skipped[{paymentId, reason, message?}]`. UI [`<PaymentsBulkTable>`](apps/web/src/components/admin/payments/payments-bulk-table.tsx) replaces the inline table в list-page'е: per-row checkbox + sticky bulk-bar с radio mode (full/fixed) + amount input (для fixed) + reason input + Apply/Clear; `confirm` через `window.confirm` с ICU-plural'ом «# платежу/платежам» + предупреждением «Uniteller refunds — необратимы». **i18n** +18 ключей `admin.payments.bulkRefund.*` × 3 локали → **978 keys × 3** (+18 от follow-up'а). **Tests**: **+7 unit** в [`admin-payments.test.ts`](apps/web/src/server/admin-payments.test.ts) для `RefundBulkSchema` (full/fixed happy + fixed без amount fail + bogus mode fail + > 50 fail + empty fail + reason < 3 fail) — теперь 36/36 в этом файле. **+9 e2e** в [`admin-payments-followup.spec.ts`](apps/web/e2e/admin-payments-followup.spec.ts) — (a)+(c) Uniteller refund happy → mock /cancel/ → Refund(completed) + synthetic id `${billnumber}-r1` + mock-state.Canceled (real outbound HTTP против mock'а); (c) attemptIndex увеличивается с failed-row'ами → `r2`; (b) failed-refund НЕ резервирует remaining (admin ретраит full); (d) bulk full mode 2 платежа → processed=2 + БД flip; (d) bulk fixed mode 30% → partially_refunded; (d) bulk mixed (captured + pending + non-existent) → `skipped` содержит `not_refundable` + `payment_not_found`; (d) bulk > 50 → 400; (d) bulk anon → 401; (d) UI smoke (bulk-bar + radio buttons видны). **CI**: typecheck ✅, lint ✅, format ✅, i18n parity 978×3 ✅, **unit 411/411 web** (+7 от follow-up'а), e2e P6-T6 follow-up локально 9/9. **Архитектурный bonus**: `UNITELLER_BASE_URL` env override открывает дорогу для будущих integration-тестов P4-T6 (webhook handler) и P4-T9 (`checkPendingPaymentsJob` в worker'е) против того же mock-server'а — раньше требовало бы рефакторинга URL-констант через DI.

- [x] **P6-T5** — Управление заказами + смена статуса + PDF-накладная с логотипом Bigmax (F13/§5.5/§8 master-prompt). **State-machine** [`apps/web/src/server/admin-orders.ts`](apps/web/src/server/admin-orders.ts) — pure-функция `canTransitionOrderStatus(from, to)` + `allowedTransitionsFrom(from)`, переходы согласованы с `validateRefundEligibility` / `validateCancelEligibility` из P5: `pending → confirmed|cancelled`, `confirmed → packing|cancelled`, `packing → shipped|cancelled`, `shipped → delivered|cancelled`, `delivered → refunded`, `cancelled`/`refunded` — терминальные (нет исходящих); self-transition всегда `false`. **Zod**: `OrderStatusChangeSchema = z.object({status: enum, reason?: string.trim().max(500)}).strict()` (extra fields → fail). **Querystring** `parseAdminOrderListQuery` — sanitize `?q=` (trim+slice 100, пустая → null), `?status=` (allow-list, garbage → null), `?page=` (NaN/<=0 → 1, array → first). **Server-fetch**: `getAdminOrders(query)` — Prisma findMany с `_count.items` + `payments[take:1, orderBy createdAt desc]` + parallel count, page-size 20, `orderBy createdAt desc`, поиск по `Order.number` ИЛИ `User.email` через ILIKE OR-clause; `getAdminOrder(id)` — full детали с items + payments + refunds (отдельный query по `paymentId in [...]` чтобы Prisma не разбухала на сложном relation), address + branch. **API endpoints**: [`POST /api/admin/orders/[id]/status`](apps/web/src/app/api/admin/orders/[id]/status/route.ts) — `requireAdminSession` → Zod → 404 если нет заказа → 409 `status_unchanged` если уже в target → 400 `illegal_transition` (с `from`/`to` в payload) если переход запрещён → `prisma.order.update` + `writePaymentLog({action: "order.status_change", request: {orderId, from, to, reason, adminId, adminEmail}})` для audit (PaymentLog с `paymentId=null` если у заказа нет payments). [`GET /api/admin/orders/[id]/invoice.pdf`](apps/web/src/app/api/admin/orders/[id]/invoice.pdf/route.ts) с `runtime = "nodejs"` (обязательно — `@react-pdf/renderer` использует Node Buffer/streams, Edge не поддерживается) и `dynamic = "force-dynamic"`; рендерит PDF через `renderToBuffer(<InvoiceDocument order={order}/>)` → возвращает `Uint8Array(buf)` (Node `Buffer` не входит в DOM `BodyInit`-типы) с `Content-Type: application/pdf` + `Content-Disposition: attachment; filename="bigmax-invoice-{number}.pdf"`. **PDF-генератор** [`apps/web/src/server/order-invoice-pdf.tsx`](apps/web/src/server/order-invoice-pdf.tsx) — `@react-pdf/renderer` JSX-friendly, A4 одностраничник: header с **текстовым логотипом** «BIGMAX» в sky-coloured rounded box (без SVG чтобы не тащить font-loading в server-runtime — Helvetica/Helvetica-Bold уже встроены) + bigmax.uz subtitle, шапка `№ {number}` + `от {date}` + статус, two-column purchaser/delivery section, items-таблица 4 колонки (наименование/qty/цена/сумма) с line-items + totals (subtotal/delivery/discount conditional/grand-total), опц. comment-section, fixed footer с pagination. Локализация — только русский (master-prompt §F4: invoice — официальный документ для UZ-покупателя; uz/en расширение отложено в P8). **Установлен** `@react-pdf/renderer ^4.5.1` в `apps/web`. **UI**: [list page](apps/web/src/app/[locale]/admin/orders/page.tsx) с filter-form [`<OrdersListFilters>`](apps/web/src/components/admin/orders/orders-list-filters.tsx) (search input по number/email + status select + `useTransition` для smooth nav, querystring-driven), table 7 колонок (number font-mono Link / createdAt / customer name+email / itemsCount / total UZS / payment provider+status / status-badge), pagination prev/next; [detail page](apps/web/src/app/[locale]/admin/orders/[id]/page.tsx) с breadcrumbs `[orders, {number}]`, header с `<OrderStatusBadge>` + кнопка «Скачать PDF» (`<a href="/api/admin/orders/{id}/invoice.pdf">` через `<Button asChild>`), 2-grid customer/delivery, items-таблица с tfoot totals, payments + refunds sections, `<OrderStatusChanger>` внизу. **`<OrderStatusBadge>`** ([`order-status-badge.tsx`](apps/web/src/components/admin/orders/order-status-badge.tsx)) — pure server component с tone-mapping `bg-{amber|sky|indigo|violet|emerald|rose|slate}-100`. **`<OrderStatusChanger>`** ([client component](apps/web/src/components/admin/orders/order-status-changer.tsx)) — radio-buttons по `allowedTransitionsFrom(current)` (если empty → fallback message «Из {status} нет переходов»), опц. `reason` textarea (max 500), submit → POST → `toast.success` + `router.refresh`; ошибки маппятся в `admin.orders.errors.{reason}` (illegal_transition / status_unchanged / not_found / generic). `<AdminNav>` обновлён: `wip` флаг снят с `orders` (точка больше не amber). **Logo**: SVG `apps/web/public/bigmax-logo.svg` для UI, в PDF — текстовый rendering (см. выше). **i18n** — новый namespace `admin.orders.{list (title/subtitle/searchPlaceholder/search/filterAll/table.{7 cols}/empty/itemsCount-plural/open), detail.{back/title/createdAt/customer/delivery/items/totals/payments/refunds/invoice}, status.{title/current/to/reasonLabel/reasonPlaceholder/submit/submitting/successTitle/successBody/noTransitions}, statuses.{7 enum keys}, errors.{5 keys}}` × 3 локали — **857 keys × 3** (+73 от P6-T5). **Tests**: **+44 unit** в [`admin-orders.test.ts`](apps/web/src/server/admin-orders.test.ts) — state-machine с `it.each` (9 разрешённых пар × `true` + 14 запрещённых × `false` + 7 self-transitions × `false`); `allowedTransitionsFrom` × 4; `OrderStatusChangeSchema` × 6 (happy + reason + bogus status + > 500 + strict + trim); `parseAdminOrderListQuery` × 10 (defaults / undefined / parsed / unknown status / page sanitize × 3 / q empty / q slice / array first). **+9 e2e** в [`admin-orders.spec.ts`](apps/web/e2e/admin-orders.spec.ts) — anon → 401 / customer → 404 (silent admin-API guard); list рендерится + search фильтрует; status=confirmed скрывает pending; detail показывает только разрешённые переходы (`order-status-target-confirmed/cancelled` видны, `shipped/delivered` отсутствуют); `pending → confirmed` API → 200 + БД updated + `PaymentLog.action = "order.status_change"` запись с `request.orderId` найдена; illegal `pending → delivered` → 400 `illegal_transition` + БД unchanged; `status_unchanged` → 409; invoice.pdf → `application/pdf` + `%PDF` magic bytes + body > 1KB; 404 на cuid_doesnt_exist. Cleanup-helper моет orphan PaymentLog по JSONB `request.path = ["orderId"]` поиску. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 857×3 ✅, **unit 364/364 web** (+44 от P6-T5), e2e P6-T5 локально 9/9. **Открытые вопросы**: нет — все четыре закрыты follow-up'ом. **Закрыто follow-up'ом**: (a)+(b) **Кириллический шрифт + локализация PDF на 3 локали** — самый критический баг: Helvetica отдавала `■■■` для русских строк во многих PDF-viewer'ах. Скачаны Roboto-Regular + Roboto-Bold TTF (Apache 2.0) из github.com/googlefonts/roboto в [`apps/web/public/fonts/`](apps/web/public/fonts/) (~1MB total), регистрация через `Font.register({family: "Roboto", fonts: [{src: path}, {src: path, fontWeight: "bold"}]})` с idempotent guard (`fontsRegistered` flag — register'им один раз на process-lifecycle) и `Font.registerHyphenationCallback((w) => [w])` чтобы заглушить «forgot to register hyphenation callback» warning'и. Все стили переключены с `Helvetica` / `Helvetica-Bold` → `Roboto` + `fontWeight: "bold"`. Для локализации в [`order-invoice-pdf.tsx`](apps/web/src/server/order-invoice-pdf.tsx) добавлен `LABELS: Record<Locale, Record<string, string>>` со ~30 ключами × 3 локали (title/no/from/statusLabel/purchaser/name/email/phone/delivery/method/courier/pickup/address/branch/apartment/items/itemName/qty/price/sum/productFallback/subtotal/deliveryRow/discount/total/comment + 7 status-key'ев + pageCounter + locale для toLocaleString). PDF выбирает локаль из `Order.locale` (которая хранится в БД с момента чекаута P4-T1) — у каждого клиента накладная на «его» языке. `getAdminOrder` расширен `locale: true` в select; роут [`/api/admin/orders/[id]/invoice.pdf`](apps/web/src/app/api/admin/orders/[id]/invoice.pdf/route.ts) теперь передаёт `{locale: order.locale}` в `renderOrderInvoicePdf`. (d) **Date range filter** — `parseAdminOrderListQuery` расширен `from`/`to` (Date | null), `parseDateBoundary(input, kind)` парсит `YYYY-MM-DD` → start-of-day UTC (для `from`) или end-of-day UTC (для `to`); regex `^\d{4}-\d{2}-\d{2}$` отсекает мусор; `from > to` → оба обнуляются (защита от опечаток в querystring, чтобы admin не получал unexpected пустой результат). `getAdminOrders` добавляет `createdAt: {gte, lte}` в where при наличии. UI [`<OrdersListFilters>`](apps/web/src/components/admin/orders/orders-list-filters.tsx) переписан с `<Label>`-ами над каждым полем + `type="date"` inputs для from/to + кнопка «Применить» + «Сбросить» (видна когда есть хоть один фильтр). Pagination href преобразовывает Date → ISO date через `.toISOString().slice(0,10)`. (c) **Bulk status change** — `OrderBulkStatusSchema = z.object({ids: array(string).min(1).max(200), status: enum, reason?: string.trim().max(500)}).strict()`. [`POST /api/admin/orders/bulk`](apps/web/src/app/api/admin/orders/bulk/route.ts): дедуп `ids` через `Set`, `findMany({where: {id: in [...]}})` для существующих заказов, цикл по uniqueIds: each → не найден → `skipped[{id, reason: "not_found"}]`, статус совпадает → `status_unchanged`, `!canTransitionOrderStatus(from, to)` → `illegal_transition`; собранные `toUpdate[]` атомарно через `updateMany` (single SQL UPDATE) + параллельно `writePaymentLog` для каждого с `bulk: true` flag'ом (отличает от single-call'а); response `{ok, updated, skipped[{id, reason}]}`. Не throw'ит на partial-failure'ы — admin видит ровно сколько применилось. **UI** [`<OrdersBulkTable>`](apps/web/src/components/admin/orders/orders-bulk-table.tsx) replaces the inline table в list-page'е: добавляет checkbox-колонку + select-all-checkbox в thead + sticky-floating bulk-bar (top-2 z-10) поверх таблицы, появляется при `selected.size > 0` с status-select + reason input + Apply + Clear; `confirm` через `window.confirm` с ICU-plural'ом «# заказа», submit → fetch → `toast.success(successAll | successPartial)` → `router.refresh()`. **i18n** +15 ключей `admin.orders.{list.{fromDate, toDate, apply, reset}, bulk.{selected/selectAll/selectRow/reasonPlaceholder/apply/applying/clear/confirm/successAll/successPartial/error}}` × 3 локали — итого **872 keys × 3** (+15 от follow-up'а). **Tests**: **+11 unit** в `admin-orders.test.ts`: date-range parsing × 4 (ISO start/end UTC, from > to → both null, garbage → null, only-from → from set), `OrderBulkStatusSchema` × 7 (1 id happy, ids+status+reason happy, empty array fail, > 200 fail, unknown status fail, strict extra fail, reason > 500 fail) — теперь 55/55 в этом файле. **+9 e2e** в [`admin-orders-followup.spec.ts`](apps/web/e2e/admin-orders-followup.spec.ts) — (a)+(b) PDF на ru/uz/en → magic bytes + buf > 50KB (Roboto embedded → большой PDF, был < 5KB на Helvetica); (d) `?from=YYYY-MM-DD&to=YYYY-MM-DD` фильтрует только сегодняшние заказы (старый 30-day-old order скрыт), `from > to` → оба обнуляются (sanitize); (c) bulk pending→confirmed для 2 happy → `updated=2 + skipped=[]` + БД verified, mixed-batch с delivered → `illegal_transition` в skipped (delivered остался в delivered), bulk с несуществующим id → `not_found` в skipped, empty ids → 400, anon → 401, UI smoke (bulk-bar появляется при checkbox-click). **CI**: typecheck ✅, lint ✅, format ✅, i18n parity 872×3 ✅, **unit 375/375 web** (+11 от follow-up'а), e2e P6-T5 follow-up локально 9/9. **Roboto licensing**: Apache 2.0, OFL-совместима для коммерческого embed'а в PDF. Размер контракта: PDF теперь весит ~250KB вместо ~3KB (TTF-шрифт embedded), но кириллица отрисуется на любом viewer'е — критично для UZ-purchaser'ов. Helvetica осталась как fallback в TTF metadata (если кто-то откроет PDF без поддержки embed'ed fonts — встречается на старых принтерах).

- [x] **P6-T4** — CRUD категорий, брендов, филиалов (F13/§8 master-prompt). Расширяет admin-панель тремя ресурсными CRUD: **Categories** (multilingual + tree через `parentId`), **Brands** (single-name `name` без локалей), **StoreBranches** (multilingual name+address + geo lat/lng). **Shared module** [`apps/web/src/server/admin-taxonomy.ts`](apps/web/src/server/admin-taxonomy.ts) — единый источник Zod-схем + server-fetch'ей: `CategoryCreate/UpdateSchema` (slug regex, parentId nullable, iconUrl URL-validate, order 0..9999), `BrandCreate/UpdateSchema` (name 2..120, slug, logoUrl URL, country/description optional), `BranchCreate/UpdateSchema` (3 локали name + 3 локали address, latitude -90..90, longitude -180..180, lat/lng/phone/workingHours все nullable). Все три имеют `Update = Base.partial()` (PATCH-семантика). `getAdminCategories/Brands/Branches` — list-query с product-count, parent-name (для категорий), `hasGeo` flag (для филиалов). `getAdminCategory/Brand/Branch` — детальные fetch'и. `getCategoryParentChoices(excludeId)` для drop-down (исключает текущий id). **9 API endpoints** (все защищены `requireAdminSession` из P6-T3): `POST/PATCH/DELETE /api/admin/{categories,brands,branches}[/[id]]`. Особенности по resource'ам: **Categories** — DELETE с pre-check `_count.products > 0` → 409 `category_in_use` + ICU-plural count (Category → Product Restrict); PATCH с `parentId === self` → 400 `parent_cycle` (защита от циклов в иерархии); slug-unique → 409. **Brands** — DELETE безопасен (Brand → Product onDelete:SetNull, products остаются с brandId=null); slug-unique. **Branches** — DELETE безопасен (Order → StoreBranch onDelete:SetNull, pickup-orders остаются с branchId=null); 400 на geo-out-of-range. Везде `prismaErrorCode(err)` duck-typed (P6-T3 webpack-границы). **UI**: 3 list-pages [`/admin/categories`](apps/web/src/app/[locale]/admin/categories/page.tsx), [`/admin/brands`](apps/web/src/app/[locale]/admin/brands/page.tsx), [`/admin/branches`](apps/web/src/app/[locale]/admin/branches/page.tsx) — табличный SSR с status-pills, breadcrumbs, link на detail. 3 формы как client-component'ы: [`<CategoryForm>`](apps/web/src/components/admin/taxonomy/category-form.tsx) (Tabs ru/uz/en для name + parent select из `getCategoryParentChoices` + slug + iconUrl + order + isActive), [`<BrandForm>`](apps/web/src/components/admin/taxonomy/brand-form.tsx) (single name + slug + logoUrl + country + description, без табов), [`<BranchForm>`](apps/web/src/components/admin/taxonomy/branch-form.tsx) (Tabs ru/uz/en c name + address textarea + общие phone/workingHours/lat/lng/isActive). Все формы имеют HTML5-валидацию (`pattern`/`min`/`max`/`required`), inline error-mapping через `admin.taxonomyErrors.*`, success → toast + router.push (create) / router.refresh (edit), DELETE с `window.confirm`. **AdminNav** обновлён: убраны `wip` флаги для categories/brands/branches (стрелки больше не amber-dot). **i18n** — расширены три namespace'а `admin.{categories, brands, branches}.{list, form}` × 3 локали + общий `admin.taxonomyErrors` (20 ключей с ICU-plural на `category_in_use`) — итого **774 keys × 3** (+106 от P6-T4). **Tests**: **+25 unit** в [`admin-taxonomy.test.ts`](apps/web/src/server/admin-taxonomy.test.ts) — все три Create/Update схемы по 7-9 кейсов: happy + boundary errors (slug-format, name-too-short × 3 локали, address × 3, geo out-of-range, refines, partial PATCH-edge cases). **+12 e2e** в [`admin-taxonomy.spec.ts`](apps/web/e2e/admin-taxonomy.spec.ts) — Categories (POST happy, slug-409, parent-cycle 400, category_in_use 409 с seeded product, list page); Brands (POST/PATCH happy, DELETE → product.brandId=null verified); Branches (POST с geo, geo-out-of-range 400, DELETE → order.branchId=null verified); auth (anon → 401 на все 3 ресурса). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 774×3 ✅, **unit 579/579** (+25; **320 web**), e2e P6-T4 локально 12/12. **Открытые вопросы**: нет — все три закрыты follow-up'ом. **Закрыто**: (a) **drag-and-drop reorder** через `@dnd-kit/core + sortable + utilities` (добавлены deps в `apps/web`): корневой уровень категорий имеет `<GripVertical>` handle; на drop вычисляется новый массив через `arrayMove`, POST `/api/admin/categories/reorder` атомарно обновляет `order` всех категорий через `prisma.$transaction([...updates])` — если хоть один id не найден (P2025), весь tx откатывается + 404 (правильно: drag-drop должен либо применить всю перестановку, либо ничего). На rollback в UI восстанавливается старый порядок + toast.error. Дочерние уровни пока только визуально nested, drag работает на одном уровне (корневые); глубокий drag-between-levels — P8 если admin-ы будут реструктурить иерархию часто. (b) **Hierarchical tree-view** — рекурсивный рендер по `parentId` через `buildForest()` (плоский список → дерево с `depth`-полем). Дочерние строки отрисовываются с `padding-left: depth * 1.25rem` + префикс `└` для визуального tree-эффекта. Server-fetch `getAdminCategories` уже возвращает все категории; tree-структура строится client-side, чтобы не дробить запросы. **Data-attributes** (`data-id`, `data-depth`) для e2e-стабильности. (c) **Bulk-действия** — checkbox в каждой строке + bulk-bar над таблицей, появляется при `selected.size > 0`. Кнопки «Скрыть выбранные» / «Активировать выбранные» + Clear; POST `/api/admin/categories/bulk` со схемой `{ids: string[1..200], action: "deactivate" | "activate"}` через `prisma.updateMany` (single SQL UPDATE). Confirm через `window.confirm`. ICU-plural'ы для «# выбрана/выбраны/выбрано» и «# изменена/изменены/изменено». **Component** [`<CategoryTree>`](apps/web/src/components/admin/taxonomy/category-tree.tsx) объединяет всё трое: Sortable корни + рекурсивный children + checkbox bulk-select. Заменил flat table в [list-page](apps/web/src/app/[locale]/admin/categories/page.tsx). **i18n** +10 ключей `admin.categories.bulk.*` × 3 локали → **784 keys × 3** (+10). **+9 e2e** в [`admin-categories-tree.spec.ts`](apps/web/e2e/admin-categories-tree.spec.ts) — tree-depth (parent + child render с правильными `data-depth`), bulk-deactivate happy (БД flip), bulk пустой ids → 400, bulk неизвестный action → 400, bulk-activate из false→true, reorder happy (order swap в БД), reorder с несуществующим id → 404 + tx rollback (исходный order сохранён), UI smoke (bulk-bar появляется при checkbox-click), anon → 401 на оба endpoint'а. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 784×3 ✅, **unit 579/579** (без изменений), e2e P6-T4 follow-up локально 9/9.

- [x] **P6-T3** — CRUD товаров с табами ru/uz/en + CSV-импорт (F13/§8 master-prompt). **Server-helpers + Zod-схемы** [`apps/web/src/server/admin-products.ts`](apps/web/src/server/admin-products.ts): `parseAdminProductListQuery` (sanitize `?q=` trim+slice 200, `?active=true|false`, page sanitize), `ProductCreateSchema` (multilingual обязательны min 2 / max 200, slug regex `^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$` + min 2 max 128, age 0..240 + cross-field refine `from ≤ to`, gender enum), `ProductUpdateSchema = ProductBaseSchema.partial().refine(...)` (PATCH-семантика — пустой объект ok), `ProductCsvRowSchema` (5 обязательных + опц. brand/desc/age/gender), `parseCsv(text)` (newline-split, header-validate с required `slug,name_ru,name_uz,name_en,category_slug`, CRLF норм, пустые ячейки игнор). **Server-fetch**: `getAdminProducts(query)` (Prisma findMany с ILIKE `%q%`, `_count.variants`, `category/brand` join, parallel count, page-size 20), `getAdminProduct(id)` (full Product + variants summary), `getAdminProductDictionaries` (active categories + all brands для drop-down'ов). **Auth-helper** [`requireAdminSession`](apps/web/src/server/admin-auth.ts) — общий gate для всех `/api/admin/*` endpoint'ов: 401 анону, 404 (не 403) customer'у. **API endpoints** все 4: [`POST /api/admin/products`](apps/web/src/app/api/admin/products/route.ts) (create → 201 + slug-unique → 409, FK-fail → 400), [`PATCH/DELETE /api/admin/products/[id]`](apps/web/src/app/api/admin/products/[id]/route.ts) (PATCH с findUnique-404-check ДО update; DELETE → soft-delete `isActive=false`, hard-delete отложен в P8 — OrderItems по FK через variants держат references), [`POST /api/admin/products/import`](apps/web/src/app/api/admin/products/import/route.ts) (text/csv, parseCsv → header-validate → row-by-row Zod → resolve category/brand by slug → upsert by slug; per-row failures накапливаются в `skipped[]`, остальные импортятся; max 1000 rows). **Важный fix**: Prisma `instanceof` проверки (`err instanceof Prisma.PrismaClientKnownRequestError`) НЕ работают в Next.js dev-server из-за webpack-дублирования модулей через rsc/server-границы — везде заменено на duck-typed `err.code` check. **UI**: [list page](apps/web/src/app/[locale]/admin/products/page.tsx) с table (name + slug font-mono / category / brand / variants count / status badge `bg-emerald` или `bg-slate` + opt `★ В подборке` amber pill), фильтр-форма [`<ProductsListFilters>`](apps/web/src/components/admin/products/products-list-filters.tsx) (`useTransition` + querystring push, search submit + active-select onchange), `<AdminBreadcrumbs items=[{labelKey:"products"}]>`, pagination prev/next; [detail page](apps/web/src/app/[locale]/admin/products/[id]/page.tsx) с `<ProductForm mode="edit">` + variants summary section; [create page](apps/web/src/app/[locale]/admin/products/new/page.tsx) с `<ProductForm mode="create">`; [import page](apps/web/src/app/[locale]/admin/products/import/page.tsx) с `<ProductImportForm>`. **`<ProductForm>`** ([client component](apps/web/src/components/admin/products/product-form.tsx)) — главная UI-фича: shadcn `<Tabs defaultValue="ru">` с RU/UZ/EN секциями для name + description (admin переключается между локалями вкладками, не скроллит); общие поля (category/brand selects, slug input с `pattern` HTML5-валидацией + lowercase enforce + hint, age-from/to, gender select, isActive/isFeatured checkboxes) — снаружи табов. На submit POST/PATCH с `Number.parseInt(age, 10)` или null; на 200/201 → `toast.success` + `router.push` (create) / `router.refresh` (edit); deactivate-кнопка с `window.confirm` + DELETE; ошибки маппятся в i18n через `errors.{reason}`. **`<ProductImportForm>`** — textarea + file-upload (читаем как text), POST `text/csv`, inline-summary `{created, updated, skipped[]}` с collapsible details. **i18n** — новый namespace `admin.products.{list, form (tabs/fields/errors/submit/deactivate), detail (back/createdAt/variants), create, import (summary/skipReasons/errors)}` × 3 локали — итого **624 keys × 3** (+85 от P6-T3, гигантский namespace покрывает все 16 form-fields × 3 локали + 12 error-keys × 3 + import skip-reasons). **Tests**: **+38 unit** в [`admin-products.test.ts`](apps/web/src/server/admin-products.test.ts) — `parseAdminProductListQuery` × 8 (q trim/slice/empty, active true/false/garbage, page sanitize × 4), `ProductCreateSchema` × 11 (happy + brand-optional + name-too-short × 4 langs + slug regex × 3 cases + age cross-field + equal-ages + one-null + gender enum + default), `ProductUpdateSchema` × 5 (empty PATCH ok, partial × 2, refine на partial), `parseCsv` × 6 (empty/header-only/missing-cols/happy/CRLF/empty-cells), `ProductCsvRowSchema` × 5 (minimal/coerce/range/gender/optional). **+15 e2e** в [`admin-products.spec.ts`](apps/web/e2e/admin-products.spec.ts) — auth (anon 401, customer 404, list page, search ?q=Nuby, ?active=false), CRUD (create flow form→POST→detail с локалезависимыми табами, edit nameRu→PATCH→header refresh, soft-delete API, slug conflict 409, PATCH 404), CSV import (happy 2-rows, unknown*category skipped, missing_columns 400, upsert по slug, anon 401). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 624×3 ✅, **unit 535/535** (+38; 121 payments + 38 worker + 78 shared-types + 22 notifications + **276 web**), e2e P6-T3 локально **15/15**. **Открытые вопросы**: только (b)/(c)/(d) — отложены или просятся в самостоятельный slot. **Закрыто follow-up'ом**: (a) **variants management** — полноценный CRUD `ProductVariant` с inline-таблицей в детальной странице. **Schemas** [`apps/web/src/server/admin-variants.ts`](apps/web/src/server/admin-variants.ts): `VariantCreateSchema` (sku regex `^[A-Z0-9](?:[A-Z0-9*-]\*[A-Z0-9])?$`UPPERCASE, color/size/barcode optional+null, priceCents integer 0..1e9, oldPriceCents > priceCents через cross-field refine, weightGrams 0..100k);`VariantUpdateSchema = partial().refine(...)`— refine на oldPrice применяется только когда`priceCents` присутствует в payload'е (PATCH без price-поля проходит). **API**: [`POST /api/admin/products/[id]/variants`](apps/web/src/app/api/admin/products/[id]/variants/route.ts) — 201 + sku-unique → 409 `sku_exists`, productId-not-found → 404; [`PATCH/DELETE /api/admin/products/[id]/variants/[variantId]`](apps/web/src/app/api/admin/products/[id]/variants/[variantId]/route.ts) — owner-check `productId`match, DELETE с **pre-check**`\_count.orderItems > 0`→ 409`variant_in_use`+`ordersCount`(history-preserving: ProductVariant ↔ OrderItem без cascade'а, hard-delete упал бы P2003; pre-check даёт чёткую причину vs гонка). Везде duck-typed`err.code`вместо`instanceof Prisma.X`. **UI** [`<VariantsManager>`](apps/web/src/components/admin/products/variants-manager.tsx) — `"use client"`inline-таблица 8 колонок (SKU font-mono / color / size / price`formatCurrencyUzs` / oldPrice line-through / barcode / weight / actions), Edit + Delete-кнопки на каждой строке. Add new + Edit открывают один и тот же [`<VariantDialog>`](apps/web/src/components/admin/products/variants-manager.tsx) (Radix `<Dialog>`) с 7 полями: SKU input с HTML5 `pattern`+ uppercase enforce + hint, color/size/barcode inputs, priceCents/oldPriceCents/weightGrams`type="number"`с min/max. Submit POST/PATCH с`Number.parseInt(value, 10)`или null для пустых; на 200/201 →`toast`+`router.refresh()`+`onClose()`; ошибки маппятся в `errors.{reason}`. Delete-кнопка с `window.confirm`+ DELETE — на 409`variant_in_use`показывает`toast.error`с ICU-plural count'ом заказов. **i18n** — новый namespace`admin.products.variants.{title, addNew, table.{8 cols}, form.{titles, hints, save/saving/create/delete/cancel + 19 errors c plural variant_in_use + 3 success}, empty, edit, noColor, noSize}`× 3 локали — итого **668 keys × 3** (+44 от follow-up; ICU plural в`variant_in_use`под русские формы 1/2/5 заказов). **Server-fetch**`getAdminProduct`расширен: variants теперь возвращают полный набор полей (priceCents, oldPriceCents, barcode, weightGrams) — экспортирован тип`AdminVariant`. Old read-only `<section variants>`в detail-page удалён, заменён на`<VariantsManager>`. **Tests**: **+19 unit** в [`admin-variants.test.ts`](apps/web/src/server/admin-variants.test.ts) — happy path × 12 (full + minimum + sku regex × 4 cases + price negative/float + oldPrice cross-field × 4 + null-clearing + weight neg); UpdateSchema partial × 6 (empty PATCH, single-field, refine-skip-when-no-price, oldPrice clear, sku-invalid). **+10 e2e** в [`admin-variants.spec.ts`](apps/web/e2e/admin-variants.spec.ts) — POST happy + 404-product + 409 sku-dup + 400 oldPrice-invalid; PATCH happy + 404 owner-mismatch; DELETE happy + 409 variant_in_use (со seeded OrderItem); UI smoke (open dialog → fill → submit → variant в таблице); anon 401. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 668×3 ✅, **unit 554/554** (+19; **295 web**), e2e variants 10/10. Остальные открытые вопросы: (b) **image upload в S3/MinIO** — отдельный slot P6-T3.6 (нужен AWS SDK + presigned URLs + image-resize + ProductImage CRUD UI; admin пока пастит готовые URL'ы как в seed); (c) hard-delete с cascade-планом OrderItems — P8 после анализа foreign keys; (d) CSV-quoted fields с запятыми — P8 (TSV или papaparse).

- [x] **P6-T2** — Dashboard со счётчиками заказов / выручки / топ-товаров / остатков / очереди pending Uniteller (F13/§8 master-prompt). Заменяет stub-landing из P6-T1. **Pure helpers** [`apps/web/src/server/admin-dashboard.ts`](apps/web/src/server/admin-dashboard.ts): `dashboardRanges(now)` строит UTC-границы today/week/month (полночь UTC + субтракция через ms — устойчиво к DST, leap-year, year-rollover). Константы: `REVENUE_STATUSES = ["confirmed","packing","shipped","delivered"]` (только paid funnel — pending/cancelled/refunded не дают revenue), `LOW_STOCK_THRESHOLD = 5`, `PENDING_UNITELLER_MIN_AGE_SEC = 120`. **Server-fetch** `getDashboardStats(now)` бьёт 7 параллельных Prisma-запросов через `Promise.all`: `order.groupBy({by:[status], _count})`, `order.aggregate({_sum:totalCents, _count})` × 3 ranges (today/week/month), `orderItem.groupBy({by:[variantId], _sum:{quantity, priceCents}, where:{order:{status:in PAID, createdAt: gte month}}, orderBy:{_sum:{quantity:desc}}, take:5})` для топа, `stock.findMany({where:{quantity:{lt:5}}, include:{branch, variant→product}, take:10, orderBy:quantity asc})`, `payment.findMany({where:{provider:uniteller, status:pending, createdAt:{lt: now-2min}}, include:{order:{number}}, take:20})`. Top-products подтягиваются через дополнительный `productVariant.findMany({where:{id:in topVariantIds}})` для product-snapshot — без N+1 (одна batch). **SSR-страница** [`/admin/page.tsx`](apps/web/src/app/[locale]/admin/page.tsx) с `export const dynamic = "force-dynamic"` (свежие цифры на каждый refresh) и пятью inline server-секциями: KPI-cards × 4 (`<ShoppingBag>` всего, `<CircleDollarSign>` today, `<TrendingUp>` × 2 week/month с ICU plural `ordersCount`); orders-by-status (left column lg) рендерит `<OrderStatusBadge>` (P5-T1) + counter; top-products (right column lg) — нумерованный список с `<Package>` иконкой + qty + revenue (приближённый qty × \_sum.priceCents — strict per-line было бы raw-SQL, fixme); low-stock с `<AlertTriangle>` amber + tone-mapped pill (rose для `quantity===0`, amber иначе); pending-Uniteller queue с `<Clock>` blue + age-formatted (sec/min ICU plural) + Link «Открыть» на `/admin/orders/{id}` (роут пока 404 до P6-T5, но тестовый user может проверить контракт ссылки). Все секции имеют `data-testid` (`dashboard-kpi`, `dashboard-orders-by-status`, `dashboard-top-products`, `dashboard-low-stock` или `dashboard-low-stock-empty`, `dashboard-pending-uniteller`) + строки помечены `data-status`/`data-product-slug`/`data-variant-id`/`data-payment-id` для e2e-стабильности. `formatCurrencyUzs(cents, locale)` для денег, `Intl.DateTimeFormat` для времени `generatedAt`. **i18n** — новый namespace `admin.dashboard.{title, subtitle, kpi.{ordersTotal, revenueToday/Week/Month, ordersCount-plural}, ordersByStatus.{title,empty}, topProducts.{title,empty,qty-plural}, lowStock.{title,empty,atBranch,remaining-plural}, pendingUniteller.{title,subtitle,empty,ageSec-plural,ageMin-plural,openOrder}, generatedAt}` × 3 локали; obsolete `admin.landing.*` удалён. **539 keys × 3** (+17 от P6-T2; ICU plural'ы для ordersCount/qty/remaining/ageSec/ageMin — корректные русские формы «1 заказ / 2 заказа / 5 заказов»). **Tests**: **+13 unit** в [`admin-dashboard.test.ts`](apps/web/src/server/admin-dashboard.test.ts) — `dashboardRanges` × 9 (UTC-полночь today, week=−7d, month=−30d, just-after-midnight, just-before, march-1 → january, leap-year 29-feb, jan-2 → dec prev-year, всё в 00:00:00 UTC); `REVENUE_STATUSES` × 2 (включает 4 paid + не включает 3 не-paid); constants × 2 (LOW_STOCK ≥1, PENDING_AGE ≥60). **+5 e2e** в [`admin-dashboard.spec.ts`](apps/web/e2e/admin-dashboard.spec.ts) — admin → 5 секций видны, KPI ordersTotal содержит цифру, seeded stale-pending Uniteller (5min age) → виден в queue с правильным orderNumber + `data-payment-id` + «5 мин» age, seeded delivered → счётчик в by-status обновлён, customer на /admin → middleware silent-redirect (regression P6-T1). **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 539×3 ✅, **unit 497/497** (+13; 121 payments + 38 worker + 78 shared-types + 22 notifications + **238 web**), e2e P6-T2 локально 5/5. **Открытые вопросы**: (a) revenue-per-top-product — приблизительный (qty × avg-priceCents), точный bы потребовал raw-SQL `sum(qty*price)` — fixme в P8-T1 при load-test; (b) auto-refresh dashboard — пока force-dynamic + ручной reload, real-time через SSE/poll отложен до P8 (admin обычно открывает «один раз», не watch-screen); (c) фильтр по filial для KPI/orders-by-status (multi-tenant view) — appears когда у Бигмаха станет >2 складов с разными менеджерами, P6-T7 stock management возможно подключит.

- [x] **P6-T1** — Role-gated `/admin` (F13/§8 master-prompt). **Layout** `/[locale]/admin/layout.tsx` ([layout](apps/web/src/app/[locale]/admin/layout.tsx)) — defense-in-depth поверх middleware (P1-T5): `auth()` SSR-side читает session, `null` → `redirect("/auth/login")` (fallback к middleware), `role !∈ {admin, manager}` → `notFound()` (404 а не 403, не раскрываем существование чужой админки). `metadata: {robots: {index:false, follow:false}}`. Layout — 2-column grid (`lg:grid-cols-[15rem_1fr]`): aside с `<RoleBadge>` + user-name + `<AdminNav>`, main — `rounded-lg border bg-card p-6`. **Landing** `/admin` ([page](apps/web/src/app/[locale]/admin/page.tsx)) — stub: hero с `<Sparkles>` иконкой, body про задачи admin'а, чек-лист «что готово в P6-T1». Реальный dashboard со счётчиками заказов / выручки / pending-платежей приедет в P6-T2. **Components**: [`<AdminNav>`](apps/web/src/components/admin/admin-nav.tsx) — `"use client"` боковое меню с 10 разделами (`dashboard`, `orders`, `payments`, `products`, `categories`, `brands`, `branches`, `stock`, `promo`, `customers`) — все кроме dashboard помечены `wip: true` (amber-dot, ссылки рендерятся но ведут в 404 пока соответствующие P6-T2..T8 не реализованы). Active-подсветка через `usePathname()` + `stripLocale()` regex (`/ru/admin/orders` → `/admin/orders`); `data-active` атрибут для e2e-стабильности. Locale-aware Link через `@bigmax/i18n/navigation`. Lucide иконки на каждый раздел. [`<RoleBadge>`](apps/web/src/components/admin/role-badge.tsx) — pure server component, tone-mapping (admin = rose, manager = indigo) + i18n labels. **Header** ([header.tsx](apps/web/src/components/header.tsx)) расширен: privileged юзер видит rose-цветную ссылку «Админ-панель» в desktop nav (между «Каталог» и user-name); customer и аноним не видят. **Middleware** не трогаем — он уже корректно обрабатывает `/admin/*` (P1-T5): аноним → `/auth/login`, customer → `/{locale}`, privileged + visit `/auth/login` → `/admin`. Layout — это второй слой защиты на случай middleware-bypass'а (баг, edge-cache stale). **i18n** — новый top-level namespace `admin.{title, subtitle, roles.{admin, manager}, nav.{10 keys + backToShop}, landing.{title, body, wip.{title, item1..3}}}` × 3 локали — итого **520 keys × 3** (+21 от P6-T1; ICU `{role}` template для subtitle, named keys `item1..3` вместо array — next-intl не поддерживает массивы как сообщения). **Tests**: **+8 e2e** в [`admin-access.spec.ts`](apps/web/e2e/admin-access.spec.ts) — аноним → /auth/login redirect, customer → /{locale} silent (не 403, admin-shell не рендерится), admin → 200 + shell + landing + nav + role-badge=admin, manager → 200 + role-badge=manager + «Менеджер» текст, повторный визит /auth/login для admin → middleware §1 ведёт на /admin, header «Админ-панель» link виден privileged + клик → /admin, header link НЕ виден customer'у, admin-nav-dashboard `data-active=true` на /admin а orders/wip-разделы `data-active=false`. Pure-helper unit-тесты не нужны — `<AdminNav>` это чистый рендер по NAV-массиву, `stripLocale` тривиален. **CI**: typecheck ✅ (10 проектов), lint ✅, format ✅, i18n parity 520×3 ✅, **unit 484/484** (без изменений — UI-only задача), e2e regression P6-T1 локально 8/8. **Открытые вопросы**: только (a) — WIP-разделы (orders/payments/products/...) ведут в 404 до своих P6-T\*. **Закрыто follow-up'ом**: (b) mobile-nav drawer — [`<AdminMobileNav>`](apps/web/src/components/admin/admin-mobile-nav.tsx) `"use client"` обёртка над shadcn `<Sheet>` с гамбургер-кнопкой (видна `lg:hidden`); drawer закрывается автоматически на смене `pathname` через `useEffect` (без него после тапа по nav-link sheet остался бы поверх перешедшей страницы). Layout перестроен: mobile-header (hamburger + role-badge + name) и desktop-aside (`hidden lg:block`) живут в разных секциях, рендерятся одновременно но видны через CSS-breakpoint. (c) **`<AdminBreadcrumbs items={...}>`** — [reusable SSR-компонент](apps/web/src/components/admin/admin-breadcrumbs.tsx) для P6-T2..T8: первый сегмент «Админ-панель» → `/admin` добавляется автоматически, последний item рендерится `font-medium` без link'а (current page); поддерживает `labelKey` (из `admin.nav.*` namespace) или произвольный `label` (например, номер заказа). `<ChevronRight>` separator. **+2 i18n** (`admin.menuButton`, `admin.breadcrumbs.home` × 3 → **522 keys × 3**), **+4 e2e** в [`admin-mobile-nav.spec.ts`](apps/web/e2e/admin-mobile-nav.spec.ts): mobile viewport (390×800) → hamburger visible / desktop-aside hidden, клик гамбургера → sheet открыт + nav видно + dashboard-active внутри, клик link'а → pathname сменился + sheet закрылся, desktop viewport (1280×800) → гамбургер hidden / desktop-aside visible. Existing admin-access.spec обновлён: badge-assertion'ы scope'нут к `aside.hidden.lg\\:block` (теперь рендерится 2 копии RoleBadge — mobile-header и desktop-aside, обе всегда в DOM). E2e P6-T1 + follow-up локально **12/12**.

### Фаза 7 — Промо, лояльность, отзывы (F16, F17, F18) 🔧 в работе

- [x] **P7-T1** — CRUD промокодов + применение в корзине. **Schema** — миграция `20260511120000_order_promo_code` добавляет `Order.promoCode VARCHAR(64)` (denormalized snapshot, не FK; index под аналитику «orders by promo»). **Server lib** [`server/admin-promo.ts`](apps/web/src/server/admin-promo.ts) — `PromoCreateSchema` / `PromoUpdateSchema` (strict; cross-field refines: `percent_out_of_range`, `ends_before_starts`, `value_required_when_type_changes`); UPPER-нормализация `code` через `.transform(s => s.toUpperCase())`; `parseAdminPromoListQuery` (q / active / sort / page); `getAdminPromos` + `getAdminPromoById`; **`incrementPromoUsage(tx, code)`** — silent no-op на `null` / пустой строке, иначе `updateMany({ code: UPPER }, { usedCount: { increment: 1 } })`. **API** `POST /api/admin/promo` + `PATCH/DELETE /api/admin/promo/[id]` — `P2002 → code_exists` (409), `not_found` (404), `invalid_body` (400). **UI** `/admin/promo` (список с типом / скидкой / мин.заказом / usage / статусом, кликабельный код → edit), `/admin/promo/new`, `/admin/promo/[id]` + общий [`<PromoForm>`](apps/web/src/components/admin/promo/promo-form.tsx) — `value`-семантика и `valueLabel` меняются по `type` (percent/fixed/free_delivery), `code` auto-UPPER на blur, `datetime-local` с правильной UTC-конверсией, `usageLimit` пусто = unlimited. **Закрытие «фиктивного `usageLimit`»**: `checkout/pay` пишет UPPER snapshot в `Order.promoCode`; Uniteller webhook в той же атомарной `$transaction([...])` делает `prisma.promo.updateMany` только на первом `pending → captured` переходе (idempotent через `isFirstCapture`). Снят `wip: true` с пункта promo в `<AdminNav>`. **i18n** — новый namespace `admin.promo.{list, new, edit, types, status, form.fields, form.valueLabel.{percent,fixed,free_delivery}, form.valueHint.{...}, errors}` × ru/en/uz (~40 ключей × 3). **Tests** [`admin-promo.test.ts`](apps/web/src/server/admin-promo.test.ts) — **25** unit (querystring-парсер, обе Zod-схема с edge-cases, `incrementPromoUsage` с fake-tx: `null` / `undefined` / пустая строка → no-op, иначе UPPER+trim+`{ increment: 1 }`). Sub-задачи из «Открытых вопросов»:
  - [x] **A · COD-капчура `usedCount`**: `POST /api/admin/orders/[id]/status` теперь при переходе `shipped → delivered` ищет COD `Payment(status="pending")` для заказа и в атомарной `$transaction([...])` (a) переводит платёж в `captured` + проставляет `capturedAt`, (b) если у заказа есть `promoCode` — бампает `Promo.usedCount`. Idempotency через фильтр `provider: "cod", status: "pending"` — повторный delivered-transition не сработает (state-machine это и так не разрешит, но безопаснее). Аудит в `PaymentLog(action: "cod.captured_on_delivery")` с маскированием. Закрыта ассиметрия между онлайн и COD: лимит использований теперь работает для обоих способов оплаты.
  - [x] **B · Redis-cache промо (OPT-010, Wave 3 / item 12)**: новый helper `getCachedPromoByCode(code)` в [`server/promo.ts`](apps/web/src/server/promo.ts) — Redis ключ `promo:v1:<UPPER>` с **60 s TTL** (`SET … EX 60`); пишет JSON-snapshot с ISO-сериализацией дат; на miss → `prisma.promo.findUnique({ where: { code } })` → write-back. Cache-aside, не write-through (на mutation only invalidate, не refill). `invalidatePromoCacheByCode(code)` зовётся из admin POST (новый code), PATCH (старый + новый если код менялся), DELETE (удалённый code). `validatePromoCode` и `GET /api/promo/validate` теперь оба ходят через cache. Стандартизация: оба пути используют **case-exact `findUnique({ code: UPPER })`** (раньше route был `findUnique` case-exact, server-lib — `findFirst({ mode: "insensitive" })`; снято расхождение). Cache-misses безопасны при недоступном Redis — try/catch на каждом cache-touch, fallback к Prisma. **Negative caching выключен**: на DB-`null` в кэш не пишем, иначе после `DELETE → POST` нового промо с тем же кодом окно до TTL отдавало бы 404. **Tests** [`promo-cache.test.ts`](apps/web/src/server/promo-cache.test.ts) — **9** unit с fake-Redis: empty/whitespace → no-op; cache hit → дата-десериализация, БД не дёргается; cache miss → DB-hit → write `EX 60`; DB-null → нет cache-write; `Redis.get` throws → fallback к Prisma; `Redis.set` throws → результат всё равно возвращается; UPPER+trim ключа; `invalidatePromoCacheByCode` шлёт `DEL` с UPPER ключом и swallow'ит ошибку Redis.
  - [x] **C · Диагностика orphan-promo заказов**: read-only скрипт [`scripts/diagnose-orphan-promo-orders.ts`](scripts/diagnose-orphan-promo-orders.ts), вызывается через `pnpm diagnose:orphan-promo` (+ опциональный `--sample=20`). Считает `Order.where({ discountCents > 0, promoCode IS NULL })` — это заказы с применённым до P7-T1 промо, у которых не сохранён snapshot кода, поэтому на их `delivered`-переходе `incrementPromoUsage` будет no-op. Скрипт **не пытается auto-attribute** — для этого нет source-of-truth, raw-body старых POST-запросов нигде не сохранялся. Печатает count + newest-N sample (id, number, status, discountCents, createdAt) на stderr; exit-code: `0` если чисто, `1` если найдены orphan-заказы (CI-friendly gate перед prod-миграцией), `2` на ошибке БД. Admin читает вывод и вручную решает, бампать ли `usedCount` для известных лимитированных промо. Новые (post-P7-T1) заказы все корректно tracked → no-op на go-forward.
  - [x] **D · Версионирование cache-key**: в [`server/promo.ts`](apps/web/src/server/promo.ts) добавлен `PROMO_CACHE_KEY_VERSION = "v1"` — теперь ключи имеют форму `promo:v1:<CODE>`. При расширении `PromoSnapshot` shape (новые поля в `select`-фильтре `validatePromoCode`) достаточно бампнуть версию → старые ключи естественно протухнут за 60 s, deserialize не вернёт stale-row без новых полей. Дополнительно экспортирован `PROMO_CACHE_GLOB = "promo:*"` (используется в e2e flush helper — см. sub-task E). Существующие 9 unit-тестов обновлены на `promo:v1:SUMMER10` (через `replace_all`). История версий ведётся в jsdoc над константой.
  - [x] **E · Redis flush helper для e2e**: новый [`apps/web/e2e/helpers/redis.ts`](apps/web/e2e/helpers/redis.ts) экспортирует `clearPromoCache()` и `clearByGlob(pattern)`. Использует `SCAN cursor MATCH glob COUNT 100` + `DEL …keys` циклически — не блокирует Redis в отличие от `KEYS` (важно при разрастании кэша). Любая ошибка глотается → недоступный Redis не валит `beforeEach`. Glob `promo:*` покрывает все версии — при будущем бампе `v1 → v2` старые ключи всё равно вычищаются. Использование: `import { clearPromoCache } from "./helpers/redis"; test.beforeEach(async () => { await clearPromoCache(); });`. Относительные импорты (`../../src/server/...`) вместо `@/`-alias — соответствует существующему паттерну `helpers/user.ts` (e2e не входит в `tsconfig.include`).

- [x] **P7-T2** — «Бигмах Бонус» (F16): начисление 1% от суммы заказа после `Payment.status=captured`, списание баллов при оплате (1 балл = 1 сум). **Schema** — миграция `20260511150000_order_loyalty_points_spent` добавляет `Order.loyaltyPointsSpent INT default 0` (snapshot, нужен для UI и будущих reverse-сценариев). `User.loyaltyPoints` и `LoyaltyTransaction(userId, orderId?, points, type[earn|spend])` уже были в P0-T3 schema. **Server lib** [`server/loyalty.ts`](apps/web/src/server/loyalty.ts) — pure: `computeEarnedPoints(totalCents, percent=1)` = `floor((cents × percent) / 100 / 100)`, `computeLoyaltyDiscountCents(points)` = `points × 100`, `clampPointsToSpend(requested, balance, totalCents)` (anti-tamper: `min(requested, balance, floor((totalCents-1)/100), 10_000_000)` — последний тийн оставляется, чтобы Uniteller не отверг 0-amount); atomic: `awardLoyaltyPoints(tx, …)` ставит `User.loyaltyPoints += N` + пишет `LoyaltyTransaction(earn, +N)`, `spendLoyaltyPoints(tx, …)` симметрично. **Spend в checkout/pay**: новое поле `pointsToSpend` в [`CheckoutPayRequestSchema`](packages/shared-types/src/checkout-pay.ts) (опциональное, server-side clamp по живому `User.loyaltyPoints`); фактическое списание — внутри той же `prisma.$transaction` что создаёт Order + Payment + reserveStock (atomicity: баланс не уйдёт в минус и заказ не будет создан без spend-записи). **Earn — две точки-перехвата** (паттерн от P7-T1 sub-task A): (i) Uniteller webhook на `isFirstCapture` — `user.update + loyaltyTransaction.create` inlined в существующий `$transaction([…])` array; (ii) `POST /api/admin/orders/[id]/status` на переходе `shipped → delivered` для COD — `awardLoyaltyPoints(tx, …)` внутри `$transaction(async (tx) => …)` рядом с `incrementPromoUsage` (P7-T1 A). Idempotency: webhook через `isFirstCapture` flag, COD через фильтр `provider:"cod" status:"pending"`. **UI checkout** — `CheckoutInitial.loyaltyBalance` подтягивает `User.loyaltyPoints` SSR-side; `useCheckout` store расширен `loyaltyPointsToSpend` (persist'ится в `bigmax:checkout-draft`); [`<StepReview>`](apps/web/src/components/checkout/step-review.tsx) рендерит `<LoyaltyBlock>` (input + «Списать всё» / «Не списывать»), [`<OrderSummary>`](apps/web/src/components/checkout/order-summary.tsx) рендерит дополнительную строку `appliedRow` с client-side previewом скидки. **UI account** — новая страница [/account/loyalty](apps/web/src/app/[locale]/account/loyalty/page.tsx) с балансом + paginated history (`?page=N`, 20/страницу); каждая строка показывает `earn`/`spend` иконкой и цветом + ссылку на заказ; ICU plurals для «N баллов»; SSR через [`server/account-loyalty.ts`](apps/web/src/server/account-loyalty.ts). Account-nav расширен пунктом «Бигмах Бонус». **i18n** — namespace `account.loyalty.{title, subtitle, balanceLabel, balanceValue, rulesHint, historyTitle, historyCount, empty, typeEarn, typeSpend, pagination.{prev,next,pageOf}, pageSizeHint}` + `account.nav.loyalty` + `checkout.loyalty.{title, balance, hint, inputLabel, applyMax, clear, appliedRow}` × ru/en/uz — **1740 keys × 3** (parity ✅). **Tests** [`loyalty.test.ts`](apps/web/src/server/loyalty.test.ts) — **24** unit: все три pure-helper'а (edge cases: 0/negative/non-integer requested, < 10_000 тийн → 0 баллов, clamp по balance/maxByTotal/LOYALTY_POINTS_MAX), `awardLoyaltyPoints` (0-point no-op, increment + earn-tx с правильным `data`), `spendLoyaltyPoints` (0/negative no-op, decrement + spend-tx с `-N points`). Sub-задачи из «Открытых вопросов»:
  - [x] **A · Reverse loyalty при cancel/refund**: миграция `20260511180000_loyalty_type_refund_clawback` расширяет `LoyaltyType` enum значениями `refund` (mirror of `spend`) и `clawback` (mirror of `earn`) — отдельные типы дают чистый audit-trail в /account/loyalty (раньше reversal-row был бы неотличим от обычного earn/spend). Новый helper `reverseLoyaltyForOrder(tx, orderId)` в [`server/loyalty.ts`](apps/web/src/server/loyalty.ts): читает все `LoyaltyTransaction.WHERE(orderId=X)`, считает `net = SUM(points)`, и если `net != 0` — пишет одну или две reversal-row (refund для spend-части, clawback для earn-части) + атомарный `User.loyaltyPoints += -net`. **Idempotent**: повторный вызов на уже-balanced заказ (`net=0`) → `alreadyReversed=true`, нет side-effects. Вызывается из (i) admin `POST /api/admin/orders/[id]/status` на переходах `→ cancelled / refunded` рядом с `releaseOrderStock`; (ii) user-side `POST /api/account/orders/[id]/cancel` — здесь callback-form `$transaction(async (tx) => …)` теперь обёртывает Order/Payment flip + `reverseLoyaltyForOrder` в одной TX. Аудит — `PaymentLog(action: "loyalty.reversed", response: { refunded, clawedBack })`. UI /account/loyalty рендерит refund (sky icon `Undo2`) и clawback (rose icon `RotateCcw`) с отдельными labels.
  - [x] **B · Min-order для spend**: новая константа `LOYALTY_MIN_ORDER_TO_SPEND_CENTS = 500_000` (5_000 сум) в [`server/loyalty.ts`](apps/web/src/server/loyalty.ts). `clampPointsToSpend(requested, balance, totalCents)` теперь возвращает `0` при `totalCents < LOYALTY_MIN_ORDER_TO_SPEND_CENTS` — защита от edge-cases типа «1 балл с заказа на 100 сум» (Uniteller примет, но UX странный и не покрывает комиссию). На границе включительно — разрешено.
  - [x] **C · LOYALTY_EARN_PERCENT через env**: `LOYALTY_EARN_PERCENT` теперь зачитывается из `process.env["LOYALTY_EARN_PERCENT"]` при module-load. Pure helper `parseEarnPercent(envValue)` парсит float, sanity-cap'ит на `[0.01..50]`, fallback'ит на `1` при `undefined`/невалидном/вне-диапазона. Документировано в [`.env.example`](.env.example) с комментарием о маркетинговых акциях («увеличенный кэшбэк на выходных»). i18n обновлён: `account.loyalty.{typeRefund, typeClawback}` × ru/en/uz — **1742 keys × 3** (parity ✅). Тесты теперь **36** (было 24, +12 на reversal/min-order/env-reader).
  - [x] **D · UI hint про min-order**: `<LoyaltyBlock>` в [`<StepReview>`](apps/web/src/components/checkout/step-review.tsx) теперь принимает `subtotalCents` и при `subtotal < LOYALTY_MIN_ORDER_TO_SPEND_CENTS` рендерит amber inline-hint «Списание баллов доступно от {min}. Добавьте товары в корзину» + блокирует input/«Списать всё». Если юзер удалил позиции с корзины через «Изменить» на review-шаге и subtotal упал ниже min'а — `useEffect` авто-чистит `loyaltyPointsToSpend` (иначе server-clamp вернёт 0 без явного UX-сигнала). Константа `LOYALTY_MIN_ORDER_TO_SPEND_CENTS` (вместе с `LOYALTY_POINT_VALUE_CENTS` и `LOYALTY_POINTS_MAX`) переехала в [`@bigmax/shared-types/src/loyalty.ts`](packages/shared-types/src/loyalty.ts) — client-safe, без Prisma-deps; `apps/web/src/server/loyalty.ts` re-export'ит для server-callers. Новый ключ `checkout.loyalty.belowMinHint` × ru/en/uz — **1743 keys × 3** (parity ✅).
  - [x] **E · Rollback runbook**: новый [`docs/runbooks/loyalty-type-rollback.md`](docs/runbooks/loyalty-type-rollback.md) описывает что делать если миграция `20260511180000_loyalty_type_refund_clawback` понадобится откатить. Postgres не поддерживает `ALTER TYPE ... DROP VALUE`, поэтому Prisma не генерирует down-миграцию автоматически — это ожидаемо, не баг. Два решения: (1) **forward-rollback** (рекомендуемый): свернуть `refund` / `clawback` rows обратно в `earn` / `spend` через UPDATE, оставив enum-values неиспользуемыми; (2) полное пересоздание enum через временный `LoyaltyType_old` + USING-cast (требует downtime + Prisma client regen). Runbook идёт под СПЕЦИФИКАЦИЕЙ §8 P8-T6 (runbook'и по инцидентам). Структура `docs/runbooks/` подготовлена к расширению будущими runbook'ами.
  - [x] **F · Hot-reload `LOYALTY_EARN_PERCENT` через features-таблицу**: миграция `20260511220000_features_table` создаёт `Feature(key VARCHAR(120) PK, value TEXT, type FeatureType, description?, updated_at)` + `FeatureType` enum (`number / boolean / string`) + seed row `loyalty.earn_percent = "1"` (StaticFlag — §6 правило 7). Новый helper [`server/features.ts`](apps/web/src/server/features.ts) — `getNumberFeature(key, fallback)` (read-through cache: `feature:v1:<key>` с 60s TTL, negative-cache marker для несуществующих ключей, fail-safe try/catch на Redis и Prisma), `invalidateFeatureCache(key)` (для будущего admin UI). `LOYALTY_EARN_PERCENT_FEATURE_KEY = "loyalty.earn_percent"` + async `getLoyaltyEarnPercent()` в [`server/loyalty.ts`](apps/web/src/server/loyalty.ts) — chain `features-row → env LOYALTY_EARN_PERCENT → hard-coded 1`. Wired в Uniteller webhook + admin orders status `delivered` (COD) — fetch percent перед/в начале TX, передаёт через `awardLoyaltyPoints({ ..., percent })`. **Tests** [`features.test.ts`](apps/web/src/server/features.test.ts) — **12** unit с fake-Redis + mocked Prisma: cache hit, versioned key, miss → DB → write-back EX 60, неправильный type → fallback с negative marker, DB null → fallback с marker, "" marker hit → fallback, garbage parse → fallback, Redis throws → fallback к Prisma, Prisma throws → silent fallback, `invalidateFeatureCache` DEL правильный ключ. **Admin override**: меняем row через прямой SQL `UPDATE features SET value='5' WHERE key='loyalty.earn_percent';` → новое значение подхватывается за ≤ 60s без рестарта. Admin UI — отложен до P8-T2; для v1 SQL-флоу достаточен. **Итого тестов loyalty/features**: 36 + 12 = **48 ✅**.
  - [x] **G · Admin UI для features** (`/admin/features`): list-страница ([page.tsx](apps/web/src/app/[locale]/admin/features/page.tsx)) и edit-by-key ([key/page.tsx](apps/web/src/app/[locale]/admin/features/[key]/page.tsx)) с типизированной формой [`<FeatureForm>`](apps/web/src/components/admin/features/feature-form.tsx) (input adapts по `type`: `number` → numeric input, `boolean` → radio `true`/`false`, `string` → text). API [`PATCH /api/admin/features/[key]`](apps/web/src/app/api/admin/features/[key]/route.ts) валидирует `value` против `type` через pure `validateFeatureValue` (discriminated union `invalid_number` / `invalid_boolean` / `empty_string`), обновляет row, **атомарно дёргает `invalidateFeatureCache(key)`** — новое значение видно сразу. POST/DELETE row'ы НЕ через UI — это invariant'ы кода, заводятся миграциями (защищает type-contract от случайного admin-ошибки). Раздел зарегистрирован в `<AdminNav>` (новый icon `Flag`) и `<AdminBreadcrumbs>` (`features` в `BreadcrumbLabelKey`).
  - [x] **H · `getBooleanFeature` / `getStringFeature`**: новые typed readers в [`server/features.ts`](apps/web/src/server/features.ts), оба построены поверх private `readFeatureValue(key, expectedType)` (extracted shared cache+DB logic). `getBooleanFeature` принимает ровно `"true"` / `"false"` (case-sensitive — защита от `1`/`yes`/`on`). `getStringFeature` возвращает raw value, пустая строка трактуется как negative (single-marker trade-off; admin form всё равно не даст создать `string`-row с `value=""` через `validateFeatureValue → empty_string`). +11 unit-тестов в `features.test.ts` (boolean: happy true/false, type-mismatch fallback, non-canonical → fallback, DB-null, cache hit; string: happy, type-mismatch, DB-null, cache hit, empty-string-as-fallback). +12 в новом [`admin-features.test.ts`](apps/web/src/server/admin-features.test.ts) (Zod schema + `validateFeatureValue` для всех 3 types с edge-cases).
  - [x] **I · Explicit invalidate-cache button** ([invalidate-cache-button.tsx](apps/web/src/components/admin/features/invalidate-cache-button.tsx)): client component кнопка в шапке edit-страницы, дергает [`POST /api/admin/features/[key]/invalidate`](apps/web/src/app/api/admin/features/[key]/invalidate/route.ts), который зовёт `invalidateFeatureCache(key)` без изменения row. Нужна для out-of-band updates (admin поменял row через прямой SQL UPDATE, минуя UI) или при подозрении на stale-cache. Toast «Кэш очищен. Новое значение подхватится сразу».

  **i18n** — namespace `admin.features.{list, edit, types.{number,boolean,string}, form.{valueLabel, hint, boolean, submit, submitting, cancel, updated, invalidate, invalidating, invalidated}, errors}` + `admin.nav.features` × ru/en/uz — **1778 keys × 3** (parity ✅, +35 ключей от sub-tasks G/H/I).
  - [x] **J · Playwright e2e**: новый [`apps/web/e2e/admin-features.spec.ts`](apps/web/e2e/admin-features.spec.ts) — **7** сценариев: anon → PATCH/invalidate → 401; admin list (seed + 2 test-rows на boolean/string); edit number с DB-проверкой; boolean radio-flow false→true; invalidate-cache button → toast; invalid number → server `invalid_number`; string edit. `beforeEach` seed'ит test-rows через `prisma.feature.upsert` + `clearByGlob("feature:*")` (Redis flush helper из P7-T1 sub-task E переиспользован); `afterEach` восстанавливает `loyalty.earn_percent = "1"` + удаляет test-rows + чистит cache.
  - [x] **K · Audit log для feature-операций**: PATCH-handler ([route.ts](apps/web/src/app/api/admin/features/[key]/route.ts)) пишет `writePaymentLog({ action: "feature.updated", request: { key, oldValue, newValue, adminId, adminEmail } })`. Invalidate-handler — `feature.cache_invalidated`. `PaymentLog` — унифицированный admin-audit store ([/admin/audit](apps/web/src/app/[locale]/admin/audit/page.tsx)), `paymentId: null` для row'ы без привязки к платежу. Группа `feature` добавлена в [`AUDIT_ACTION_GROUPS`](apps/web/src/server/admin-audit.ts) (prefix-match `feature.`) → admin может фильтровать audit timeline `?group=feature`. i18n `admin.audit.groups.feature` × ru/en/uz.
  - [x] **L · Первый real-world consumer `loyalty.spend_enabled`**: миграция `20260511230000_feature_loyalty_spend_enabled` seed'ит boolean row `loyalty.spend_enabled = "true"`. Новый `isLoyaltySpendEnabled()` в [`server/loyalty.ts`](apps/web/src/server/loyalty.ts) — обёртка над `getBooleanFeature(LOYALTY_SPEND_ENABLED_FEATURE_KEY, true)` (fail-open: cache недоступен → spend разрешён). Wired в [`checkout/pay`](apps/web/src/app/api/checkout/pay/route.ts): `if (request.pointsToSpend > 0 && (await isLoyaltySpendEnabled())) { … }` — **асимметричное отключение** (spend off, earn on). Use case: emergency-disable при fraud incident → admin переключает radio в `/admin/features/loyalty.spend_enabled` → за ≤ 60s spend выключается без deploy'а. Earn-flow (webhook + COD `delivered`) не зависит от flag'а. Это первый production-grade consumer `getBooleanFeature` — API больше не «только для будущего». +2 sanity-теста на ключи (`LOYALTY_*_FEATURE_KEY === "loyalty.*"`). **i18n**: +1 ключ `admin.audit.groups.feature` × 3 локали → **1779 keys × 3** (parity ✅).
  - [x] **M · UI cue про `loyalty.spend_enabled`**: `CheckoutInitial.loyaltySpendEnabled` подтягивается SSR через `isLoyaltySpendEnabled()`. `<StepReview>` теперь рендерит `<LoyaltyBlock>` только когда `isAuthenticated && loyaltyBalance > 0 && loyaltySpendEnabled`. Auto-clear `loyaltyPointsToSpend` в `useEffect` при выключенном флаге (юзер мог зайти на /checkout с persisted-значением в localStorage, а admin переключил flag после). Server-guard в `checkout/pay` остаётся как defense-in-depth.
  - [x] **N · E2e в CI workflow**: новый job `e2e` в [.github/workflows/ci.yml](.github/workflows/ci.yml) после `ci` (через `needs:`). Service containers: `postgres:16-alpine` + `redis:7-alpine` с health-checks. Шаги: write `.env` (для dotenv-cli обёртки в seed/playwright `webServer`) → `prisma migrate deploy` → `prisma generate` → `db:seed` → `playwright install --with-deps chromium` → `pnpm e2e` → upload Playwright report артефактом при failure. Все P7-T2 миграции (features-table + loyalty.spend_enabled + brand.maintenance_message) применяются автоматически, e2e может полагаться на seed-row'ы.
  - [x] **O · Первый real-world string consumer — `brand.maintenance_message`**: миграция `20260511240000_feature_brand_maintenance_message` seed'ит string row с `value=""` (banner off по умолчанию). [`server/maintenance.ts`](apps/web/src/server/maintenance.ts) — `getMaintenanceMessage()` (обёртка над `getStringFeature(..., "")`). [`<MaintenanceBanner>`](apps/web/src/components/maintenance-banner.tsx) — server component, renders amber pill-banner с `AlertTriangle` иконкой если message непустой, или `null`. Wired в [layout.tsx](apps/web/src/app/[locale]/layout.tsx) между `<Header>` и `<main>` — banner появляется на всех страницах сайта. `validateFeatureValue("string", ...)` смягчён: пустая строка теперь legitimate "off" state (admin clearing banner = `value=""`). Это первый production-grade consumer `getStringFeature` — API больше не «только для будущего». +5 unit-тестов в новом [`maintenance.test.ts`](apps/web/src/server/maintenance.test.ts) (feature-key, empty=hidden, non-empty=shown, DB-null=hidden, cache hit). **i18n**: +1 ключ `maintenance.ariaLabel` × 3 локали → **1780 keys × 3** (parity ✅). Маленькая семантическая deprecation: `empty_string` error reason остался в union'е, но теперь не возвращается из `validateFeatureValue` — резерв для будущих string-feature'ов где empty действительно invalid.

- **P7-T3** — Отзывы с фото, модерация.
- **P7-T4** — Авто-запрос отзыва после доставки.

### Дальше

- **Фаза 6 (продолжение)** — P6-T2..T8 (dashboard, products CRUD, orders management, payments/refunds)
- **Фаза 7 (продолжение)** — P7-T2..T4
- **Фаза 8** — Доводка и запуск

## Стек

- **Фронтенд:** Next.js 14 (App Router) + TypeScript (strict) + Tailwind CSS + shadcn/ui + Zustand + TanStack Query
- **Бэкенд:** Next.js Route Handlers / Fastify API + WebSocket
- **БД:** PostgreSQL (Prisma ORM), Redis (кеш + BullMQ)
- **Платежи:** **Uniteller** (единственный эквайер) + наложенный платёж (COD)
- **Авторизация:** NextAuth.js (OTP через Eskiz.uz + email/пароль)
- **i18n:** next-intl, локали `ru` (default), `uz`, `en`
- **Хранилище:** S3-совместимое (MinIO в dev, AWS S3/Uzcloud в prod), bucket `bigmax-media`
- **Уведомления:** Telegram Bot (@bigmax_shop_bot), SMS (Eskiz.uz, sender `BIGMAX`), Email (Resend)

## Требования

- Node.js ≥ 20.11 (см. `.nvmrc`)
- pnpm ≥ 9.0
- Docker + Docker Compose (для Postgres / Redis / MinIO / Mailhog / Uniteller mock в dev — появится в P0-T2)

## Установка

```bash
pnpm install
cp .env.example .env  # и заполнить
pnpm run docker:up    # Postgres, Redis, MinIO, Mailhog, Adminer, Uniteller mock
```

## Dev-окружение (Docker)

Все зависимости раннера поднимаются одним `docker compose`. Сервисы:

| Сервис         | Порт | URL / подключение                                          |
| -------------- | ---- | ---------------------------------------------------------- |
| PostgreSQL 16  | 5432 | `postgresql://bigmax:bigmax@localhost:5432/bigmax`         |
| Redis 7        | 6379 | `redis://localhost:6379`                                   |
| MinIO (S3)     | 9000 | `http://localhost:9000` (API), bucket `bigmax-media`       |
| MinIO console  | 9001 | `http://localhost:9001` (bigmax / bigmax-secret-change-me) |
| Mailhog SMTP   | 1025 | SMTP для dev-писем                                         |
| Mailhog UI     | 8025 | `http://localhost:8025`                                    |
| Adminer        | 8080 | `http://localhost:8080` (server: `postgres`)               |
| Uniteller mock | 8787 | `http://localhost:8787` (`/pay/`, `/results/`, `/cancel/`) |

Команды:

```bash
pnpm run docker:up      # поднять стек в фоне
pnpm run docker:down    # остановить
pnpm run docker:logs    # tail логов
pnpm run docker:ps      # статус сервисов
pnpm run docker:reset   # удалить volume'ы (ВНИМАНИЕ: уничтожает данные)
```

Бакет `bigmax-media` создаётся автоматически init-контейнером `minio-init` с публичным download ACL.

Uniteller mock — минимальный стаб (возвращает HTML-страницу выбора исхода на `POST /pay/`, фиктивный XML на `GET /results/` и `POST /cancel/`). Полная логика (проверка подписи, тестовые карты, callback'и, таймауты) будет в P4-T12.

## E2E тесты (Playwright)

```bash
# первый раз — скачать браузеры (~300 MB, кешируется в ~/.cache/ms-playwright)
pnpm --filter @bigmax/web run e2e:install

# локальный прогон (Playwright сам поднимает dev на 3030)
pnpm --filter @bigmax/web run e2e

# интерактивный UI-режим с трейсом
pnpm --filter @bigmax/web run e2e:ui

# против уже поднятого сервера
E2E_BASE_URL=http://localhost:3030 pnpm --filter @bigmax/web run e2e
```

Тесты лежат в [`apps/web/e2e/`](apps/web/e2e). Требуют поднятый `docker:up` (Postgres для создания/удаления эфемерных тест-юзеров, Redis для OTP).

## Worker (BullMQ jobs)

Long-running Node-процесс с BullMQ-очередями. На P4-T9 регистрирует один repeatable job `check-pending-payments` (раз в минуту pull-проверка зависших Uniteller-платежей через `/results/`); в P4-T10 добавится notification-очередь.

```bash
# 1. Поднять Redis + Postgres (если ещё не подняты)
pnpm run docker:up

# 2. Сидить БД (если первый запуск)
pnpm --filter @bigmax/db run db:migrate
pnpm --filter @bigmax/db run db:seed

# 3. Запустить worker (hot-reload через tsx watch)
pnpm run worker:dev

# Без hot-reload, как в проде:
pnpm run worker:start
```

Все секреты worker берёт из корневого `.env` через `dotenv-cli`. Если `UNITELLER_AUTH_LOGIN/PASSWORD` пусты — job логирует warn и возвращает `{scanned: 0}` (dev-окружение без боевых credentials).

Наблюдаемость: ошибки идут через `apps/worker/src/observability.ts::reportError` → `console.error`. С наличием `SENTRY_DSN` ставится тег `[sentry-stub]` (полная Sentry-интеграция — P4-T11 / P8-T3).

В prod worker запускается отдельным контейнером/процессом (PM2 или docker-compose); следующий релиз P4-T10 добавит `apps/worker/Dockerfile`.

## Структура монорепо

```
apps/
  web/              @bigmax/web            — Next.js 14 (витрина + API routes)
  api/              @bigmax/api            — опциональный Fastify API
  worker/           @bigmax/worker         — BullMQ воркеры (уведомления, платежи, PDF)
packages/
  ui/               @bigmax/ui             — shadcn/ui-компоненты общего назначения
  shared-types/     @bigmax/shared-types   — Zod-схемы, brand.ts, locales.ts
  payments/         @bigmax/payments       — Uniteller client, signature, webhook, COD
  notifications/    @bigmax/notifications  — Telegram / SMS / Email шаблоны
  i18n/             @bigmax/i18n           — next-intl конфиг и словари ru/uz/en
```

## Скрипты

```bash
pnpm dev           # dev-сервер всех приложений (параллельно)
pnpm web:dev       # только Next.js storefront
pnpm worker:dev    # только BullMQ worker (P4-T9 pull-проверка платежей)
pnpm build         # prod-билд всех пакетов
pnpm lint          # ESLint
pnpm format        # Prettier
pnpm typecheck     # TypeScript strict
pnpm test          # Vitest unit + Playwright e2e
```

## Коммиты

Используем Conventional Commits. Husky + commitlint проверяют формат автоматически.

```
<type>(<scope>): <subject>

feat(checkout): add Uniteller payment flow
fix(cart): prevent negative quantities
i18n(catalog): add uz translations for filters
payments(uniteller): verify callback signature
```

Доступные scopes: `web`, `api`, `worker`, `ui`, `shared-types`, `payments`, `notifications`, `i18n`, `db`, `auth`, `cart`, `checkout`, `uniteller`, `admin`, `deps`, `config`, `ci`, `release`.

## Бренд

- Название (RU): **Бигмах** · Название (LA): **Bigmax**
- Домен: **bigmax.uz**
- Email: info@bigmax.uz · support@bigmax.uz · orders@bigmax.uz
- Telegram: канал @bigmax_uz, бот @bigmax_shop_bot
- Instagram: @bigmax.uz

## Безопасность

- Секреты Uniteller (`UNITELLER_PASSWORD`, `UNITELLER_AUTH_*`) — **только серверные**. Никогда в клиентский код и `NEXT_PUBLIC_*`.
- Подпись Uniteller всегда вычисляется на сервере.
- Логи платежей — в `PaymentLog` с маскированием PAN (показываем только `last4`).
- Все запросы к Uniteller — HTTPS, timeout 30 сек, retry 3x для идемпотентных операций.
