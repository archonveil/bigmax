{
  "name": "@bigmax/notifications",
  "version": "0.0.0",
  "private": true,
  "description": "Outbound notifications for Бигмах: Eskiz.uz SMS, Telegram bot, Resend email + order templates (P4-T10).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build — consumers import .ts directly'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@bigmax/db": "workspace:*",
    "@bigmax/i18n": "workspace:*",
    "@bigmax/shared-types": "workspace:*",
    "bullmq": "^5.21.2",
    "ioredis": "^5.4.1",
    "next-intl": "^3.22.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.4"
  }
}
