{
  "name": "@bigmax/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Brand constants, locales, Zod enum schemas and small pure utils shared across Бигмах apps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build needed — consumers import .ts directly'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.4"
  }
}
